# Versão mínima de Python exigida pelo projeto: `requires-python` em
# pyproject.toml (achado R3-4, auditoria DL-017 rodada 3) — declarada UMA
# vez só, verificada por apps/core/tests/test_versao_minima_python.py.
# Não repita o número aqui: um comentário desatualizado é pior que nenhum.
Django==6.1.1
djangorestframework==3.18.1
psycopg[binary]==3.3.5
django-environ==0.14.0
gunicorn==26.2.0
whitenoise==6.12.0
# DL-010 F1 (DE-074 item 2): única dependência nova da etapa. Leitura de XML
# de terceiro (NFS-e) com DTD proibido — proteção contra entidade externa e
# expansão exponencial, o ataque clássico a um importador de XML.
defusedxml==0.7.1
