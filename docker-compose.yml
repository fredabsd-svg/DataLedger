services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-dataledger}
      POSTGRES_USER: ${POSTGRES_USER:-dataledger}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-dataledger}
    volumes:
      - dataledger_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-dataledger}"]
      interval: 5s
      timeout: 5s
      retries: 5

  web:
    build: .
    restart: unless-stopped
    env_file: .env
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8000:8000"
    volumes:
      - staticfiles:/app/staticfiles

volumes:
  dataledger_postgres_data:
  staticfiles:
