# Auditoria DL-024 — identidade visual e interface, revisão `5c7303e`

> **Relatório do `auditor-qa` preservado integralmente.** Nenhuma frase editada,
> suavizada ou omitida pelo `arquiteto-senior`. As notas do arquiteto vêm
> **depois**, na última seção. Os sinais `<`, `>` e `&` foram restaurados dos
> escapes de transporte; nada mais mudou.

Servidores encerrados, árvore de trabalho conferida: `git status --porcelain` vazio, `HEAD` em `5c7303e`, sem stash. Toda mutação foi feita em cópias fora do repositório (`/tmp/claude-0/.../scratchpad/mut` e `/scratchpad/ant`, geradas por `git archive`).

## 0. Reprodução dos seus números

| Verificação | Seu número | O meu | Igual? |
|---|---|---|---|
| `ruff check .` | 0 | 0 — "All checks passed!" | sim |
| `ruff format --check .` | 164 | "164 files already formatted" | sim |
| `manage.py check` | limpo | "System check identified no issues (0 silenced)" | sim |
| `pytest` | 1292 passed, 0 xfailed | **1292 passed, 1 warning, 41.23s** | sim |
| varredura de interface | 13 guardas verdes | 13 passed | sim |
| `validate-docs.ps1` | — | **não executei: `pwsh` ausente.** Repliquei as cinco regras em Python: 94 `.md`, 0 problemas |

Ambiente: `.venv` do projeto, Python 3.14.0rc2, PostgreSQL (via `DATABASE_URL` do ambiente) para a suíte; SQLite isolado para as medições em navegador.

---

## 1. Os cinco testes tocados — julgados sem deferência

**Veredito: os cinco continuam medindo. Nenhum foi afrouxado. Mutei todos.**

**(a) Rodapé `"1.500,00"` → `"Débito: 1.500,00"` — não afrouxou.** Mutante em cópia: `views_web.py:1103`, `total_debito += valor` → `total_debito += valor / 2`. Os dois testes morrem:

```
E  AssertionError: ['Débito: 500,00', 'Crédito: 1.000,00']
FAILED test_dl017_rodada1_correcoes.py::test_adicionar_linha_mostra_o_total_real_das_linhas_preenchidas
FAILED test_dl017_rodada2_frontend.py::test_adicionar_linha_anuncia_quantas_linhas_ficam_fora_do_total
```
A captura `class="valor-monetario">([^<]+)<` ficou até **mais estrita**: se o valor sumir, a captura vira `"Débito: "` e também falha.

**(b) Parser de indentação que resolve `var()` — não afrouxou.** Seis mutantes no CSS, todos mortos, e os quatro relevantes morrem também nos testes **textuais** (os que rodam na CI sem Chromium):

| Mutante | Resultado |
|---|---|
| `nivel-5: 5.75rem → 5rem` (passo achatado) | REPROVOU (textual e navegador) |
| ME2 — apagar o prefixo `.tabela-dados td` | REPROVOU (textual: `KeyError: 1`) |
| `nivel-1: var(--esp-3) → var(--esp-4)` | REPROVOU |
| `nivel-0: padding-left: 0` (sem unidade) | REPROVOU — `_resolver_rem` recusa explicitamente |
| `padding` base trocado para `--esp-2` | REPROVOU |
| `--esp-3` redefinido fora do `:root` | REPROVOU |

**(c) `test_views.py` (tenancy) — ficou mais estrito**, não menos: trocou `"Escritório A" in conteudo` (texto solto) por `<strong>Escritório A</strong>` (marcação).

**(d) As duas marcas `xfail(strict=True)` removidas — as dívidas foram fechadas de verdade.** Varri o CSS por conta própria, incluindo cores **nomeadas** que o detector não vê: fora do `:root` sobra apenas `transparent` (3 ocorrências), que não é cor de tinta. E as células de totalizador em `razao.html:96-97` e `lancamento_form.html:167-168` carregam a classe no `<td>`. **A guarda não foi afrouxada para a marca poder sair.**

---

## 2. Achados

Gravidade calibrada pela **DE-038**: nada aqui corrompe dado, erra cálculo, vaza entre empresas, desbalanceia lançamento, altera período encerrado ou derruba servidor. **Nenhum bloqueador.** Confirmei o isolamento: usuário de outro escritório recebe 404 sem vazar razão social nem nome do escritório; papel `cliente` recebe **403** com a tela `erros/sem_permissao.html` dentro da moldura.

### A1 — ALTA — A varredura de interface tem cinco cegueiras demonstradas (achado contra o **seu** mecanismo)

**Requisito:** critério 13 do plano; §5 de `/home/user/DataLedger/docs/projeto/direcao-de-arte.md`.
**Arquivo:** `/home/user/DataLedger/apps/core/tests/test_dl024_varredura_de_interface.py`.

Ataquei a varredura em cópia. O que reprovou **nomeando o arquivo** (bom): `<caption>` removida; cor hex solta em `base.css`; classe de valor removida de `<td>` com `{{ ..._ptbr }}`; `style="..."` com aspas duplas; `<th>` sem `scope`; tela sem `{% extends %}`; `.valor-monetario` sem `tabular-nums`; pasta de módulo sem linha na direção de arte.

O que **passou** — cada linha é uma sabotagem que a CI aprovaria:

| # | Sabotagem | Por quê | Linha |
|---|---|---|---|
| 1 | Classe de valor removida do `<td>` que contém `{% include "_saldo.html" %}` (a coluna **Saldo** do Razão) | `PADRAO_VALOR` exige `{{ ..._ptbr }}`; o valor viaja como `with valor=item.saldo_ptbr` e chega ao parcial como `{{ valor }}`, sem `_ptbr` e sem `<td>` no arquivo | `:50`, `:81-96` |
| 2 | `.teste { color: red; background: white; }` em `base.css` | `PADRAO_COR` só vê hex/rgb/hsl; **cor nomeada é invisível** | `:52` |
| 3 | `static/css/modulos/fiscal.css` com `color: #ff0000` | `ESTILOS.glob("*.css")` não é recursivo | `:203` |
| 4 | `static/tema.css` com cor solta | só varre `static/css/` | `:203` |
| 5 | `style='...'` com **aspas simples**, e `<style>…</style>` dentro do template | regex só cobre `style="[^"]+"` | `:218` |
| 6 | `templates/nota/` (casa com "nota fiscal" no corpo do texto). E mais grave: criei `templates/fiscal/` **e apaguei a linha `\| **Fiscal** \|` da tabela do §3** — passou, porque "Fiscal" sobrevive em outras 4 frases do documento | a guarda faz *substring no documento inteiro*, não "tem linha na tabela" | `:231-239` |

E a varredura **nunca verifica tamanho nem espaçamento**, embora o critério 13 e o §5 digam "cor, **tamanho ou espaçamento** fora dos tokens".

**Impacto:** o critério 13 é o que converte a direção de arte de instrução em garantia — é o argumento do §5 e a lição da BL-271. Metade do que ele promete não é verificado. O Fiscal pode nascer com folha de estilo própria em subpasta, cores nomeadas e `style='...'`, e a CI fica verde.

**Correção recomendada:** varrer `ESTILOS.rglob("*.css")` e `static/**/*.css`; incluir cores nomeadas CSS no `PADRAO_COR`; aceitar `style='...'` e `<style>`; resolver `{% include %}` (ou exigir a classe no parcial) e considerar todo `{{ ... }}` dentro de célula, não só `_ptbr`; trocar o teste de módulo por "existe uma linha `| **Nome** |` na tabela do §3"; acrescentar detector de `px/rem/em` literais em `padding/margin/gap/font-size/border-width`.
**Como verificar:** repetir as seis sabotagens acima; cada uma tem de reprovar nomeando o arquivo.
**Responsável:** `arquiteto-senior` (mecanismo dele) ou `desenvolvedor-pleno`.

### A2 — ALTA — A exigência de acessibilidade que você impôs não tem mecanismo nenhum; mutei e a suíte ficou verde

**Requisito:** exigência do `arquiteto-senior` registrada em `templates/base.html:61-67`; critério 7.
**Arquivos:** `/home/user/DataLedger/templates/base.html`, `/home/user/DataLedger/templates/contabilidade/_navegacao_empresa.html`.

Primeiro: **a afirmação está correta no produto**, e eu medi com a árvore de acessibilidade do próprio Chromium (CDP `Accessibility.getFullAXTree`), não pela aparência:

```
link nome='Balancete'        keyshortcuts='Alt+L'
link nome='Plano de contas'  keyshortcuts='Alt+C'
link nome='Painel'           keyshortcuts='Alt+P'
```
Sem colisão: `p, m, c, i, l, k, n` — sete teclas distintas.

O problema é que **nada guarda isso**. `grep -rn "aria-keyshortcuts\|accesskey\|aria-hidden" apps/ --include=*.py` devolve **zero**. Mutante em cópia: removi todos os `aria-hidden="true"` e troquei o `accesskey="i"` do Diário por `"c"` (colisão com Plano de contas). Resultado:

```
varredura de interface ........ 13 passed
suíte inteira ................. 1292 passed
```
e o efeito medido na árvore de acessibilidade do mutante:
```
link nome='Plano de contas Alt+C' keyshortcuts='Alt+C'
link nome='Diário Alt+I'          keyshortcuts='Alt+C'   <- colisão
link nome='Balancete Alt+L'       keyshortcuts='Alt+L'
```

**Impacto:** exatamente o modo de falha que o comentário de `base.html` diz ter evitado — "um leitor de tela anunciaria 'Balancete Alt L' como se fosse parte do rótulo" — volta em silêncio, com 1292 verdes. É a classe de defeito que a BL-271 nomeou.
**Correção recomendada:** um teste que renderize as seis telas de contabilidade e afirme: todo `kbd.tecla` tem `aria-hidden="true"`; todo elemento com `accesskey` tem `aria-keyshortcuts` coerente; **nenhum `accesskey` se repete na mesma página**; os cinco atalhos da contabilidade existem em todas as telas que incluem a parcial.
**Como verificar:** repetir a mutação acima — o teste tem de morrer.
**Responsável:** `especialista-frontend`.

### A3 — ALTA — Critério 4 não atendido, e o CSS declara que está atendido

**Requisito:** critério 4 do plano — "Totalizador **alcançável sem rolar a tabela inteira**".
**Arquivo:** `/home/user/DataLedger/static/css/base.css:540-546`.

O comentário afirma:
> "aqui a página inteira rola, então o totalizador do rodapé já é alcançável sem rolar 'a tabela inteira' — **critério 4 da DL-024** — pelo scroll normal da página, sem precisar de sticky no rodapé também."

Medido no balancete real (73 contas, 1280×800):

```
altura_pagina=2989  viewport=800
topo_do_rodape=2920 px
rolagem_para_ver_o_total = 2153 px
tfoot_position='static'  linha_total_position='static'
tem_resumo_no_topo=False
```

Para ver o total é preciso rolar **2153 px**, isto é, passar por **todas as 73 linhas**. O comentário não descreve conformidade: redefine o critério até ele ficar vazio ("existe barra de rolagem"). O `thead` sticky funciona (`top: 0` no fim da página), mas ele não é o totalizador.

**Impacto:** o número que fecha o balancete é o menos acessível da tela, no produto cujo argumento de venda é densidade. É também a única leitura que responde "fecha ou não fecha" no balancete — e ela exige percorrer a tabela toda.
**Correção recomendada:** a faixa "Fecha" no topo, que você mesmo apontou como viável só com template (`total_debitos_ptbr` e `total_creditos_ptbr` já chegam na view); ou `tfoot` sticky com `bottom: 0`.
**Como verificar:** repetir a medição — `rolagem_para_ver_o_total` tem de ficar ≤ 0 na entrada da tela, ou existir resumo no topo com os dois totais e a diferença.
**Responsável:** `especialista-frontend`.

### M1 — MÉDIA — Os 15 do gauntlet não se confirmam: **9** medidos no produto

**Requisito:** RC-89 (confirmado pelo Fred); `direcao-de-arte.md` §4.8 e §7; critério 3.

Base sintética que eu criei: **73 contas, 4 níveis, 60 lançamentos** (`/tmp/claude-0/.../scratchpad/semear.py`). Dois servidores, mesma base, mesmo Chromium, 1280×800, contando linha **inteiramente** visível (`top >= 0 && bottom <= innerHeight`):

| Tela | Anterior (`52beee3`) | DL-024 (`5c7303e`) |
|---|---|---|
| Balancete, **sem** período (entrada padrão) | 0 de 73 (topo da tabela: 1025px) | **3** de 73 (625px) |
| Balancete, com período | 1 de 73 (667px) | **9** de 73 (411px) |
| Plano de contas | 9 de 73 (323px) | **14** de 73 (282px) |
| Diário | 4 de 60 (567px) | **11** de 60 (388px) |

O ganho é real e grande — **1 → 9 no balancete filtrado, 9×** — e o critério 3 ("pelo menos o dobro do estado atual") está **atendido com folga**.

Mas os textos que viraram contrato dizem outra coisa:
- RC-89: "A **15**, B 10, C 9, contra ~8 do estado atual".
- `direcao-de-arte.md` §4.8: "A tela de tabela de um módulo novo mostra **pelo menos 15 linhas** em 1280×800 sem rolar. O número existe porque foi medido: o estado anterior mostrava ~8, e a direção escolhida mostra 15."
- §7: "A régua de 15 linhas vale para 1280×800."

**Nenhuma das duas pontas reproduz no produto**: nem os 15 da campeã (medi 9, e 3 na entrada padrão), nem os ~8 do estado anterior (medi 1). A causa aparece na captura e é o anti-padrão que o próprio brief nomeou na variante B: o bloco de filtro do balancete ocupa 411px antes da primeira linha, porque cada campo de data carrega dois parágrafos de ajuda e o "Nível" quebra para a segunda linha. O protótipo da variante A (`/home/user/DataLedger/docs/assets/design/gauntlet/variante-A/02-balancete-1280.png`) resolvia isso numa faixa única de filtro sem ajuda por campo.

Agravante: **os HTML dos três protótipos não estão versionados** — só os PNG. `juiz.py` recebe pasta de variante, e essa pasta não existe no repositório. A medição que decidiu o gauntlet **não é reproduzível**; só as capturas restam.

**Impacto:** um módulo novo que cumprir §4.8 ao pé da letra vai reprovar contra um número que a tela de referência não atinge. E uma afirmação desmentida por medição ficou em dois documentos de contrato — o padrão que o `CLAUDE.md` manda nunca deixar voltar.
**Correção recomendada:** corrigir RC-89 e §4.8/§7 com os números medidos aqui, declarando o método (base, quantidade de contas, URL) — ou reduzir o filtro do balancete até chegar aos 15 e então manter o número. Versionar os HTML dos protótipos.
**Como verificar:** `/tmp/claude-0/.../scratchpad/dens.py` reproduz a tabela acima.
**Responsável:** `arquiteto-senior` (documento) e `especialista-frontend` (filtro).

### M2 — MÉDIA — DE-042 §3 não existe no produto, nas duas telas que ela nomeia

**Requisito:** DE-042, item 3 — *"A resposta 'fecha ou não fecha' é a segunda coisa que o olho encontra, **no balancete e no lançamento**, antes de qualquer ação de gravar"*; critério 5.

Medido: a palavra "fecha" e a palavra "diferen" **não aparecem** em nenhuma das duas telas. No lançamento, após preencher 1.500,00 a débito e 900,00 a crédito e clicar "Adicionar linha", o rodapé mostra `Total conferido antes de gravar | Débito: 1.500,00 | Crédito: 900,00` — dois números, sem veredito, sem diferença, sem mudança visual entre fechar e não fechar. No balancete não há faixa alguma.

Do lado positivo, e medido: a informação existe **sem JavaScript** (`document.querySelectorAll('script').length === 0` em todas as telas) e a gravação desbalanceada é recusada no servidor com texto — `"Erro: Débitos (1.500,00) e créditos (900,00) precisam ser iguais antes de gravar."`

**Impacto:** o "momento da verdade" do Contábil, escrito no §3 da direção de arte, é deixado para a subtração mental do contador na tela onde ele digita.
**Responsável:** `especialista-frontend` (uma linha de veredito no template basta) / `desenvolvedor-pleno` se exigir a diferença calculada na view.

### M3 — MÉDIA — Critério 8: cores estão limpas, **medidas não**

**Requisito:** critério 8 — "nenhuma cor **ou medida** solta fora das variáveis CSS"; e a regra que o próprio arquivo declara no topo (`base.css:10-13`).

Varredura minha, fora do `:root`, ignorando comentários:
- cores: **0** (só `transparent`) — confirmado;
- medidas soltas em propriedades de espaço/tipo: **7** — `gap: 0.4em` (×3, linhas 284/346/358), `font-size: 0.72em` (317), `gap: 0.1rem` (374), `margin: 0.25rem 0 0` (509), `margin-left: 0.3em` (624);
- mais ~15 larguras de borda literais (`1px`, `2px`, `3px`) e `letter-spacing: 0.05em`/`0.02em`, sem token correspondente.

O arquivo abre dizendo "**REGRA DESTE ARQUIVO: nenhuma cor ou medida solta fora das variáveis do `:root`. Se um valor não existe aqui, ele nasce aqui**" e depois não cumpre a metade "medida".
**Responsável:** `especialista-frontend`.

### M4 — MÉDIA — DE-042 §1: a hierarquia por **peso** não foi implementada, e a coluna "Nível" continua sendo o sinal

**Requisito:** DE-042 §1 — "A hierarquia do plano de contas vem de **peso e recuo**, não de uma coluna 'Nível' nem de cor".

Medido no balancete, por nível:

```
nível 1 (ATIVO)            classe=nivel-1 peso=400 padding-left=12px
nível 2 (ATIVO CIRCULANTE) classe=nivel-2 peso=400 padding-left=32px
nível 3 (Disponível)       classe=nivel-3 peso=400 padding-left=52px
nível 4 (Caixa geral)      classe=nivel-4 peso=400 padding-left=72px
```

O recuo existe e é aditivo. O **peso é 400 em todos os quatro níveis** — sintética e analítica têm exatamente o mesmo peso. E a primeira coluna da tabela continua sendo `Nível`. O protótipo vencedor fazia o contrário, e escrevia isso na própria legenda ("Peso maior e recuo indicam conta sintética… peso normal é conta analítica").
**Responsável:** `especialista-frontend`.

### M5 — MÉDIA — Parênteses de saldo invertido ausentes no balancete, com efeito visível em dado real

**Requisito:** RC-89 e DE-042 §2 (a convenção vale para o produto, não para uma tela).

Medido: `.valor-invertido` no Razão = 5 ocorrências; **no balancete = 0**, embora a base tenha o caso clássico — conta `1 ATIVO` (natureza devedora) com saldo final `13.366,61 C`. A tela onde o contador procura anomalia é justamente a que não marca.

Você declarou a lacuna e atribuiu a causa a `views_web.py` estar fora do escopo do frontend. **Confirmo a causa** (o balancete só recebe `saldo_final_natureza`, o sinal computado, não a natureza cadastrada por linha) e confirmo o **efeito**: é uma mudança de view, não de template — `desenvolvedor-pleno`.

### M6 — MÉDIA — Critério 9: a impressão sai com a marca do fornecedor

Medido com `emulate_media(media="print")`: corpo `rgb(0,0,0)` sobre `rgb(255,255,255)`, links e `th` em preto, filtro e navegação ocultos, faixa de contexto preservada com `"Escritório ativo Escritório Contábil Sintético ME"`. Legível, econômico — **e com `DataLedger.` no topo**. É o "vício nacional a não herdar" nomeado no item 4 da pesquisa do próprio plano. Confirmo sua declaração.

Observação adicional não coberta: não há `@page { size: A4; margin }` nem `break-inside: avoid` em `tr`. Emulei mídia de impressão, **não medi paginação A4 real** (ver §4).

### B1 — BAIXA — Rótulos de contexto inconsistentes na faixa que a DE-042 acabou de redesenhar

`base.html:97,102` usa versalete sem pontuação e valor em `<strong>` ("USUÁRIO / **auditor**"). `balancete.html:7,12` e `razao.html:7,12` mantêm `Empresa:` e `Período:` com dois-pontos e valor em peso normal. Aparece lado a lado na mesma faixa em toda tela de contabilidade — é o defeito visível na captura `/tmp/claude-0/.../scratchpad/capturas/razao.png`. Foi por essa mudança que o teste de `tenancy` foi tocado; metade da faixa ficou para trás.

### B2 — BAIXA — O atalho à mostra é renderizado a 9,36px

`kbd.tecla { font-size: 0.72em }` sobre `--tipo-sm` (13px) = **9,36px medidos**. Contraste ok (5,14:1), mas é o menor texto da interface, e ele existe para ser lido. A medida também é uma das "medidas soltas" do M3.

### B3 — BAIXA — Duas telas de contabilidade ficaram sem os cinco atalhos

`_navegacao_empresa.html` é incluída em 6 dos 8 templates de `templates/contabilidade/`. Ficaram de fora `/home/user/DataLedger/templates/contabilidade/conta_form.html` e `/home/user/DataLedger/templates/contabilidade/lancamento_detalhe.html`. Do detalhe do lançamento — para onde se chega clicando em qualquer linha do Razão ou do Diário — o usuário perde os cinco atalhos e precisa voltar. O comentário da parcial diz "toda tela de contabilidade" e depois lista seis.

### B4 — BAIXA — Estado de erro e estado vazio deixam a tela quase toda em branco

Com período inválido, a página mostra a mensagem, o filtro e **nada mais** (`/tmp/claude-0/.../scratchpad/capturas/erro.png`). Com período sem movimento, 3 linhas visíveis de 73. Não é defeito de conformidade — o critério 6 pede o estado desenhado, e ele existe com texto — mas é o oposto da tese de densidade.

---

## 3. O que NÃO regrediu — verificado, não assumido

| Conquista DL-009/DL-017 | Como medi | Resultado |
|---|---|---|
| Contraste 4,5:1 / 3:1 | juiz adaptado às URLs do servidor, composição de alfa **de fundo e de cor de texto**, 11 telas | **0 pares reprovados** |
| Contraste do estado anterior | mesma sonda em `52beee3` | 1 reprovado (`th` "Débitos", **4,46:1**) — a reescrita **corrigiu** uma falha preexistente |
| Foco visível | comparação do estilo calculado antes/depois de `focus()`, 40 elementos | 39/40 mudam; o único que não muda é o `input[type=hidden]` do CSRF. Anel `3px solid #1D3A6E`, offset 2px, **10,51:1** sobre o papel |
| Tabulação de algarismos | largura de `111111` vs `888888` no elemento-folha | **373/373** no balancete, 122/122 no diário, 21/21 no razão. Estado anterior: **0/373** |
| `D`/`C` explícito | leitura do DOM | presente, com extenso em `.visualmente-oculto` ("13.366,61 C (credor)") |
| Mensagem de erro com texto | fluxo real de gravação desbalanceada | `"Erro: Débitos (1.500,00) e créditos (900,00) precisam ser iguais antes de gravar."` |
| Sem permissão | login com papel `cliente` | **403**, template próprio, dentro da moldura, mensagem e link de volta |
| Isolamento entre escritórios | login com usuário de outro escritório | 404, e **não vaza** razão social nem nome do escritório alheio |
| Sem rolagem horizontal em 1280 | `scrollWidth` vs `innerWidth` | `false` em 11 telas |
| Sem dependência externa / sem JS | listener de requisições + contagem de `<script>` | 0 requisições externas, 0 scripts em todas as telas. Fonte auto-hospedada com `OFL.txt` versionado junto |
| `caption` / `th[scope]` | DOM | 11/11 `th` com scope no balancete; caption em todas as tabelas |

---

## 4. O que eu **não** consegui medir

1. **`validate-docs.ps1`** — `pwsh` não existe nesta máquina. Repliquei as cinco regras em Python (94 `.md`, 0 problemas). Não é a mesma coisa que rodar o script da CI.
2. **Leitor de tela real** — nenhum. Medi a **árvore de acessibilidade calculada pelo Chromium**, que é marcação correta, não uso real.
3. **Firefox e Safari** — nenhum. Só Chromium 1194.
4. **A medição que decidiu o gauntlet** — irreproduzível: os HTML dos três protótipos não estão no repositório, só os PNG. Pude re-medir o produto e a revisão anterior, não as variantes.
5. **Paginação A4 real** — emulei `media=print` na tela; não gerei PDF nem conferi quebras de página, repetição de `thead` entre folhas ou margens.
6. **Contraste de borda de componente (WCAG 1.4.11)** em todos os componentes — medi só o indicador de foco (10,51:1). `--borda-input` declara 3,54:1 e eu não conferi elemento a elemento.
7. **1920×1080** — medi só 1280×800.
8. **Renderização sobre PostgreSQL** — as telas foram medidas com SQLite em cópia isolada. A suíte (1292) rodou sobre PostgreSQL.

---

## 5. A resposta direta, com número

> **A razão pela qual esta direção venceu o gauntlet se confirma no produto, ou só no protótipo?**

**Só no protótipo.** A campeã venceu por **15 linhas** contra 10 e 9. No produto, com plano de contas realista (73 contas, 4 níveis) em 1280×800, o balancete mostra **9** linhas inteiras com período filtrado e **3** na entrada padrão sem período — abaixo das 10 do protótipo da variante B e empatado com as 9 do protótipo da C. A ordem de classificação **não se confirma**.

O que **se confirma** é o ganho: a mesma tela, com os mesmos dados, mostrava **1** linha antes (`52beee3`). De 1 para 9 são **9×**, e o critério 3 do plano ("pelo menos o dobro do estado atual") está atendido com folga. O que não se sustenta é o **15** — que está escrito como número medido em RC-89, em `direcao-de-arte.md` §4.8 e §7, e que nenhum mecanismo verifica, porque a varredura é estática e não mede densidade.

A causa é identificável e corrigível: 411px de bloco de filtro antes da primeira linha, pelo mesmo motivo que fez a variante B cair de 15 declaradas para 10 medidas — dois parágrafos de ajuda por campo de data e o "Nível" quebrando linha. O protótipo vencedor não tinha isso.

---

## 6. Parecer

# REPROVADO

Três achados de gravidade **alta**, nenhum bloqueador:

- **A1** — a varredura de interface, que é o critério 13 e o que torna a direção de arte garantia em vez de pedido, tem **cinco cegueiras demonstradas por sabotagem**, incluindo a rota de fuga que o próprio `_saldo.html` desta etapa abriu. É a BL-271 de novo: guarda que não guarda desliga a próxima medição.
- **A2** — a exigência de acessibilidade que você impôs **está correta no produto e medida**, mas **não tem mecanismo**: removi todo `aria-hidden` e criei colisão de `accesskey`, e a suíte devolveu **1292 passed**.
- **A3** — o **critério 4 não está atendido** (2153px de rolagem para alcançar o total) e um comentário em `base.css:540-546` afirma que está, redefinindo o critério.

O resto da entrega é sólido e eu o registro sem reservas: contraste calculado com **zero** reprovações em 11 telas (inclusive corrigindo uma falha de 4,46:1 que existia antes), foco visível medido a 10,51:1, tabulação de 0/373 para 373/373, zero dependência externa, zero JavaScript, isolamento entre escritórios preservado com 403/404 sem vazamento, e os cinco testes tocados **não foram enfraquecidos** — provei mutando cada um. A densidade melhorou de verdade, 9× no balancete.

Pela **DE-038**, nada aqui está na lista de dano (dado, cálculo, isolamento, partida dobrada, período encerrado, disponibilidade), e é legítimo você converter M1–M6 e B1–B4 em backlog nomeado. **A1, A2 e A3, não**: as duas primeiras são mecanismo que declara guardar e não guarda, e a terceira é um critério de aceite do plano declarado atendido por um comentário. Essa é a falha que este projeto repete, e é a única razão pela qual esta auditoria existe.

Não afirmo ausência de defeitos, segurança absoluta nem conformidade contábil ou legal: auditoria de software não substitui validação profissional das regras contábeis e legais.

**Evidências:** capturas em `/tmp/claude-0/-home-user-DataLedger/492d5b99-2064-5537-b2bb-d0e317702949/scratchpad/capturas/` (produto) e `.../capturas-ant/` (estado anterior); instrumentos em `.../scratchpad/medir.py`, `medir2.py`–`medir8.py`, `ax2.py`, `dens.py`, `sabota.sh`, `sabota2.sh`, `mut_css.sh`; semeadura sintética em `.../scratchpad/semear.py`. Nenhum arquivo do repositório foi alterado.

---

## Notas do `arquiteto-senior`, escritas DEPOIS do relatório

**Parecer aceito integralmente. Nenhum achado contestado.** Dois dos três altos
são contra mecanismo **meu**, e o M1 é contra uma afirmação **minha** que virou
contrato — foi a primeira coisa que eu corrigi, antes de distribuir qualquer
tarefa.

**Sobre o M1, que é o achado mais sério desta rodada, ainda que classificado
como médio.** Eu escrevi "15 linhas" em **dois documentos de contrato** — RC-89
e a direção de arte — como se fosse número medido **no produto**. Era número
medido **no protótipo**. O auditor mediu o produto com base realista e achou
**9** com período filtrado e **3** na entrada padrão.

Isso é exatamente o defeito que este projeto persegue desde 2026-09-13: uma
afirmação desmentida por medição, escrita num lugar que outras pessoas vão usar
como verdade. Pior: eu a transformei em **régua para módulos futuros** — o
Fiscal e a Folha iam ser medidos contra um número que a própria tela de
referência não alcança.

**O que eu fiz, e o que eu não fiz:** corrigi os dois textos com os números
medidos e o método declarado (quantas contas, qual tela, qual largura). **Não**
abaixei a régua para o que a tela entrega hoje: os 15 continuam como **meta
registrada**, com a causa nomeada — 411px de filtro antes da primeira linha — e
com item de backlog próprio. Abaixar a régua para caber no resultado seria
trocar a medição pela conveniência.

**Sobre o A1, a minha varredura.** Ela reprovou oito sabotagens e deixou passar
seis. A mais constrangedora é a nº 1: a rota de fuga foi aberta **pelo parcial
`_saldo.html` desta mesma etapa** — o valor viaja como `{{ valor }}` e o meu
detector exige `_ptbr`. Eu escrevi a guarda e a própria entrega que ela deveria
guardar abriu o buraco no mesmo commit.

**Sobre o A3, e ele merece um nome.** Um comentário no CSS afirma que o critério
4 está atendido porque "a página inteira rola". O critério diz "alcançável sem
rolar a tabela inteira", e a medição é **2153px de rolagem, 73 linhas**. Não é
uma interpretação generosa: é redefinir o critério até ele ficar vazio. Isso é a
família da BL-142 e da BL-246 — comentário que afirma mais do que a defesa
entrega — na sua forma mais direta, porque aqui o comentário **cita o número do
critério** que está descumprindo.

**Encaminhamento:**

| Achado | Responsável | Rodada |
| --- | --- | --- |
| **M1** (texto de RC-89 e da direção de arte) | `arquiteto-senior` | feito nesta mesma entrega |
| **A1** (cegueiras da varredura) | `desenvolvedor-pleno` | 2 |
| **A2** (mecanismo da acessibilidade), **A3** (totalizador), **M2** (veredito "fecha"), **M3** (medidas soltas), **M4** (hierarquia por peso), **B1**, **B2**, **B3** | `especialista-frontend` | 2 |
| **M1** (filtro de 411px), **M5** (parênteses no balancete, precisa de view), **M6** (timbre do escritório), **B4** | backlog nomeado | própria |

**O que eu NÃO vou fazer, e declaro:** não vou transformar "leitor de tela real"
em critério desta etapa. Ninguém da equipe tem um, e fingir que a árvore de
acessibilidade do Chromium substitui NVDA seria a mesma classe de erro que o
A3. Fica como limite escrito na direção de arte, onde já está.
