# DL-035 — parecer da primeira auditoria independente

## Transcrição integral

```text
## Parecer: REPROVADO

As guardas BL-514 a BL-519 passaram nas verificações locais que executei. Encontrei uma inconsistência documental no estado do commit auditado, que precisa de correção e reconferência.

### Matriz de critérios

| Critério | Resultado | Evidência |
|---|---|---|
| BL-514 | Atendido | A suíte ponta a ponta passou. O controle confirma seis páginas, bloco e nota em todas; `transparent` e `#FFFFFF` reprovam com `CONTRASTE`; as seis sabotagens anteriores continuam reprovando. |
| BL-515 | Atendido | Seis casos comportamentais isolados passaram. Mover cada veto para avisos fez a emissão passar e o contrato matou cada mutante, nomeando a lista. |
| BL-516 | Atendido | A apuração nomeia as raízes `1` e `2`; tipos diferentes não são agrupados. L1/L2 continuam passando. A tela mostra o aviso sem inventar ancestral comum, enquanto o resíduo de R$ 200 ainda recusa a emissão. |
| BL-517 | Atendido | A nota é uma `div` irmã da identificação normativa, no mesmo cabeçalho repetido; o controle PDF encontra a nota nas seis páginas. |
| BL-518 | Atendido | A varredura deriva os modificadores dos templates e exige `display: none` em `@media print`; retirar cada regra deixa a classe órfã nomeada. |
| BL-519 | Atendido | O parser lê `<div>` aninhadas até o fechamento correspondente e recusa o bloco incompleto; os testes cobrem os dois casos. |

**Achado documental — gravidade baixa.** Requisito: manter o estado da etapa fiel ao Git. Em [DL-035](docs/planos/DL-035-as-guardas-da-demonstracao.md:3), o plano diz “Ainda sem commit”; em [estado.md](docs/agents/estado.md:279), o texto diz “sem commit” e lista o commit como pendente. Porém, `git rev-parse HEAD` confirma que o commit `38e7a6b2e0dfa4c6dff558d0b48b034c72b0ff43` já existe. Isso torna a trilha de estado inexata. Corrigir os dois registros para apontar esse commit local e deixar push, PR e CI como pendentes; depois, reconferir o novo SHA.

### Verificações executadas

O checkout auditado foi `38e7a6b2e0dfa4c6dff558d0b48b034c72b0ff43`, filho de `22a0241efe174bf5bbcf1fd3ac1ea7d979a61a53`. Antes dos testes, `git status --porcelain` e `git diff --stat HEAD` estavam vazios.

Confirmei o banco-alvo com `docker exec dl035-integrated-test psql ...`; ele é PostgreSQL 16.15. Os testes Django foram direcionados ao banco descartável em `127.0.0.1:32771`. Após os testes, a consulta por bancos `test_%` não retornou resultados.

- `pytest -q apps/contabilidade/tests/test_dl034_balanco_patrimonial.py apps/contabilidade/tests/test_dl034_tela_do_balanco.py` — **42 passed in 34.16s**.
- `DL_PYTHON_DO_SISTEMA=/tmp/dl035-venv/bin/python pytest -q scripts/test_medir_identificacao_do_emitente.py` — **135 passed in 211.04s**.
- Probes BL-515 — **6 casos isolados aprovados; 6 mutantes mortos e nomeados**.
- `ruff check .` — `All checks passed!`
- `ruff format --check .` — `225 files already formatted`
- `manage.py check` — `System check identified no issues (0 silenced).`
- `git diff --check BASE HEAD` — saída vazia, código 0.
- O diff contém dez arquivos e nenhuma migração. A árvore permaneceu limpa após os testes: HEAD inalterado, `git status --porcelain` e `git diff --stat HEAD` vazios.

### Limitações

Não executei o workflow integral da CI para a DL-035; ele ainda precisa rodar no PR mais recente. Também não repeti a suíte completa: o registro do projeto documenta uma falha local em Python 3.12 no teste de sintaxe PEP 758, que exige Python 3.14. O `--collect-only` conhecido reporta 2210 coletados, mas sai com código 1 pela conferência BL-218; não o contei como teste aprovado.

**SHA e árvore:** antes e depois dos testes, `HEAD` continuou em `38e7a6b2e0dfa4c6dff558d0b48b034c72b0ff43`; status e diff de trabalho vazios.
```

## Reconferência única

### Transcrição integral

```text
**Parecer: APROVADO** para esta reconferência documental.

- O checkout está no commit `75a3530b529cd737e884f3442bcdf0ea00198f45`, filho de `38e7a6b2e0dfa4c6dff558d0b48b034c72b0ff43`. O HEAD aponta para a árvore `fbc7fdb28c334b7f54225a23983f2cd2e5f63073`, igual à árvore informada.
- Antes e depois da inspeção, `git status --porcelain` e `git diff --stat HEAD` estavam vazios.
- [estado.md](/home/fred/Documentos/DataLedger/docs/agents/estado.md:276) e [o plano DL-035](/home/fred/Documentos/DataLedger/docs/planos/DL-035-as-guardas-da-demonstracao.md:3) reconhecem o commit local de implementação `38e7a6b2…`, registram a reconferência e mantêm push, PR e CI pendentes. Não encontrei a afirmação antiga de que ainda não havia commit como estado atual.
- Ambos apontam para [o relatório da rodada 1](/home/fred/Documentos/DataLedger/docs/auditorias/2026-09-25-dl-035-rodada-1.md), que contém a transcrição integral do parecer, incluindo matriz, achado, evidências e limitações.
- O commit de reconferência altera somente documentação: `estado.md`, o plano e o relatório. `git diff --check HEAD` terminou sem saída; não alterei arquivos.

**Ressalva:** esta reconferência não executou a CI. Os workflows da DL-035 continuam pendentes e ainda precisam passar antes de declarar a etapa integrada.
```
