# Auditoria independente — DL-036

**Versão auditada:** `353c9ed065c2a4dcb1dc5b0f36a3fde88aef5030`
**Comparação:** `main...HEAD`
**Escopo:** entrada pública, cadastro, autenticação, criação atômica de escritório e isolamento. Nenhum arquivo foi editado pelo auditor.

## Resultado

Não identifiquei falha bloqueadora ou de alta gravidade nas alterações examinadas. Os critérios essenciais do cadastro e da entrada pública foram atendidos no ambiente local testado.

**Inspecionado:**

- A página `/` mostra apresentação pública para visitantes e preserva o painel para usuários autenticados.
- Cadastro e login têm caminhos visíveis entre si.
- O cadastro cria usuário comum, escritório próprio e vínculo administrador; não concede `is_staff`, `is_superuser` ou vínculo com escritório existente.
- O serviço existente de criação do escritório permanece dentro de uma transação externa que inclui a criação do usuário.
- CNPJ e e-mail são normalizados; duplicidades são verificadas no formulário e protegidas por unicidade no banco.
- Não há descoberta nem aceitação automática de convites por e-mail.
- Senhas recebem os validadores configurados do Django, hash pelo gerenciador de usuários e proteção de parâmetros sensíveis.
- A autenticação conserva o nome exato de contas legadas e permite variação de caixa para o novo login por e-mail.
- A apresentação usa exemplos expressamente identificados como demonstrativos.
- Capturas desktop da landing e mobile do cadastro foram examinadas visualmente: ações legíveis, formulário completo e disposição compatível com as dimensões exibidas.

## Evidência executada pelo auditor

Comando:

```bash
env DJANGO_SECRET_KEY=teste-local DEBUG=True \
DATABASE_URL=sqlite:////tmp/dl036-audit.sqlite3 \
/tmp/dl036-venv/bin/python -m pytest \
apps/accounts/tests/test_signup.py \
apps/tenancy/tests/test_isolamento.py -q
```

Saída:

```text
..............................                                           [100%]
BL-218: conferência do elo de execução pulada (execução parcial — ela só vale para a suíte inteira).

30 passed in 1.40s
```

Também executei experimento independente via Python/Django, em banco de teste descartável criado por `setup_databases`, sem modificar os testes versionados. Foram exercitados:

1. GET das três páginas públicas.
2. Recusa de métodos de escrita não contratados.
3. Colisão **real da restrição SQL de CNPJ**, introduzida após a validação do formulário, comprovando rollback de usuário, escritório e vínculo.
4. Entrada HTML maliciosa no nome, com erro de confirmação de senha: conteúdo escapado, campo não sensível preservado e senha ausente do HTML.
5. Cadastro válido, adulteração do escritório ativo na sessão e consulta ao painel: escritório terceiro permanece inacessível.
6. Logout e novo login com `next` apontando para domínio externo: redirecionamento externo recusado.

Saída:

```text
PASS: three public GET routes accessible
PASS: signup and root method restrictions
PASS: real SQL unique collision rolls back entire outer transaction
PASS: invalid form retains escaped non-secret input and never password
PASS: tampered tenant session ignored and external login redirect refused
```

Verificação final:

```bash
git status --short
git diff --stat
git rev-parse HEAD
```

Os dois primeiros comandos não produziram saída. O terceiro confirmou:

```text
353c9ed065c2a4dcb1dc5b0f36a3fde88aef5030
```

A árvore permaneceu limpa.

## Limites e pendências de entrega

- **Não testado pelo auditor:** execução do contêiner Docker, PostgreSQL, disputa realmente simultânea entre conexões, pipeline remota e suíte completa. A colisão SQL exercitada comprova o rollback por unicidade, não constitui teste de carga ou concorrência PostgreSQL.
- **Inspecionado, com limite declarado:** não há confirmação de titularidade de e-mail ou CNPJ nesta etapa. A conta criada obtém somente seu ambiente novo; os testes não encontraram concessão de acesso preexistente.
- **Informado pelo implementador, não reproduzido nesta auditoria:** falha preexistente da suíte completa envolvendo duplicidade de código contábil em SQLite, reproduzida na `main`. Este relatório não declara a suíte integral aprovada.
- Documentação final de evidências, push, PR e CI permanecem responsabilidade da entrega. O parecer técnico não equivale a autorização de merge ou implantação.

**APROVADO**

## Reconferência única — ajuste Docker

**Commit:** `9cbf102c9aa4978d70efb19fd3983f8eabf502cb`.

**Inspecionado:** o delta remove somente a montagem e declaração do volume `staticfiles`. O Dockerfile já coleta os estáticos na imagem, servidos por WhiteNoise. A retirada impede que um volume antigo encubra o CSS novo após reconstrução. O volume PostgreSQL foi preservado integralmente; nenhum comando de exclusão de volumes foi introduzido.

**Testado:** leitura do YAML com PyYAML e assertivas sobre ausência da montagem no serviço web, preservação do volume PostgreSQL e presença de `collectstatic` no Dockerfile.

```text
PASS: YAML parse, no web static mount, PostgreSQL volume preserved, image collects static assets
```

A primeira tentativa no virtualenv falhou por ausência de PyYAML; a execução acima passou no Python do ambiente, sem instalar dependências.

**Não testado:** inicialização e atualização reais de contêineres, pois Docker não está disponível. Não apresentar essa inspeção como execução Docker comprovada.

Nenhum código Python ou template mudou desde a auditoria inicial. A árvore estava limpa na reconferência. O ajuste é pertinente ao pedido e não altera o parecer técnico anterior.

**APROVADO**
