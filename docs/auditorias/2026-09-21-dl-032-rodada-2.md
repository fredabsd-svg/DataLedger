# Reconferência da DL-032, fatia 1 — rodada 2 (última, §3.1)

> **Nota do `arquiteto-senior`, e é só esta:** relatório do `auditor-qa`,
> preservado **integralmente** — inclusive a seção final, em que ele registra
> que **a minha principal preocupação desta rodada não se realizou**, e explica
> por que ela não era 50/50. Eu pedi essa seção; é a que mais ensina.
>
> ⚠️ **E o achado R1 existe por causa de uma instrução específica minha:**
> *"confirme com o SEU mutante, não com o dele"*. O único mutante que sobreviveu
> foi justamente o que o implementador **não** escreveu. A regra pagou uma vez
> nesta rodada, e uma vez bastou.

---

**Revisão reconferida:** `2e4ad02e766201ca3dd96f4a45d08ad278587f34` (correção) contra `b73c7294983cf5da4c81b127dd4483caa6219ff6` (rodada 1), branch `claude/accounting-agent-team-setup-mn6lyf`. Árvore limpa antes e depois: `git status --porcelain` vazio, `git diff --stat` vazio, HEAD inalterado, nenhuma worktree residual, três bancos criados por mim e dropados.

---

# PARECER: APROVADO COM RESSALVAS

**Motivo, em uma frase:** o achado alto (A2) está fechado com declaração e não com movimento de dinheiro — a agregação por raízes continua intacta e provada por mutação —, os oito achados da rodada 1 estão todos atendidos e medidos por mim, e o que sobra são **três ressalvas de gravidade baixa**, sendo uma delas achada agora por sonda nova: uma frase do docstring que afirma mais do que o código faz.

Nenhuma ressalva esconde falha essencial. Nenhuma é bloqueadora, alta ou média. Declaro cada uma abaixo, com reprodução.

---

## V4 — Não-regressão: os números que EU medi

| Verificação | Comando | Resultado medido por mim | Declarado por ele | Bate? |
| --- | --- | --- | --- | --- |
| Suíte completa | `pytest` (banco próprio `test_dataledger_auditoria_dl032`) | **2035 passed, 14 skipped, 2 warnings, 4 subtests passed em 105,02 s** | 2035 passed, 14 skipped | **Sim** |
| Lint | `ruff check .` | `All checks passed!` — exit 0 | sem alteração | Sim |
| Formatação | `ruff format --check .` | `210 files already formatted` — exit 0 | sem alteração | Sim |
| Django | `manage.py check` | `no issues (0 silenced)` — exit 0 | sem alteração | Sim |
| Migração nova | `makemigrations --check --dry-run` | `No changes detected` — **exit 0, sem migração nova** | sem alteração | Sim |
| Migrar em banco vazio | `migrate` em `dl032_r2_migrate` criado do zero | todas OK — exit 0 | — | Sim |
| Desempenho | teste dele, minha máquina | **0,0085 s, 3 consultas** (73 contas / 4 níveis / 60 lançamentos) | 0,008 s, 3 consultas | Sim |

**+10 testes** (2025 → 2035), exatamente o declarado. **Classificação: Testado.**

Nunca rodei `ruff format` sem `--check`. Toda mutação em worktree `--detach`. Banco próprio (BL-273). Nenhum comando de ambiente proibido.

---

## V1 — BL-475, o achado alto: FECHADO

### A sonda P2, refeita por mim

**Classificação: Testado.** Reconstruí a sonda P2 da rodada 1 (raiz `1` ATIVO, `1.1` Caixa, `1.9` marcada `despesa` pendurada sob a raiz do Ativo, mesmos dois lançamentos):

```
R1 divergentes: [{'conta': '1.9', 'nome': 'Despesa mal cadastrada',
                  'tipo': 'despesa', 'tipo_da_raiz': 'ativo'}]
R1 totais:      {'ativo': '1000.00', 'passivo': '0',
                 'patrimonio_liquido': '1000.00', 'receita': '0', 'despesa': '0'}
R1 equacao diferenca: 0.00
```

A conta **é nomeada**, com o `tipo` próprio **e** o `tipo_da_raiz`, e com o nome legível — o contador tem o que precisa para agir. O dicionário deixou de se contradizer em silêncio: a linha diz `despesa`, `totais_por_tipo` diz `ativo`, e agora **a própria resposta explica por quê**. Era isso que faltava.

### Controle positivo: a lista sai VAZIA no caso são

**Classificação: Testado.** `test_cenario_correto_nao_tem_conta_com_tipo_divergente_da_raiz` afirma `== []` no `cenario_completo`, e minha sonda R5 confirma o mesmo em árvore plana e em empresa sem contas. **A guarda não acusa tudo**, então não vira ruído a ser ignorado em duas semanas.

### Mutação: com o MEU mutante, não com o dele

**Classificação: Testado.** Dois mutantes próprios contra a detecção, mais dois contra o campo que a alimenta:

```
PEGOU | N1 detecção de divergência ZERADA (`if False`)        -> test_conta_descendente_com_tipo_diferente_da_raiz_e_declarada...
PEGOU | N2 detecção de divergência SEMPRE (`if True`)         -> test_cenario_correto_nao_tem_conta_com_tipo_divergente_da_raiz
PEGOU | N3 tipo_da_raiz = o próprio tipo (sempre igual)       -> test_conta_descendente_com_tipo_diferente_da_raiz_e_declarada...
SOBREVIVEU | N4 tipo_da_raiz = tipo do PAI direto             27 passed
```

Os dois lados da guarda morrem: zerar a detecção mata o teste do caso torto, e ligá-la sempre mata o controle positivo. **É uma guarda de dois lados, e é o formato certo.** O N4 é ressalva R1 abaixo.

### ⚠️ E a agregação NÃO mudou — este era o seu risco maior, e ele não se realizou

**Classificação: Testado.** M4 e M5 refeitos contra a suíte nova:

```
PEGOU | M4 soma TODAS as linhas (não só raízes) | 8 failed, 19 passed
        -> test_retificadora_dentro_do_patrimonio_liquido_subtrai_nunca_soma
        -> test_apos_zeramento_resultado_e_zero_e_equacao_fecha_na_forma_classica
        -> test_totais_por_tipo_valores_conferidos_a_mao  (+5)
PEGOU | M5 soma só as folhas (analíticas)       | 7 failed, 20 passed
        -> test_retificadora_dentro_do_patrimonio_liquido_subtrai_nunca_soma
        -> test_apos_zeramento_resultado_e_zero_e_equacao_fecha_na_forma_classica  (+5)
```

Continuam morrendo, e **matam mais testes do que na rodada 1** (M4: 6 → 8; M5: 5 → 7). A retificadora do RC-104 está protegida. E a prova independente disso é o número: na sonda R1, `totais_por_tipo['ativo']` é **1.000,00** e `diferenca` é **0,00** — exatamente o que eu medi na rodada 1, antes da correção. **A correção não moveu um centavo.** Ele só acrescentou a declaração, que é precisamente o que eu recomendei e o que o plano permite.

---

## V2 — BL-477 e o risco escondido: a fixture acordou uma guarda e NÃO adormeceu nenhuma

### A guarda do critério 1 agora existe por direito

**Classificação: Testado.**

```
PEGOU | M1 — SÓ test_apurar_saldos_bate_com_apurar_balancete_conta_a_conta | 1 failed
PEGOU | M1 — SÓ test_lancamento_no_dia_exato_da_data_base_entra_no_saldo   | 1 failed
```

Na rodada 1 os dois equivalentes eram `SOBREVIVEU`. A mudança de `date(2026, 1, 20)` para `jan` (= `data_base`) em `cenario_completo` fez o teste do critério 1 **reprovar rodado sozinho**, e o teste nomeado da fronteira (`test_lancamento_no_dia_exato_da_data_base_entra_no_saldo`, com fixture dedicada) reprova sozinho também. **A guarda deixou de ser acidente.**

### ⚠️ O inverso — a sua preocupação central — MEDIDO: nada adormeceu

**Classificação: Testado.** Rodei a bateria inteira da rodada 1 (M1 a M11b) contra a suíte nova e comparei com a tabela que publiquei. Resultado item a item:

| Mutante | Rodada 1 | Rodada 2 | Variação |
| --- | --- | --- | --- |
| M1 `saldo_final`→`saldo_anterior` | PEGOU (1 teste) | **PEGOU (5 testes)** | fortaleceu |
| M2 sinal invertido | PEGOU (5) | **PEGOU (8)** | fortaleceu |
| M3 arredonda p/ inteiro | PEGOU (1) | PEGOU (1) | igual |
| M4 soma todas as linhas | PEGOU (6) | **PEGOU (8)** | fortaleceu |
| M5 soma só as folhas | PEGOU (5) | **PEGOU (7)** | fortaleceu |
| M6 tipos enumerados à mão | SOBREVIVEU | SOBREVIVEU | igual (ver abaixo) |
| M7 `diferenca = zero` | PEGOU (1) | PEGOU (1) | igual |
| M8 equação sem o resultado | PEGOU (2) | PEGOU (2) | igual |
| M9 receita/despesa trocadas | PEGOU (2) | PEGOU (2) | igual |
| M10 `float` no lugar de `Decimal` | PEGOU (1) | PEGOU (1) | igual |
| M11b escrita escondida | PEGOU (2) | PEGOU (2) | igual |

**Zero mutantes que antes morriam e agora sobrevivem.** Cinco fortaleceram, seis ficaram iguais. A alavanca de dois lados da fixture compartilhada **não** bateu no outro lado — e não era garantido, porque `cenario_completo` alimenta 14 testes.

**Sobre o M6, que sobrevive nas duas rodadas e não é defeito**, refiz a medição do cenário para o qual a guarda existe, agora com a suíte nova:

```
TipoConta NOVO + código como entregue (derivado)  | PEGOU | 2 failed
      -> test_tipos_tratados_na_equacao_batem_com_tipoconta      <-- NOVO, BL-479
      -> test_tipo_gravado_fora_de_tipoconta_e_nomeado_nunca_derruba
TipoConta NOVO + M6 (enumerado à mão)             | PEGOU | 6 failed
```

**Este é o número que fecha o A5 da rodada 1.** Na rodada 1, acrescentar um sexto `TipoConta` dava `17 passed` — a suíte não reprovava e os R$ 400,00 do tipo novo viravam `diferenca` anônima. Agora a guarda derivada do BL-479 **reprova nomeando o tipo esquecido**. Medido, não lido.

---

## V3 — Contrato, de novo: `tipo_da_raiz` é aditivo e não vaza

**Classificação: Inspecionado + Testado.**

- `git diff --name-only 2e4ad02^..2e4ad02` (via `git show --stat`): **dois** arquivos, `apps/contabilidade/services.py` e o arquivo de teste da DL-032. **Nenhuma view, nenhum template, nenhum modelo, nenhuma migração.**
- A linha do Balancete passou de 12 para 14 chaves. Medi o conjunto completo (sonda R7):

```
['analitica','conta','creditos','creditos_proprios','debitos','debitos_proprios',
 'natureza','nivel','nome','raiz','saldo_anterior','saldo_final','tipo','tipo_da_raiz']
```

As 12 originais estão intactas em nome, tipo e significado; `raiz` e `tipo_da_raiz` são as novas. Os dois consumidores (`views.py:1159`, `views_web.py:2357`) continuam montando a saída em **lista branca campo a campo** — não foram tocados —, então **nada vaza** para a resposta da API nem para a tela do Balancete. Suíte verde (2035), incluindo `test_bl281_natureza_cadastrada_no_balancete.py` e `test_bl290_veredito_balancete.py`.
- `tipo_da_raiz` **não** é repassado para `apurar_saldos["contas"]` (medido: as chaves da linha são `conta, natureza, nivel, nome, raiz, saldo, tipo`). Coerente — ele serve à detecção interna, não ao consumidor.
- `tipo_da_raiz` **nunca é `None`** e é sempre um valor de `TipoConta` em dado íntegro; para toda linha raiz, `tipo_da_raiz == tipo` (sonda R7, verificado linha a linha).
- Sob filtro de `nivel` (sonda R8, `nivel=2`), `tipo_da_raiz` continua correto: `('1','ativo','ativo')`, `('1.5','despesa','ativo')`, `('3','patrimonio_liquido','patrimonio_liquido')` — a raiz é computada na hierarquia **completa**, não na exibida. Era um jeito plausível de errar, e não erraram.

---

## Os demais achados da rodada 1, confirmados

| Achado r1 | Correção | Verificação minha | Situação |
| --- | --- | --- | --- |
| **A2 alta** — tipo divergente da raiz somado em silêncio | BL-475 | sonda R1 + controle positivo + N1/N2/N3 + M4/M5 intactos | **FECHADO** — Testado |
| **A1 média** — `KeyError` em tipo fora de `TipoConta` | BL-476 | N5 (volta a estourar) e N6 (cria chave nova) **morrem**; sondas R3/R4: nomeado, e `set(totais_por_tipo) == set(TipoConta.values)` sempre | **FECHADO** — Testado |
| **A3 média** — fronteira da `data_base` sem guarda | BL-477 | M1 reprova o teste do critério 1 **sozinho**; teste nomeado da fronteira, com fixture dedicada | **FECHADO** — Testado |
| **A4 média** — `resultado_do_periodo` mentia | BL-478 | chave agora `resultado_nao_transferido` (medido em `sorted(s["equacao"])`); docstring declara que a camada **não** serve para DRE e manda usar `apurar_balancete(inicio, fim)`; `test_apos_zeramento_resultado_nao_transferido_e_zero_mas_resultado_do_mes_nao` fixa a diferença **por teste**, exatamente como eu pedi | **FECHADO** — Testado |
| **A5 média** — equação enumerada, tipo novo vira diferença anônima | BL-479 | sexto `TipoConta` agora **reprova** nomeando o tipo (era `17 passed`) | **FECHADO** — Testado |
| **A7 baixa** — `HierarquiaInconsistente` fora do contrato | BL-480 | declarado no docstring; dois testes (ciclo e pai de outra empresa) | **FECHADO** — Inspecionado + Testado |
| **A8 baixa** — `contas` sem `raiz` | BL-481 | `raiz` repassado (N7 mata quem o remove); teste reproduz `totais_por_tipo` a partir de `contas` filtrando por `raiz` | **FECHADO** — Testado |
| **A9 baixa** — conta inativa não declarada | BL-482 | declarado; teste fixa que a conta desativada continua com saldo e continua somada | **FECHADO** — Testado |
| **A6 / DE-067** | fora do escopo, por decisão sua | **CONFIRMO que ele NÃO implementou:** varri de `apurar_balancete` (linha 1560) ao fim do arquivo — as duas únicas ocorrências de `atomic`/`REPEATABLE`/`SET TRANSACTION` estão **dentro do comentário** (linhas 2043 e 2049), nenhuma em código. Os `@transaction.atomic` do arquivo são todos de serviços de **escrita**, acima da linha 1200. A DE-067 foi documentada e obedecida | **Confirmado** |
| **A10** | você corrigiu | `estado.md:587` agora diz *"A DL-032 ESTÁ INTEGRADA (`b73c729`) E REPROVADA NA RODADA 1"* | **Fechado por você** |

---

# ACHADOS DESTA RODADA — três, todos BAIXOS (ressalvas DE-066)

## R1 — BAIXA — A semântica de `tipo_da_raiz` não está fixada: "raiz" e "pai direto" são indistinguíveis pela suíte

**Requisito afetado:** integridade do contrato de `apurar_balancete` (o campo é novo e público na resposta). Não afeta número nenhum.

**Arquivo e localização:** `apps/contabilidade/services.py:1832` (`"tipo_da_raiz": contas_por_id[raiz_id_de(conta.id)].tipo,`) e o teste `test_conta_descendente_com_tipo_diferente_da_raiz_e_declarada_nunca_corrigida`.

**Evidência — mutante meu:**

```
SOBREVIVEU | N4 tipo_da_raiz = tipo do PAI direto | 27 passed in 1.50s
```

Trocar a raiz pelo **pai direto** passa nos 27 testes. A causa é a profundidade da fixture: no teste do caso torto, `1.9` é filha **direta** da raiz, então o `tipo` do pai e o `tipo` da raiz coincidem e os dois algoritmos dão o mesmo resultado.

**O caso que os distingue — sonda R2, executada, comportamento entregue CORRETO:** subárvore `1` ATIVO → `1.5` DESPESA → `1.5.1` DESPESA → `1.5.1.01` DESPESA:

```
R2 nomeadas: ['1.5', '1.5.1', '1.5.1.01']
R2 tipo_da_raiz: [('1.5','despesa','ativo'), ('1.5.1','despesa','ativo'),
                  ('1.5.1.01','despesa','ativo')]
```

O código entregue nomeia **todas as três** e devolve `ativo` em todas — semântica de raiz, que é o que o nome do campo promete. O mutante N4 nomearia **só `1.5`**, e nada ficaria vermelho.

**Impacto.** Baixo, e digo por quê: o comportamento está **certo**; o que falta é a guarda. E mesmo sob o mutante o contador ainda veria o topo da subárvore mal pendurada (`1.5`), que é o que ele precisa para agir. O dano real é de manutenção: o significado de um campo público pode mudar sem a suíte acusar.

**Correção recomendada:** acrescentar o caso de profundidade ≥ 3 ao teste existente, exigindo que **todas** as contas da subárvore divergente sejam nomeadas.

**Caso de teste, pronto para implementar:**

```
Dado: 1 ATIVO(raiz) / 1.1 Caixa(pai=1) / 1.5 DESPESA(pai=1)
      / 1.5.1 DESPESA(pai=1.5) / 1.5.1.01 DESPESA(pai=1.5.1) / 3 PL / 3.1 Capital
E lançamentos: D Caixa / C Capital 1000,00 ; D 1.5.1.01 / C Caixa 300,00
Então sorted(c["conta"] for c in contas_com_tipo_divergente_da_raiz)
      == ["1.5", "1.5.1", "1.5.1.01"]
E todas as três têm tipo_da_raiz == TipoConta.ATIVO
E totais_por_tipo[ATIVO] == 1000,00  e  diferenca == 0,00   (o dinheiro não se move)
```

**Forma de verificar:** aplicar o mutante N4 (`tipo_da_raiz` = tipo do pai direto) e exigir que reprove.

**Responsável:** `desenvolvedor-pleno`.

---

## R2 — BAIXA — O docstring afirma que o tipo desconhecido é "OMITIDO da soma"; num DESCENDENTE ele NÃO é

**Requisito afetado:** honestidade do contrato. É a família que este repositório já nomeou catorze vezes — *"comentário que afirma mais do que a defesa entrega"*. **Achado novo, nascido da correção (DE-055).**

**Arquivo e localização:** `apps/contabilidade/services.py`, docstring de `apurar_saldos`, o parágrafo do BL-476: *"o tipo desconhecido é **OMITIDO da soma** e aparece, nomeado, em `contas_com_tipo_desconhecido`"*. O código é `services.py:2099-2105`.

**Reprodução — sonda R3, executada.** Conta `1.7`, **descendente** da raiz `1` ATIVO, com `tipo` gravado como `"custo"` via `.update()`, e um lançamento de 300,00 nela:

```
R3 desconhecidos: [{'conta': '1.7', 'nome': 'Descendente corrompida', 'tipo': 'custo'}]
R3 divergentes:   ['1.7']
R3 totais:        {'ativo': '1000.00', 'passivo': '0',
                   'patrimonio_liquido': '1000.00', 'receita': '0', 'despesa': '0'}
R3 diferenca:     0.00
R3 chaves de totais_por_tipo: ['ativo','despesa','passivo','patrimonio_liquido','receita']
```

Os R$ 300,00 **estão dentro** de `ativo` (consolidados na raiz pela DE-020: 1.000 − 300 + 300 = 1.000), e `diferenca` é **0,00**. Nada foi omitido.

Compare com a sonda R4, onde a corrompida é a **raiz**: aí sim o valor sai da soma, e a camada **declara** — `totais_por_tipo['ativo'] = 0`, `diferenca = -1.000,00`, conta `1` nomeada. A frase do docstring descreve **apenas este segundo caso**.

**Impacto.** Baixo. O **comportamento está certo nos dois casos** — omitir o descendente faria dinheiro desaparecer do Balanço, que é pior. E o descendente corrompido é nomeado em **duas** listas (`contas_com_tipo_desconhecido` e `contas_com_tipo_divergente_da_raiz`), então nada fica escondido. O dano é que o contrato afirma uma coisa que só vale metade das vezes, e quem for construir a fatia 2 sobre essa frase vai desenhar o aviso errado.

**Correção recomendada:** trocar a frase por algo como *"o tipo desconhecido não entra em `totais_por_tipo` como chave nem como soma **quando a conta é RAIZ**; quando é descendente, o valor dela continua consolidado no grupo da raiz (DE-020) e a conta aparece nas duas listas"*. Uma frase, nenhuma mudança de código.

**Forma de verificar:** a sonda R3 como teste, afirmando as duas listas **e** que `totais_por_tipo[ATIVO] == 1000,00` com `diferenca == 0,00` — fixando por teste a metade que o docstring hoje contradiz. Mais a sonda R4 para a metade que ele descreve certo.

**Responsável:** `desenvolvedor-pleno`.

---

## R3 — BAIXA — O teste do tipo desconhecido usa o literal `"custo"`, que pode virar um `TipoConta` de verdade

**Arquivo e localização:** `test_tipo_gravado_fora_de_tipoconta_e_nomeado_nunca_derruba` (`.update(tipo="custo")`).

**Evidência:** nas duas medições em que acrescentei `CUSTO = "custo", "Custo"` a `TipoConta`, esse teste reprovou — junto com a guarda do BL-479, que é quem *deveria* reprovar:

```
TipoConta NOVO + código como entregue | PEGOU | 2 failed
      -> test_tipos_tratados_na_equacao_batem_com_tipoconta          <-- o certo
      -> test_tipo_gravado_fora_de_tipoconta_e_nomeado_nunca_derruba <-- ruído
```

**Impacto.** Baixíssimo, mas é ruído numa hora ruim: quando alguém acrescentar um `TipoConta` (por exemplo dividindo despesa e **custo**, que é justamente o que RC-104/PE-62 tornam plausível), a suíte vai reprovar em **dois** lugares, e só um deles é o defeito. Diagnóstico embaçado é o que faz mecanismo ser ignorado.

**Correção recomendada:** usar um valor que não pode virar `TipoConta`, por exemplo `"tipo-que-nao-existe"`, ou derivá-lo: `"zzz-" + uuid4().hex` conferido contra `TipoConta.values`.

**Responsável:** `desenvolvedor-pleno`.

---

## DE-055 — Portas que a correção NÃO abriu (medido, não presumido)

| Suspeita | Medição | Resultado |
| --- | --- | --- |
| `tipo_da_raiz` traz consulta nova / N+1 | sonda R6: 404 contas/profundidade 4 e 210 contas/profundidade 10 | **3 consultas em ambos**; 0,0129 s e 0,0115 s. Zero consultas novas. `raiz_id_de` é memoizado e só anda em `conta_pai_id` já carregado |
| `None` onde ninguém espera | sonda R7, linha a linha | `tipo_da_raiz` nunca `None`; sempre em `TipoConta.values` em dado íntegro; `== tipo` em toda raiz |
| Conta sem raiz / árvore de um nível só | sonda R5 (árvore plana, todas raízes) | as duas listas vazias, todo `raiz` verdadeiro, nada quebra |
| Empresa sem contas | sonda R5 | as **duas chaves novas presentes e vazias**, `resultado_nao_transferido == 0`, nenhum `KeyError` — o retorno antecipado de `apurar_balancete` está antes do novo código, e isso confere |
| Recursão sem guarda de ciclo em `raiz_id_de` | inspeção de `_construir_hierarquia` (services.py:1319-1378) + testes dele de ciclo e pai de outra empresa | seguro: `_construir_hierarquia` valida **toda** conta antes, e levanta `HierarquiaInconsistente` nomeando a conta — `raiz_id_de` nunca anda sobre cadeia quebrada |
| Raiz corrompida com descendentes válidos | sonda R4 | raiz nomeada em desconhecidos; os dois descendentes nomeados em divergentes com `tipo_da_raiz='custo'` (o valor real corrompido, não um `None` inventado); `diferenca = -1.000,00` **declarada** |
| `tipo_da_raiz` errado sob filtro de `nivel` | sonda R8, `nivel=2` | correto: a raiz é computada na hierarquia completa, não na exibida |
| Ordem não determinística das listas novas | inspeção | seguem `balancete["contas"]`, que é `order_by("codigo")` — determinística |

---

## O que eu NÃO consegui medir, e por quê

1. **Concorrência real (A6/DE-067).** Continua inspecionada, não reproduzida — exige duas conexões com escrita comprometida no meio da leitura, e a suíte roda dentro de transação de teste. O que mudou é que agora está **declarado** no contrato, com a decisão nomeada. Confirmei que não foi implementado, como você determinou.
2. **Base com anos de escrituração.** Medi até 3.000 lançamentos, 404 contas e profundidade 10. Não medi centenas de milhares de itens. Afirmo: 3 consultas fixas, crescimento ~linear nessa faixa, `tipo_da_raiz` de custo nulo. Não afirmo nada sobre o planejador do PostgreSQL em escala de produção.
3. **Interface.** Nenhuma tela nasceu; a do Balancete verifiquei por inspeção dos dois consumidores intactos e pela suíte verde, não em navegador.
4. **Autorização no servidor.** Continua **não aplicável e pendente**: `apurar_saldos` não é alcançável por requisição. Migra inteira para a fatia 2, e agora o contrato pelo menos avisa que `HierarquiaInconsistente` precisa de `try/except` lá.
5. **Validação profissional das regras contábeis.** Conferi aritmética, sinal, precisão, consistência interna e conciliação. **Não** validei — e não tenho competência para validar — conformidade normativa da forma da equação nem da classificação que RC-104 descreve. Auditoria de software não substitui validação profissional das regras contábeis e legais.

---

## ⚠️ Onde o erro foi SEU nesta rodada

**Um, e é pequeno — mas é o único, e cortou na direção certa.** Você escreveu: *"o risco escondido desta rodada é a mudança de FIXTURE — mexer nela para acordar uma guarda pode adormecer outra, sem nada ficar vermelho"*, e mandou refazer a bateria inteira. **Medi, e não aconteceu: zero mutantes enfraquecidos, cinco fortalecidos.** A suspeita era legítima e o instrumento que você pediu era o certo — mas o resultado foi o oposto do temido, e vale dizer com todas as letras para que a próxima rodada não pague o mesmo custo por reflexo: mexer em `data_base` numa fixture cujos totais não mudam é uma mudança **monotônica** na força das guardas, porque só acrescenta movimento a um dia que antes estava vazio. Não era 50/50.

**E uma nota de crédito que não é erro seu nem dele.** A instrução *"confirme com o seu mutante, não com o dele"* foi o que produziu o achado R1: o mutante que **ele** não escreveu (`tipo_da_raiz` = pai direto) é o único que sobreviveu, e é o único ponto do contrato novo que a suíte não fixa. A regra valeu o custo exatamente uma vez nesta rodada, e valeu.

---

**Situação das ressalvas.** As três (R1, R2, R3) são de gravidade **baixa**, nenhuma toca número, saldo, equação, isolamento ou precisão. Duas se resolvem com um teste cada e uma com uma frase de docstring. Pela DE-066 entram como **ressalva declarada**, não como nova rodada — e a §3.1 proíbe a terceira de todo modo. Recomendo que entrem no backlog como itens próprios, visíveis, e não como pendência anexada a esta etapa.

**Não declaro** ausência de bugs, segurança absoluta nem conformidade normativa garantida.
