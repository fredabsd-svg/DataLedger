Bancada desmontada: worktree isolada removida (`git worktree list` só com o repositório), bancos `auditoria_r12` e `test_auditoria_r12` derrubados e conferidos como inexistentes, credenciais temporárias apagadas, árvore principal com `git status --porcelain` e `git diff --stat` vazios, `HEAD` em `d63bfe0`. Segue o relatório.

---

# Auditoria DL-029 (rodadas 2 e 3), DL-030 e fechamento de DL-026/DL-028 — rodada 12

- **Revisão auditada:** `d63bfe0a61b534645ca340fc21b40442ca8124c2`
- **Branch:** `claude/accounting-agent-team-setup-mn6lyf`
- **`main` no momento da auditoria:** `7c5b1d4` (inalterada desde a rodada 11)
- **Data:** 2026-09-20
- **Auditor:** `auditor-qa` (independente; sem `Write`/`Edit`)
- **Régua:** a **frase da DE-061**, e o instrumento **enquanto juiz dela**. Não os L1–L9.
- **Parecer:** **REPROVADO**

## Como foi medido

Toda sabotagem rodou em **cópia isolada** (`git worktree add --detach d63bfe0`, sob o scratchpad), nunca na árvore (BL-311). Banco próprio (`auditoria_r12`), derivado da variável de ambiente **sem imprimir a URL**, criado e derrubado. Nenhum `ruff format` que reescreve. Nenhum `ps aux`/`env`/`printenv`/`pgrep -af`. Dois auxiliares em paralelo (`auxiliar-pesquisa`, `auxiliar-verificacao`) — nenhum `auxiliar-implementacao`.

**Os quatro números que você declarou, conferidos por mim:**

| Sua declaração | Meu resultado | Situação |
| --- | --- | --- |
| `pytest`: 2058 passed, 15 skipped, 4 subtests | `2058 passed, 15 skipped, 2 warnings, 4 subtests passed` em **95,16 s** | **Confere** |
| `ruff check .` | `All checks passed!` | **Confere** |
| `ruff format --check .` | `211 files already formatted` | **Confere** |
| `python manage.py check` | `System check identified no issues (0 silenced)` | **Confere** |

**Controle limpo do instrumento, refeito por mim:** `exit 0`, 3,6 s, 3 telas derivadas; Balancete 773/352/324 px de tinta contra piso de 40 (19×–8,1× de margem), contraste 21,00/20,62/20,38:1, 16 px negrito + 14 px normal, folha 1, `pdf_contem_marca_do_fornecedor: false`, metadados limpos (`Title` sem a marca, `Creator`/`Producer` só diagnóstico). **O produto continua certo em tudo que o instrumento mede — sexta rodada seguida.**

**CI de `d63bfe0`:** 7 execuções, todas `success` (o job `Lint e testes` ainda estava `in_progress` quando comecei; reconferi e fechou verde). Custo do critério 9, lido dos passos do job real: **4 s** no passo *"Medir identificação do emitente"* — sua declaração de 3,5–3,6 s é verdadeira. ⚠️ **E um número que você não declarou:** o passo *"Testes automatizados do instrumento"* foi de **24 s** (`92679e0`, 14 casos) para **33 s** (`d63bfe0`, 18 casos) — ver **M11**.

---

# Achados

Numerados **M** para não colidir com os **K** e **L**.

---

## M1 — ALTA — a propriedade CSS `scale:` reabre **BL-428 e BL-436 inteiros**, com o papel byte a byte idêntico ao da sabotagem que reprova

**Classe:** Testado (reprodução completa, com o par de controle e a medida do glifo no papel).

**Requisito afetado:** a frase, cláusula **C2**; **BL-428** e **BL-436**, declarados fechados nas rodadas 2 e 3; e a **DE-060**, cuja declaração no código afirma que a medição é a propriedade.

**Arquivo e localização:** `scripts/medir_identificacao_do_emitente.py:582-616` (`js_fonte_das_linhas`, a função `escalaAcumulada`), com o comentário que a justifica em `:571-581`.

O código anda pelos ancestrais lendo **duas** coisas:

```
"      escala *= parseFloat(cs.zoom) || 1;"
"      const t = cs.transform;"
```

**`cs.scale` não é lido em lugar nenhum.** Varri o instrumento e a suíte dele: a palavra `scale` fora de `scale(...)` não aparece — **zero** ocorrências em `scripts/medir_identificacao_do_emitente.py` e em `scripts/test_medir_identificacao_do_emitente.py`. Não é limite declarado; é ausência.

**O mecanismo, medido no Chromium 153 do ambiente:**

```
scale: 0.6          →  transform: 'none'                     scale: '0.6'   rect.height 9.5999994
transform: scale(.6)→  transform: 'matrix(0.6,0,0,0.6,0,0)'  scale: 'none'  rect.height 9.6000022
```

As duas construções produzem **a mesma altura renderizada**. O caminhador lê só a segunda.

**Evidência — o par, no produto real, com o mesmo CSS a menos de uma palavra:**

| Construção | `tamanho_renderizado_px` relatado | Pixels de tinta (Balancete) | Altura do glifo `CRC-TO` no papel (`pdftotext -bbox`) | Veredito |
| --- | --- | --- | --- | --- |
| Controle limpo | 16 / 14 / 14 | 773 / 352 / 324 | **11,4249 pt** (15,23 px) | `exit 0` |
| `transform: scale(0.6)` | **9,6 / 8,4 / 8,4** | 265 / 95 / 76 | 6,8549 pt (9,14 px) | **`exit 1`** — *"renderizada a 9.6px, abaixo do mínimo de 11px"* |
| **`scale: 0.6`** | **16 / 14 / 14** | **265 / 95 / 76** | **6,8549 pt (9,14 px)** | **`exit 0` — PASSOU nas três telas** |

Os pixels de tinta são **idênticos** (265/95/76) e a caixa do glifo é **idêntica ao milésimo de ponto** (6,8549 pt; razão medida 6,8549/11,4249 = **0,5999**). **É a mesma folha.** O instrumento reprova uma e aprova a outra porque leu propriedades CSS diferentes.

**E não é só o piso de tamanho: o BL-436 volta inteiro.** Com `font-size: 24px` + `scale: 0.6` + tinta `#D0D0D0`:

```
CODIGO=0   "veredito": "PASSOU"   "motivos": []
tamanho_declarado_px: 24   tamanho_renderizado_px: 24   (real no papel: 14,4)
razao_minima_wcag_exigida: 3.0   (devida: 4.5)   contraste_maximo_medido: 4.174
```

É o **mesmo JSON, linha por linha**, que a verificação independente colou ao abrir o BL-436 — só que agora o campo chamado `tamanho_renderizado_px` mente por **67%**, porque a correção da rodada 3 o alimenta com `declarado × escala`, e a escala está errada.

**E alcança o ancestral, que era o caso "comum" do BL-428:** `@media print { body { scale: 0.6 } }` — *"encolher a impressão para a tabela caber"* — sai **`exit 0`** com `tamanho_renderizado_px: 16/14/14`.

**Por que importa.** `scale`, `rotate` e `translate` como propriedades individuais são a forma **recomendada** de escrever transformação CSS hoje, exatamente como o `:is()`/aninhamento que matou a correção do BL-343 na rodada 7 (o G1 que originou a DE-055). A DL-027 é a etapa que entrega personalização de timbre por escritório.

**Correção recomendada.** ⚠️ **Não acrescente `cs.scale` à leitura** — isso seria a décima sétima ocorrência da classe (a lista cresce: amanhã é `rotate`, `translate`, `offset-path`, `perspective`). A propriedade é *"que escala o navegador está aplicando a este elemento"*, e há como perguntá-la sem enumerar: inserir no `<p>` um elemento-sonda de altura CSS conhecida (`display:inline-block; height:100px; width:0`) e ler `getBoundingClientRect().height / 100` — geometria composta, subpixel, sem o arredondamento inteiro que derrubou a tentativa 1 (`offsetHeight`) na rodada 3. Qualquer transformação futura entra sozinha.

**Como verificar a correção.** `scale: 0.6` no timbre tem de sair com código **1** nomeando **tamanho**, com o número medido; `scale: 0.6` no `body` idem; e `font-size: 24px + scale: 0.6 + #D0D0D0` tem de exigir **4,5:1**. E um teste que force a divergência entre `cs.transform` e a escala realmente aplicada — hoje ele não existe em nenhuma forma.

---

## M2 — ALTA — C5 não alcança as **anotações de link** do PDF, e o produto de hoje embute **268** delas no Balancete

**Classe:** Testado (inspeção dos bytes do PDF de controle, mais sabotagem com a marca do fornecedor).

**Requisito afetado:** a frase, cláusula **C5**, e a fronteira nova da **DE-061** — *"no conteúdo que o documento controla, **isto é, tinta na folha e metadados do arquivo**"*.

**Arquivo e localização:** `scripts/medir_identificacao_do_emitente.py:2135-2151` — o C5 examina `_texto_do_pdf` (`pdftotext -layout`) e `_metadados_do_pdf` (`pdfinfo`, dicionário `Info`). Anotação de link não está em nenhum dos dois.

**Evidência 1 — o produto de hoje, sem sabotagem nenhuma.** Nos PDFs que o próprio instrumento exporta:

| Tela | Anotações `/URI` no PDF |
| --- | --- |
| Balancete | **268** |
| Diário | **120** |
| Razão | **10** |

Cada linha das tabelas vira um link clicável de volta para dentro da aplicação (`/URI (file:///contabilidade/painel/empresas/1/razao/17/?inicio=...)` na bancada). **Em produção, servido de um domínio, o Chromium grava a URI absoluta** — e essa é exatamente a string que a DE-061 chamou de *"o identificador do fornecedor"* ao justificar a exclusão da faixa do navegador. A diferença é decisiva: a faixa foi excluída porque **nenhuma folha de estilo a alcança** (BL-332); a anotação de link **o documento controla inteiramente** (o template decide se emite `<a href>`), e a frase a deixa de fora mesmo assim — porque a fronteira virou uma **enumeração de dois canais**.

**Evidência 2 — a sabotagem, para fechar a dúvida.** Acrescentei ao `balancete.html`, na cópia isolada, um link de texto inócuo com o domínio do fornecedor:

```html
<p><a href="https://dataledger.com.br/">Emitido pelo sistema</a></p>
```

```
EXIT=0
strings <pdf> | grep -i dataledger  →  /URI (https://dataledger.com.br/)>>>>
pdftotext -layout <pdf> | grep -i dataledger  →  (nada: o texto visível é "Emitido pelo sistema")
```

**O PDF que o escritório entrega ao cliente carrega o domínio do fornecedor, clicável, e o instrumento sai com código 0.** É o **BL-404 por outro canal** — o mesmo bloqueador da décima auditoria, por uma porta que a frase corrigida fechou por descuido de redação.

**⚠️ E a redação defeituosa é minha.** A DE-061 é a minha frase, e eu troquei uma promessa aberta (*"nenhum identificador do fornecedor"*) por uma **lista de dois itens** (*"isto é, tinta na folha e metadados do arquivo"*). Isso é a **DE-056 dentro da correção que eu propus para a DE-059**: a guarda passou a enumerar.

**Correção recomendada — da frase, e só depois do código.** Trocar a enumeração por um **critério**:

> *"…no conteúdo que o documento controla — **tudo que o arquivo exportado carrega e que o template ou a folha de estilo podem suprimir** —, excluída a faixa que o navegador acrescenta por fora e que nenhuma folha de estilo alcança (BL-332)…"*

Isso alcança anotação de link, XMP, anexo embutido, campo de formulário e o que vier, sem lista. No instrumento, a medição correspondente é barata: `pdftotext` não serve, mas os `/URI` saem da varredura dos objetos de anotação (ou, no limite, de `strings` sobre o arquivo, que é o que eu usei).

**Como verificar.** O link de sabotagem acima tem de sair com código **1**, nomeando *"identificador do fornecedor presente em anotação de link"*. E vale decidir à parte se as 268 anotações internas devem existir num documento entregue ao cliente — elas expõem a estrutura de URL e os identificadores internos (`empresas/1/lancamento/17/`) do sistema do escritório num arquivo que sai dele.

---

## M3 — ALTA — a frase cobre **metade** do que um contador chama de "o documento sai identificado": empresa, período e paginação ficam de fora, e as folhas 2..N não carregam nada

**Classe:** Testado.

**Requisito afetado:** a **frase inteira** (esta é a resposta à sua pergunta 1), e o critério 3 da DL-026.

**Evidência 1 — o que o papel carrega hoje, folha por folha, no controle limpo:**

| Tela | Folhas | Folha 1 | Folhas 2..N |
| --- | --- | --- | --- |
| Balancete | **4** | timbre do escritório + `EMPRESA / Comércio Sintético de Materiais Ltda` + `PERÍODO / 01/03/2026 a 31/03/2026` | **nada**: só cabeçalho de coluna e linhas. Sem empresa, sem período, sem escritório, **sem número de folha**. A folha 4 sai **em branco** (0 caracteres não-brancos) |
| Diário | **3** | idem | **nada** |
| Razão | 1 | idem | — |

**Evidência 2 — a identificação da empresa pode sumir com job verde.** Uma linha de CSS de impressão:

```css
@media print { .contexto-item, .contexto-rotulo, .cabecalho__contexto { display: none !important; } }
```

```
EXIT=0   — as três telas PASSOU
```

E o papel que sai:

```
Escritório Contábil Sintético ME
Rua Sintética 100, Sala 2 - Palmas/TO
CRC-TO 000000/O-0 (sintético)

Balancete de verificação
  [tabela]
```

Um *"Balancete de verificação"* sem **de quem** e **de quando**, emitido corretamente por um escritório identificado, e a verificação obrigatória diz que está tudo certo.

**Evidência 3 — nada no repositório guarda isso.** Nenhum teste liga `empresa.razao_social` nem o período ao papel: os testes de impressão (`test_bl282_timbre_de_impressao.py`, `test_bl329_marca_fora_do_papel.py`) tratam só do timbre do escritório e da ausência da marca do fornecedor.

**Por que importa, e onde eu paro.** O `docs/projeto/personalizacao-de-relatorio.md` §1 — documento **do projeto**, citando RC-95/RC-96 — registra que o Balancete é classe 1 (*"nenhuma norma contábil fixa o cabeçalho"*), mas que a classe 2 tem bloco obrigatório **em cada página** e que na classe 3 (Diário e Razão em forma de livro) *"forma, numeração, termos e assinaturas são prescritos"* (NBC ITG 2000, itens 5, 9, 10, 13). **Duas das três telas medidas são exatamente Diário e Razão.** ⚠️ **Eu não afirmo o conteúdo da norma nem que o produto a viola** — não é minha competência e o `AGENTS.md` proíbe inventar exigência. Afirmo o que medi: **a frase, e portanto a garantia, não faz pergunta nenhuma sobre entidade, período ou numeração de folha**, e o próprio mapa do projeto diz que ao menos numeração é matéria prescrita para duas das três telas.

**Correção recomendada.** Não alargar a frase por impulso — a decisão de **o que** precisa sair identificado por classe de documento é do Fred (é a DL-027 inteira). O que eu recomendo agora, e é barato: **declarar o limite** no código e no `estado.md`, na forma da DE-056, com esta redação medida — *"o instrumento mede a identificação do ESCRITÓRIO EMITENTE na folha 1. Não mede a identificação da EMPRESA, do PERÍODO, nem a numeração de folhas, e não mede nada nas folhas 2..N"* — para ninguém ler o job verde como *"o documento sai identificado"*.

**Como verificar.** A sabotagem acima, hoje `exit 0`, é o caso de aceite quando a decisão do Fred chegar.

---

## M4 — ALTA — BL-440: você está certo sobre o **artefato** e a etiqueta **generaliza** além dele; `#999999` e `opacity: 0.4` são a mesma tinta percebida e recebem vereditos opostos

**Classe:** Testado. **Você pediu discordância; aqui está, com o dado.**

**Requisito afetado:** a frase, cláusula **C2**, e a **DE-058**.

**O que eu concedo, sem ressalva:** o argumento central é **correto**. O instrumento mede o artefato; se a tinta no PDF do Chromium é (54,54,55), a folha **tem** aquele contraste, e a medição está certa **sobre aquele arquivo**. E você tem razão em recusar-se a afirmar o mecanismo sem o código do Chromium.

**O que eu meço e que a sua classificação não cobre.** O piso de contraste é usado como **par calibrado**: o critério 7 do plano exige que `opacity: 0.4` **reprove**. `opacity: 0.4` de tinta preta sobre papel branco compõe exatamente `rgb(153,153,153)` = `#999999`. Sabotei as duas:

| Construção | Contraste relatado pelo instrumento | Veredito |
| --- | --- | --- |
| `opacity: 0.4` | **2,85 / 2,81 / 2,81 :1** | **REPROVADO** |
| **`color: #999999`** | **9,59 / 9,29 / 9,29 :1** | **PASSOU, `exit 0`** |

A razão WCAG 2.2 verdadeira de `#999999` sobre branco, pela fórmula de três canais, é **2,849:1** — que é **exatamente o que o caminho da `opacity` mede** e **3,3× menos** do que o caminho da `color` relata. Ou seja: **um dos dois caminhos entrega o número do padrão que a mensagem cita, e o outro não** — e o que não entrega é o que **passa**.

**Onde isso deixa a sua classificação.** *"A direção do desvio é a segura"* é verdadeira para a pergunta *"este PDF do Chromium é legível?"*. É **falsa** para a pergunta *"a cor que este escritório escolheu é legível?"* — que é literalmente a pergunta que a DL-027 vai fazer, e que você mesmo nomeou como o motivo de o achado importar. A frase da DE-061 diz *"a folha A4 exportada"* **sem nomear o motor**; medido, o mesmo CSS dá 2,85:1 ou 9,29:1 conforme o caminho de renderização. Isso não é fato de produto: é **premissa não declarada do instrumento**, na forma que a DE-058 proíbe — afirmação em tom de medição, com etiqueta `(WCAG 2.2, 1.4.3)`, sobre um número que descreve a tinta **depois** de uma transformação que ninguém entende.

⚠️ **E isso reabre, com dado, a sua terceira autocorreção do dia.** Você escreveu *"eu havia dito que a direção do erro é o lado do falso conforme. **Não é**"*. Para o artefato, você tem razão. Para a cor declarada — `#999999`, que é o cinza discreto mais banal que existe em folha de estilo — o instrumento relata **3,3× mais contraste do que a tinta escolhida tem**, e isso **é** o lado do falso conforme. **A correção que você publicou é mais estreita do que o fato.**

**Correção recomendada.** Uma das duas, escrita, no formato da DE-058: **(a)** a mensagem e a docstring passam a dizer *"contraste da tinta como o Chromium a exporta para PDF, que pode diferir da cor declarada no CSS (BL-440)"*, com o teste que reprova se a divergência deixar de existir; **ou (b)** o instrumento mede também a cor declarada (`getComputedStyle` já a tem) e **reprova a divergência** entre as duas como achado próprio. Não serve a forma atual, em que o número é apresentado como a razão do WCAG da tinta do documento.

**Como verificar.** `color: #999999` e `opacity: 0.4` no timbre têm de produzir **o mesmo veredito**, ou mensagens que expliquem por que não produzem.

---

## M5 — MÉDIA — C4 mede a ordem do **DOM**, substituto da ordem de leitura no **papel**: o timbre sai de cabeça para baixo com `exit 0`

**Classe:** Testado.

**Requisito afetado:** a frase, cláusula **C4** — *"cada uma no seu próprio lugar"*, cujo contrato no plano é *"âncora por ocorrência **na ordem de leitura**"*.

**Arquivo e localização:** `scripts/medir_identificacao_do_emitente.py:2174` (`filhos = medida.get("filhos")`) e `:2275` (`filhos[indice]`) — casamento por **posição no `querySelectorAll`**, que é a ordem do DOM.

**Evidência.**

```css
@media print { .timbre-impressao { display: flex; flex-direction: column-reverse; } }
```

```
EXIT=0
```

e o papel:

```
CRC-TO 000000/O-0 (sintético)
Rua Sintética 100, Sala 2 - Palmas/TO
Escritório Contábil Sintético ME
```

O bloco de identificação do emitente sai **invertido** — registro profissional primeiro, razão social por último — e nenhuma cláusula percebe, porque a ordem do DOM não mudou e o bloco continua contíguo.

**Por que importa.** `flex-direction`, `order:`, `grid-row`, `float` e posicionamento absoluto desacoplam ordem de DOM de ordem de leitura, e a DL-027 é a etapa que deixa cada escritório **reordenar** o timbre. É a DE-060 num sexto eixo: a propriedade é *"a linha `i` está no lugar `i` da folha"*; o substituto é *"a linha `i` é o `i`-ésimo `<p>`"*.

**Correção recomendada.** O instrumento **já tem** o dado: `_localizar_linhas_do_timbre_no_documento` devolve o `bbox` de cada linha, em pontos de PDF. Exigir que os `yMin` das linhas sejam **monotônicos na ordem declarada** fecha a família inteira com uma comparação, sem lista.

**Como verificar.** `column-reverse` no timbre tem de sair com código 1 nomeando **ordem**; o controle limpo continua em 0.

---

## M6 — MÉDIA — a instrução corrigida do BL-373 devolve **dois dos quatro** nomes quando executada como está escrita, e o defeito é da **minha** recomendação

**Classe:** Testado (API do GitHub, sem autenticação).

**Requisito afetado:** BL-373, de que tudo nesta auditoria depende para deixar de ser conselho.

**Arquivo e localização:** `AGENTS.md`, seção *"Como estas regras são impostas"*, bloco *"Como conferir, buscando em vez de afirmar"*; espelhado em `CLAUDE.md`.

O texto manda:

```
GET /repos/fredabsd-svg/DataLedger/commits/<sha_da_main>/check-runs
    → os quatro nomes, para copiar daqui e não de memória
```

**Executado hoje, contra o `sha` da `main` (`7c5b1d4`):**

```
total_count = 2
  'Validar documentação'   success
  'Lint e testes'          success
```

**Dois.** `Regras do projeto` só dispara em `pull_request`; `Identificação do emitente` não tem execução nenhuma nesse commit. Quem seguir a instrução ao pé da letra copia **metade** da lista — e a metade que falta inclui, justamente, o job que o Fred escolheu tornar obrigatório.

**⚠️ E a instrução é minha.** Foi a correção que eu recomendei no L5 e que você adotou. É o L5 um nível abaixo: a fonte de verdade que eu indiquei **não contém a verdade inteira** no alvo que eu mandei consultar.

**A lista correta continua correta** — conferi os quatro nomes contra o `name:` de cada job nos YAML, e eles batem caractere a caractere com o que a API devolve para `d63bfe0`:

```
backend.yml                    → 'Lint e testes'
documentation.yml              → 'Validar documentação'
identificacao-do-emitente.yml  → 'Medir identificação do emitente no navegador'
regras-do-projeto.yml          → 'Regras do projeto'
```

**Correção recomendada.** Trocar o alvo da consulta: *"o `sha` do **último commit de uma branch de trabalho** (não o da `main`), que é onde os quatro reportam"* — e, melhor, o teste que eu recomendei na rodada 11 e que **não foi escrito**: uma comparação offline entre os quatro nomes do `AGENTS.md` e os `name:` dos jobs em `.github/workflows/*.yml`. Confirmei que ele não existe (`grep -rln "Lint e testes" apps/ scripts/ --include=*.py` → vazio). É barato, é completo e não depende de qual evento rodou em qual commit.

---

## M7 — MÉDIA — o BL-434 entregou 18 casos ponta a ponta e a cláusula **C1** ficou sem nenhum

**Classe:** Inspecionado + Testado.

**Requisito afetado:** critério **18** da rodada 2 — *"Para **cada** cláusula da frase, **um** caso de aceite ponta a ponta que afirme o código de saída e a substring da mensagem"* — e os critérios 5 e 6 do plano original.

**Evidência.** Os 18 `test_ponta_a_ponta_*` cobrem: controle limpo, C3 (falta/sobra), recusa de infraestrutura, C2 (tamanho ×4, contraste ×2, BL-436, BL-437 ×3), C1-C3 (`letter-spacing`, fonte extrema), C4 (decoy), C5 (pseudo-elemento). **Nenhum** exercita:

- *"linha do timbre está na folha N, não na folha 1"* — o **BL-410**, critério 6 do plano;
- *"linha do timbre AUSENTE do papel"*;
- a rota com `lancamento_id` — o **BL-406**, critério 5;
- o ramo *"POUCOS PIXELS"* vencendo sozinho (só há duas asserções de que ele **não** aparece).

Ou seja: **C2, C3, C4 e C5 têm caso ponta a ponta; C1 não tem nenhum.**

**Confirmei que o comportamento está certo**, por medição minha: `margin-top: 260mm` sai com `exit 1` e a mensagem *"linha do timbre está na folha 2, não na folha 1"*. O achado é que **nada guarda isso** — que é literalmente o argumento com que o BL-434 foi comprado (*"o L1 é um `assert` desses que nunca foi escrito"*), aplicado à única cláusula que ficou de fora.

**Correção recomendada.** Quatro casos, no molde dos 18 que já existem.

---

## M8 — MÉDIA — DL-030: o **critério de aceite 5** não tem teste

**Classe:** Testado (execução própria e do `auxiliar-verificacao`, com banco isolado).

**Requisito afetado:** DL-030, critério 5 — *"A prova do R5: gravação que falha por validação **não** deixa trilha"*.

**Evidência.** `apps/core/tests/test_dl030_trilha_cobre_o_admin.py` tem **9** testes. O único de atomicidade é `test_change_de_empresa_via_admin_reverte_se_registrar_falha` — a direção **oposta** (a trilha falha, a gravação reverte). A direção que o critério 5 nomeia não é exercitada por nenhum teste do repositório pelo caminho do admin (`test_dl024_atomicidade_trilha.py` cobre as views de API/web, não o admin).

**O comportamento está certo** — medido fora do repositório: `POST` com `razao_social=""` devolve 200 com o formulário reapresentado, o objeto não muda e a contagem de `RegistroAuditoria` fica igual. **A metade que falta é a prova**, e o critério a pedia por nome. Isso é a DE-058 aplicada ao critério de aceite: a afirmação existe, a medição não está versionada.

**Correção recomendada.** O teste que o auxiliar escreveu fora do repositório, incorporado. Entrego o caso pronto: `POST` no `change` de `Empresa` com campo obrigatório vazio; asserir `status_code == 200`, objeto inalterado e `RegistroAuditoria.objects.count()` igual ao de antes.

---

## M9 — BAIXA — DL-030: `ItemLancamento` é coberto pela trilha e **não tem escritório derivável**

**Classe:** Testado (instanciação real dos 11 modelos cobertos e chamada direta de `_escritorio_do_objeto`).

**Requisito afetado:** R6 da DL-030, e o isolamento entre escritórios na própria trilha.

**Arquivo e localização:** `apps/auditoria/signals.py:364-381` — a cadeia segue **um** nível de indireção.

**Evidência.** Dos 11 modelos cobertos, 9 derivam corretamente. Dois devolvem `None`:

- `Usuario` — **declarado no código**, com a razão escrita e um teste próprio. Correto.
- **`ItemLancamento` — não declarado.** `ItemLancamento.lancamento → LancamentoContabil → Empresa → Escritorio` são **dois** níveis; `ItemLancamento.conta → Conta → Empresa → Escritorio` idem. A função para no primeiro.

**Por que a gravidade é baixa, e eu medi antes de classificar:** `LancamentoContabilAdmin` recusa `add`, `change` e `delete` (todos `False`), `ItemLancamentoInline` só existe dentro dele, e `ItemLancamento` não tem `ModelAdmin` próprio — **o caminho é inalcançável hoje**. Se o `change` for reaberto, a derivação cai em silêncio para `request.escritorio`, que é o substituto que a etapa inteira existiu para tirar de `Empresa`.

**Correção recomendada.** Ou tornar a cadeia recursiva (com guarda de ciclo), ou **declarar** `ItemLancamento` ao lado de `Usuario`, com a razão e a condição que a torna inofensiva — DE-056/DE-060, no arquivo em que elas foram aplicadas com cuidado em todo o resto.

---

## M10 — BAIXA — BL-419: a razão nova tem uma premissa **verificável e falsa**, e é a terceira instância da classe do BL-442

**Classe:** Testado (leitura do repositório).

O argumento que você adotou no lugar do meu:

> *"toda guarda que eu escrever vai morar **no mesmo YAML que ela guarda**; a regressão é infinita **por construção**…"*

**A premissa é falsa, e a contraprova já está no repositório.** Uma guarda contra `paths-ignore` em `identificacao-do-emitente.yml` não precisa morar nesse YAML: ela cabe como teste `pytest` que **lê o arquivo**, e roda no job `Lint e testes` de **`backend.yml`** — outro workflow. A suíte da raiz já coleta `scripts/test_*.py` (é assim que os 60 testes puros do instrumento também rodam no Backend). Para desligá-la seria preciso editar **dois** arquivos em **dois** workflows — o que não elimina a regressão, mas **muda a sua natureza**: deixa de ser um ponto único e passa a exigir uma edição coordenada, que é justamente o que o seu modelo de risco real (**acidente**, não atacante) não produz.

**O que sobrevive do seu argumento, e sobrevive bem:** a conclusão — *não comprar a guarda agora* — continua defensável por **prioridade**: enquanto a `main` não tem proteção, nenhuma checagem impede nada, e a guarda nº 16 não muda isso. Essa razão é verdadeira, medida e não depende de uma afirmação estrutural falsa.

**Por que eu registro em vez de deixar passar.** Esta é a **terceira** vez, na mesma semana e no **mesmo item**, em que a razão publicada é mais fraca que a verdadeira — e desta vez a razão publicada é a que você adotou **de mim**, depois de eu derrubar a anterior. Eu não acerto mais que você nesse eixo; a diferença é só que agora há dois de nós medindo.

---

## M11 — BAIXA — o custo declarado é verdadeiro sobre o passo e subestima o da etapa em ~30 s por job

**Classe:** Testado (API do GitHub, passos do job real).

O critério 9 pede *"tempo do passo de medição antes e depois, no job real"*. Você declarou 3,5–3,6 s, e **confere**: o passo *"Medir identificação do emitente"* custou **4 s** em `d63bfe0`, **3 s** em `9b14e20` e **4 s** em `92679e0`. Verdadeiro, literalmente.

O que não foi declarado é o passo vizinho, que a mesma etapa fez crescer:

| Revisão | *"Testes automatizados do instrumento (puros e ponta a ponta)"* | Casos ponta a ponta |
| --- | --- | --- |
| `92679e0` | **24 s** | 14 |
| `9b14e20` | 32 s | 18 |
| `d63bfe0` | **33 s** | 18 |

~1,8 s por caso ponta a ponta, num passo que antes do BL-434 não abria navegador. Não é caro e não recomendo cortar — **recomendo declarar**, porque o critério 9 existe para o custo aparecer, e o número que apareceu descreve 11% do custo real da etapa.

---

## M12 — BAIXA — a folha 4 do Balancete sai **em branco**

**Classe:** Testado, com a condição de medição declarada.

No controle limpo, o Balancete exporta **4 folhas**; as folhas 1, 2 e 3 têm 1880, 1991 e 1867 caracteres não-brancos, e a folha 4 tem **zero**. ⚠️ **Medido com a configuração do instrumento** (margem 0, sem faixas); com as opções padrão do diálogo de impressão a paginação muda e eu **não** medi esse caso. Registro porque uma folha em branco no fim de um documento entregue ao cliente é defeito de impressão, e porque nenhuma cláusula da frase olha para isso.

---

## M13 — BAIXA — o isolamento do timbre é provado no **contexto**, não no papel

**Classe:** Inspecionado.

A DE-061 declara, corretamente, que o instrumento **não prova isolamento entre escritórios** e que isso fica com `apps/tenancy/tests/test_bl282_timbre_escritorio.py:182`. Lendo esse teste: ele afirma sobre `resposta.context["timbre_linhas"]` — a **declaração do servidor**, não a tinta na folha. É um substituto legítimo e barato, e eu **continuo não recomendando** duplicá-lo no navegador; registro só para que a declaração da DE-061 fique completa: o que está provado é *"o servidor declara as linhas do escritório certo"*, não *"o papel carrega as linhas do escritório certo"*.

---

## M14 — BL-373 remedido: a `main` continua **sem proteção nenhuma**, nos três endpoints

**Classe:** Testado (API pública, sem autenticação, 2026-09-20).

```
GET /branches?protected=true  ->  []
GET /rulesets                 ->  []
GET /branches/main            ->  "protected": false
                                  protection.enabled: false
                                  required_status_checks.enforcement_level: "off"
                                  required_status_checks.contexts: []
                                  required_status_checks.checks:   []
```

**Nada está no ar.** As quatro verificações rodaram e ficaram verdes em `d63bfe0`, e continuam sendo **conselho** — inclusive esta reprovação. Terceira rodada seguida com o mesmo resultado; o `AGENTS.md` está **correto** ao dizer isso, e a instrução de como ligar tem o defeito do **M6**.

⚠️ **E uma observação de leitura do job, que o Fred precisa saber antes de marcar a checagem como obrigatória:** no evento **push** de `d63bfe0` (commit só de documentação), o job *"Medir identificação do emitente no navegador"* rodou com os **onze passos de medição pulados** e reportou **success**. Isso é **o comportamento correto e desejado** (`decidir_caminhos_vigiados.py` usa lista **negativa** — `docs/**`, `*.md` — então todo caminho de código é vigiado por padrão, que é a inversão do BL-415 bem aplicada). Registro porque um check verde com esse nome pode significar *"medi e passou"* ou *"não havia o que medir"*, e quem olhar a tela do GitHub não distingue os dois.

---

## M15 — O que eu tentei derrubar e **não** caiu

Registro pelo mesmo motivo de sempre. Tudo abaixo é **Testado**, no produto real, com código de saída conferido.

| Construção | Resultado |
| --- | --- |
| **BL-427 refeito** — `{% for linha in timbre_linhas\|slice:":2" %}` no Balancete | **Fechado de verdade.** `exit 1`: *"número de linhas do timbre no papel (2) diverge do número declarado pelo servidor (3)"*. **Nunca** mais "falha de infraestrutura". Era o L1, e é o melhor fechamento desta entrega |
| **BL-428** — `transform: scale(0.6)` | **Reprova nomeando tamanho**, 9,6/8,4/8,4 px. A correção faz o que promete **no eixo em que foi escrita** (ver M1 para o eixo que sobrou) |
| **BL-429** — `color: #FF0000` | **Reprova relatando 4,00:1 / 3,99:1** contra 4,5:1. Bate com o meu cálculo à mão da fórmula RGB do WCAG. Fechado |
| **BL-430** — `letter-spacing: 0.2em` a 14 px | **Passa** (`exit 0`). O falso alarme morreu |
| **BL-437** — `font-family: monospace` a exatos 11 px | **Passa.** O falso alarme no próprio piso morreu |
| **Calibração do C2** — `opacity: 0.4` | **Reprova**, 2,85/2,81/2,81:1, nomeando contraste. O par do critério 7 continua valendo |
| **BL-424** — `font-size: 8px` | **Reprova nomeando tamanho** nas três linhas |
| **C1/BL-410** — `margin-top: 260mm` | **Reprova nomeando paginação**: *"está na folha 2, não na folha 1"* (sem teste guardando — M7) |
| **C5 em folha posterior** — marca do fornecedor injetada na linha 40 da tabela, que cai na folha 2/3 | **Reprova.** A busca cobre **todas** as páginas, como o plano prometeu |
| **Deslocamento horizontal para fora da folha** — `position: relative; left: 260mm` | **Reprova**: *"AUSENTE do papel (PDF exportado)"*. O eixo horizontal, que o BL-410 não tratou, está coberto pelo mecanismo de bbox |
| **Recorte do contêiner** — `clip-path: inset(100%)` | **Reprova** pelo mesmo caminho |
| **Metadados do PDF** — limite declarado de que só o dicionário `Info` é lido, sem XMP | **O limite é verdadeiro hoje, e eu o confirmei em vez de aceitá-lo:** `pdfinfo -meta` do controle sai **vazio**, `Metadata Stream: no`. A afirmação da docstring não envelheceu |
| **BL-432** — as duas asserções de premissa | Presentes e corretas (`test_bl432_print_background_nunca_e_passado_ao_subprocesso_de_medicao`) |
| **BL-441 — o seu argumento, atacado** | **Sustenta-se, e eu verifiquei em vez de repetir.** Movido o admin para `/gestao/`, 8 de 16 testes da trilha ficam vermelhos por 404; e se alguém "consertar" os caminhos para `/gestao/`, o `assert status_code == 302` passa mas a busca do `RegistroAuditoria` **falha** — a suíte reprova nas **duas** formas. Ver a ressalva abaixo |
| **DL-030 — R1/R2, R4, R6 (Empresa), R7 (inline), atomicidade (um lado), delete** | Todos reproduzidos e corretos. A guarda do R2 **nomeia** o `ModelAdmin` de mentira. O `delete` de linha de formset (sem teste no repositório) foi medido e **grava trilha correta**, com escritório derivado por indireção e sem evento falso no pai |
| **Volume e custo da DL-030** | `POST` de `change` em `Empresa`: **17 queries**, ~112 ms, **1** `RegistroAuditoria`. Campo de 300 caracteres gravado **sem truncamento**. Não há `TextField` no projeto — o risco declarado do plano não se materializa hoje |

**Ressalva sobre o BL-441, que é dado novo e não muda a minha conclusão sobre a sua classificação:** em **produção** a falha é **muda** — o admin em `/gestao/` responde 302, a alteração persiste e **nenhum** `RegistroAuditoria` é criado, sem erro, sem aviso. A "barulheira" existe na suíte, não no sistema. Pela letra da **DE-054**, BL-441 defende o item 4 (trilha de auditoria) e isso lhe daria fundamento para subir a gravidade. **Eu mantenho BAIXA**, porque a suíte reprova nas duas formas e porque a correção (`reverse("admin:index")`) é de uma linha — mas registro o modo de falha em produção, que a sua nota não continha.

---

# Respostas diretas às suas quatro perguntas

## 1. A frase corrigida aguenta?

**A correção da DE-061 estava certa e eu a refaria. O que ela introduziu foi um defeito novo, e é meu: eu troquei uma promessa por uma lista.**

A fronteira — *"no conteúdo que o documento controla … excluída a faixa que o navegador acrescenta por fora"* — é **necessária e correta**. Não é afrouxamento. Mas eu a escrevi como **enumeração de dois canais** (*"isto é, tinta na folha e metadados do arquivo"*), e o **M2** mostra o custo: o PDF que o escritório entrega carrega **268 anotações de link** por Balancete, e um `href` com o domínio do fornecedor passa com `exit 0`. É o **BL-404 por uma porta que a minha própria correção abriu** — a DE-056 acontecendo dentro da correção da DE-059.

**A cláusula que um contador reconheceria e que a frase não cobre, e é maior que a anterior:** a frase fala **só do escritório emitente**. Não diz nada sobre **de quem** é o documento, **de quando**, nem sobre **numeração de folha** — e o **M3** mede que as folhas 2, 3 e 4 do Balancete e 2 e 3 do Diário saem **sem identificação nenhuma**, e que apagar a razão social da empresa e o período da folha 1 mantém o job verde. O próprio `personalizacao-de-relatorio.md` do projeto registra que numeração e forma são **prescritas** para Diário e Razão em forma de livro. **Se o Fred lesse "o documento sai identificado" e recebesse a folha 3 de um Diário, ele não saberia de quem é.**

**O que a frase acertou e eu mantenho sem mudar uma palavra:** *"em qualquer caixa ou espaçamento"* continua produzindo cobertura que eu não antecipei — ela pegou o pseudo-elemento, pegou os metadados, e nesta rodada segurou a marca injetada numa folha posterior. E *"exatamente as linhas que o servidor declarou"* fechou o BL-427 de forma que eu não consegui furar em nenhuma direção.

**A redação que eu proponho agora, e é uma troca de lista por critério:**

> A folha A4 exportada carrega, com tinta que contrasta com o papel, exatamente as linhas de identificação do escritório emitente que o servidor declarou, **na ordem declarada**, cada uma no seu próprio lugar; e — **em tudo que o arquivo exportado carrega e que o template ou a folha de estilo podem suprimir**, excluída a faixa que o navegador acrescenta por fora e que nenhuma folha de estilo alcança (BL-332) — não carrega nenhum identificador do fornecedor do software, em qualquer caixa ou espaçamento.

E, à parte da frase, porque é decisão de produto e não de engenharia: **o que mais precisa sair identificado, por classe de documento, é pergunta do Fred** (M3). Enquanto ela não for respondida, o limite precisa estar **declarado**, para ninguém ler o job verde como "o documento sai identificado".

## 2. O instrumento é juiz fiel dela? Sobrou substituto não declarado?

**Sobrou. Três, e o primeiro é o mesmo substituto de novo, um eixo ao lado.**

| | Cláusula | Falso conforme? | Falso alarme? |
| --- | --- | --- | --- |
| **C1** | *"a folha A4 exportada"* | Residual e declarado (rotas com `pk`/`token`). **Mas é a única cláusula sem caso ponta a ponta** (M7) | Não achei |
| **C2** | *"tinta que contrasta com o papel"* | **Sim, duas vezes:** `scale:` (**M1**, reabre BL-428 **e** BL-436) e a tinta do Chromium × a tinta declarada (**M4**) | Não achei — `letter-spacing` e `monospace` a 11 px passam |
| **C3** | *"exatamente as linhas declaradas"* | **Não.** Fechada, nas duas direções, com a frase certa e o código certo | Não achei |
| **C4** | *"cada uma no seu próprio lugar"* | **Sim:** ordem de leitura invertida passa (**M5**) | Não achei; o decoy continua não enganando |
| **C5** | *"nenhum identificador do fornecedor"* | **Sim:** anotação de link (**M2**) | Não achei |

**Os três substitutos que sobraram, na forma da DE-060:**

| A propriedade | O substituto medido | Diverge quando | Declarado? |
| --- | --- | --- | --- |
| Que **escala** o navegador aplica a esta linha | `cs.transform` × `cs.zoom`, acumulados | a página usa `scale:`, `rotate:`, `translate:` — as propriedades **individuais** de transformação | **NÃO** — a palavra `scale` não existe no instrumento nem na suíte dele |
| Que **lugar** cada linha ocupa na folha | a posição no `querySelectorAll` (ordem do DOM) | `flex-direction`, `order`, `grid`, `float` | **NÃO** |
| Que tinta a folha tem, **para a cor que o escritório escolheu** | a tinta que o Chromium exportou, depois da transformação do BL-440 | a cor tem múltiplos canais elevados — `#999999`, `#D0D0D0` | **Parcialmente** — o fato está em `direcao-de-arte.md`; a **etiqueta da mensagem** e a frase continuam generalizando |

⚠️ **E a observação de forma é a que eu quero que sobreviva a este relatório.** A DE-060 prometeu ser **finita**: *"o instrumento faz cinco medições; para cada uma, isto é a propriedade ou um substituto?"*. Nesta rodada, as cinco medições foram revisadas e corrigidas — **e o substituto mudou de nível em vez de acabar**. O que era *"que tamanho a fonte tem"* virou *"que escala o navegador aplica"*, e a nova pergunta tem a mesma forma da antiga. Isso é dado, não teoria: **a DE-060 não é finita como eu previ**, e eu previ isso por escrito na rodada 11. A correção do BL-428 foi feita por propriedade (`escala_acumulada` é melhor que `fontSize`) e ainda assim caiu na primeira construção que ninguém tinha escrito — que é a DE-055 provando o seu ponto pela terceira vez.

**Você declarou o critério de parada: se aparecesse um sexto eixo, a conversa passava a ser *"quanta garantia o produto precisa"*, pergunta do Fred.** Apareceram **três** (M1, M2, M5). **Eu recomendo acionar o seu próprio critério de parada.** Detalho em seguida.

## 3. DL-026, DL-028, DL-029 e DL-030 fecham?

**Em termos da frase, não de achados:**

- **C3 — fecha.** Nas duas direções, com a mensagem e o código certos. Não consegui furar.
- **C1 — não fecha por prova.** O comportamento está certo em tudo que medi; a cláusula é a única **sem caso de aceite ponta a ponta**, contra o critério 18 que a etapa entregou (M7). E a frase não alcança as folhas 2..N (M3).
- **C2 — não fecha.** `scale:` devolve o BL-428 e o BL-436 inteiros com uma palavra de CSS (M1), e a etiqueta do contraste generaliza além do artefato que ela mede (M4).
- **C4 — não fecha.** Ordem de leitura invertida passa (M5).
- **C5 — não fecha.** Canal de anotação de link fora do alcance, com o domínio do fornecedor dentro do arquivo entregue (M2).

**Então: DL-026, DL-028 e DL-029 não fecham.** Continua valendo **produto bom, garantia parcial** — e o nome do buraco muda de novo: **BL-427 fechou e eu confirmo**; o que está aberto é *"o instrumento mede três substitutos novos, e a frase enumera onde deveria criterizar"*.

**DL-030 fecha, com ressalvas, e é a melhor entrega desta rodada.** Todos os sete gaps foram tratados; reproduzi R1, R2, R4, R6, R7, o `delete`, a atomicidade e a guarda da cobertura, e todos fazem o que o plano diz. As declarações DE-060 no arquivo são as mais honestas que eu li neste repositório — o `_escritorio_do_objeto` **declara** que o fallback para `request.escritorio` é outro substituto, com exemplo concreto, sem eu ter perguntado. **As ressalvas são M8** (o critério de aceite 5 ficou sem teste, e o critério o pedia por nome) e **M9** (`ItemLancamento` sem derivação e sem declaração, hoje inalcançável). Nenhuma é alta. **Se essas duas forem fechadas, eu não tenho objeção a declarar a DL-030 concluída.**

**E a recomendação que eu devo ao seu critério de parada.** Apareceu o sexto eixo, e o sétimo, e o oitavo. A leitura que eu faço não é *"mais uma rodada"*:

> **O instrumento está certo sobre o produto em seis rodadas seguidas. O que não converge é a tentativa de fazê-lo provar uma frase que promete mais do que qualquer instrumento consegue.** Cada rodada eu acho um substituto novo porque *"o navegador renderizou isto do jeito certo"* é uma propriedade **infinita**, não finita — eu errei ao dizer que era finita.

Então a pergunta que eu levaria ao Fred não é *"corrige o `scale`?"*. É esta, em duas linhas: **(a)** *"quanto vale, em horas, cada nível seguinte desta garantia, sabendo que o produto está certo e que o que se poliu seis vezes foi o andaime?"*; e **(b)** *"o que mais precisa sair identificado no documento — empresa, período, folha X de Y — porque isso não está guardado por nada e é mais barato e mais útil que o sétimo substituto"*. **M1 e M2 eu corrigiria de qualquer jeito** — o primeiro porque desfaz duas correções já pagas, o segundo porque é o BL-404 vivo. **M5 e os que vierem depois, eu perguntaria antes de comprar.**

## 4. O seu método: quatro correções num dia é o processo funcionando, ou é publicar rápido demais?

**São as duas coisas, e a divisão é limpa. Tenho o dado para separá-las.**

**O que eu consegui verificar das suas declarações nesta rodada:**

| Tipo de afirmação sua | Quantas eu verifiquei | Quantas se confirmaram |
| --- | --- | --- |
| **Número medido** (suíte, lint, formatação, `check`, custo do job, contagem de testes ponta a ponta) | 7 | **7** |
| **Achado fechado** (BL-427, 428, 429, 430, 436, 437, gaps 1–7 da DL-030) | 13 | **13** |
| **Razão / mecanismo / enquadramento** (BL-419 novo, BL-440 "direção segura", BL-441 "barulhenta") | 3 | **1** |

**Você mede bem e narra mal.** Tudo que você contou, conferiu ou executou está certo — **nada**, nesta rodada nem na anterior, foi um número inventado. O que erra é sempre a mesma coisa: a **explicação** que você publica ao lado do número, escrita no tom de quem mediu, quando não mediu. BL-435 (grep no lugar da propriedade), BL-442 caso 1 e caso 2, a aritmética do "84", e agora M10 (a premissa do BL-419) e M4 (a generalização do "lado seguro"). **Seis instâncias, uma classe.**

**Por isso eu não leio "quatro correções num dia" como velocidade.** Velocidade produziria erro **variado**. Seis instâncias da mesma forma, em um dia, é **mecanismo faltando** — e é você quem escreveu, no `CLAUDE.md`, que regra escrita é pedido e que a causa do defeito de 2026-09-13 não foi distração, foi estrutura.

**E a estrutura que falta é nominal e você já a inventou.** A **DE-058** diz exatamente isto: *"toda afirmação de medição escrita **no código** tem, ao lado, o teste que a reprova, ou a marca explícita de não medido"*. Ela vale para docstring. **Não vale para a sua prosa** — e `decisoes.md`, `backlog.md` e `estado.md` são, hoje, os documentos do projeto com o maior número de afirmações verificáveis **e sem verificação ao lado**. As seis instâncias estão todas lá, nenhuma no código.

**A proposta, e é barata:** estender a DE-058 à prosa. Toda afirmação verificável escrita em `docs/projeto/**` e `docs/agents/estado.md` carrega, ao lado, **ou o comando que a produziu, ou as palavras "não medido"**. Você já aplicou isso três vezes hoje sem nomear (as sabotagens coladas no backlog do BL-440 são exatamente isso). Formalizá-lo custa um parágrafo numa decisão, e `scripts/validate-docs.ps1` já anda por **todos** os `.md` — não sei se dá para verificar isso mecanicamente, e **não afirmo que dá**; afirmo que o gancho existe e que vale medir.

**O que eu não diria, e quero ser explícito porque a pergunta era honesta:** não recomendo desacelerar. As quatro correções de ontem custaram horas; os dois defeitos que sobreviveram — M1 e M2 — custaram **seis rodadas** de auditoria cada um, e nenhum deles apareceu por falta de tempo: apareceram porque ninguém tinha escrito aquela construção. **Velocidade não os teria evitado; a DE-055, sim, e ela já está ligada.** Nesta rodada ela achou dois (BL-436, BL-437) antes de eu chegar, e eu achei três que ela não achou. Esse é o sistema funcionando com o tamanho de malha que ele tem.

---

# Resumo dos achados

| # | Gravidade | Classe | Cláusula / requisito | Uma linha |
| --- | --- | --- | --- | --- |
| **M1** | ALTA | Testado | **C2** / BL-428, BL-436, DE-060 | `scale: 0.6` passa com `exit 0` relatando 16/14/14 px sobre glifo de 9,14 px — **o mesmo papel** que `transform: scale(0.6)` reprova |
| **M2** | ALTA | Testado | **C5** / DE-061, BL-404 | `/URI (https://dataledger.com.br/)` dentro do PDF entregue, `exit 0`; o produto já embute **268** anotações de link por Balancete |
| **M3** | ALTA | Testado | a **frase** / critério 3 da DL-026 | Apagar a razão social da empresa e o período: `exit 0`. Folhas 2..N saem sem identificação nenhuma e sem número |
| **M4** | ALTA | Testado | **C2** / BL-440, DE-058 | `#999999` (9,59:1, passa) e `opacity: 0.4` (2,85:1, reprova) são a mesma tinta percebida; a razão WCAG real é **2,85:1** nos dois |
| **M5** | MÉDIA | Testado | **C4** / DE-060 | `flex-direction: column-reverse` põe o timbre de cabeça para baixo com `exit 0` |
| **M6** | MÉDIA | Testado | BL-373 | A instrução que **eu** recomendei devolve **2 de 4** nomes quando executada contra a `main` |
| **M7** | MÉDIA | Inspecionado + Testado | **C1** / critério 18 | 18 casos ponta a ponta e **nenhum** para C1 — paginação, ausência, `lancamento_id`, contagem pura |
| **M8** | MÉDIA | Testado | DL-030, critério 5 | *"Gravação que falha por validação não deixa trilha"* está certo e **não tem teste** |
| **M9** | BAIXA | Testado | DL-030, R6 | `ItemLancamento` é coberto e não deriva escritório (dois níveis); hoje inalcançável, não declarado |
| **M10** | BAIXA | Testado | BL-419 / BL-442 | *"Toda guarda mora no mesmo YAML que ela guarda"* é falso: cabe como `pytest` no job `Lint e testes` |
| **M11** | BAIXA | Testado | critério 9 | Passo de medição 3–4 s (verdadeiro); passo de testes do instrumento **24 s → 33 s**, não declarado |
| **M12** | BAIXA | Testado | — | Folha 4 do Balancete sai em branco (medido com margem 0) |
| **M13** | BAIXA | Inspecionado | DE-061 | O isolamento do timbre é provado no **contexto**, não no papel |
| **M14** | BAIXA | Testado | BL-373 | `main` sem proteção nos três endpoints; job verde pode significar "nada foi medido" |
| **M15** | — | Testado | — | O que aguentou: BL-427/428/429/430/437, calibração do C2, paginação, C5 em folha posterior, off-page horizontal, `clip-path`, XMP, e a DL-030 quase inteira |

**Nota sobre gravidade, declarada para você discordar com o mesmo dado que eu tenho.** Pela letra da **DE-054**, M1 a M4 defendem o item 5 (*"o documento que sai para o cliente"*) e M2 devolve um identificador do fornecedor ao arquivo entregue — o **corte exato** que fez do K1 um bloqueador na rodada 10. **Não classifico M2 como bloqueador** porque a construção que o expõe é uma sabotagem, e o produto de hoje não tem link para o domínio do fornecedor; o que ele tem são 268 URIs internas cuja leitura em produção eu **inferi** e declarei como inferência. Se você aplicar a DE-054 ao pé da letra, o dado sustenta chamar M1 **e** M2 de bloqueadores. **O parecer não muda nos dois casos.**

---

# Parecer

## REPROVADO

**Motivo, em uma frase:** quatro achados de **gravidade alta**, e dois deles — `scale:` (M1) e a anotação de link (M2) — **desfazem correções que esta etapa acabou de pagar**, reabrindo o BL-428, o BL-436 e o canal do BL-404 por construções que ninguém tinha escrito.

Não aprovo com ressalvas porque C2, C4 e C5 são três quintos da frase que esta etapa existe para executar, e porque uma ressalva sobre *"o identificador do fornecedor está dentro do arquivo entregue ao cliente"* não é ressalva.

**O que está bom e eu não quero que se perca na reprovação, porque é muito:**

- **O BL-427 fechou de verdade, e era o pior modo de falha de todos.** Um Balancete sem o registro profissional do contador agora sai com código 1 dizendo *"número de linhas do timbre no papel (2) diverge do número declarado pelo servidor (3)"*, em vez de mandar quem lê o job procurar defeito no navegador. Refiz a sabotagem exata e confirmei.
- **Os outros quatro substitutos da rodada 11 morreram como prometido**, e eu reproduzi todos: `#FF0000` reprova a 4,00:1 — número que bate com o meu cálculo à mão; `letter-spacing` passa; `monospace` a 11 px passa; `transform`/`zoom` reprovam nomeando tamanho.
- **A DL-030 é a entrega mais limpa que eu auditei neste projeto.** A cobertura virou propriedade com lista de exclusão de **dois** itens e motivo por item; o escritório passou a vir do objeto por **inspeção do modelo**; a senha é redigida **pelo widget**; a atomicidade deixou de ser comentário e virou medição; a guarda do R2 reprova **nomeando** um `ModelAdmin` de mentira. E o implementador declarou o substituto que sobrou **antes de alguém perguntar**, com exemplo concreto do projeto. Duas ressalvas pequenas, nenhuma alta.
- **Você tinha razão no BL-441, e eu verifiquei em vez de amplificar o meu auxiliar.** A falha é barulhenta na suíte **nas duas formas** — o 404 e o `assert` da trilha. Mantenho BAIXA, e acrescento o dado que faltava: em produção ela é **muda**.
- **Os quatro números conferem, um a um, pela segunda rodada seguida**, e o custo do critério 9 é real: **4 s** no job.
- **A tese do caminho A está confirmada pela quarta rodada seguida.** Continuo **não** recomendando revertê-la: todos os achados desta rodada vieram de abrir o Chromium e olhar o PDF — inclusive os quatro que são contra o instrumento de navegador.

**E o item que eu peço para você tratar antes de qualquer correção de código:** o **M6**. A instrução que destrava todos os outros é a minha, está no `AGENTS.md`, e executada como escrita devolve metade da lista. Corrigir o alvo da consulta custa minutos; o teste offline contra os `name:` dos jobs custa vinte linhas e acaba com o assunto. Enquanto a proteção não estiver ligada e **lida na API**, tudo aqui — inclusive esta reprovação — é **conselho** (M14).

**Nenhuma afirmação deste relatório é declaração de ausência de defeitos, de segurança absoluta ou de conformidade legal.** Auditoria de software não substitui a validação profissional das regras contábeis e legais pelo responsável técnico do domínio — e, em especial, **a adequação do conteúdo do timbre e da identificação do documento às normas aplicáveis é decisão do Fred**, não deste relatório: onde citei `personalizacao-de-relatorio.md` (M3), citei o **mapa do projeto**, não a norma, e não afirmo violação de norma nenhuma. Onde precisei julgar legibilidade, **medi ou declarei não medido**: pelo **BL-422**, não formei juízo olhando imagem em nenhum ponto; a folha 4 em branco (M12) foi medida só com a configuração do instrumento, e eu declaro isso. As pendências de piso — **PE-57** (o WCAG como empréstimo), **PE-58** (o tamanho mínimo no papel), **PE-59** (monocromático ou colorido, que o M4 volta a tocar) e **PE-60** — continuam sendo dele, e a elas eu acrescento a pergunta do M3: **o que mais precisa sair identificado no documento, por classe.**
