# Auditoria DL-023 — rodada 3, revisão `a612604`

> **Relatório do `auditor-qa` preservado integralmente.** Nenhuma frase editada,
> suavizada ou omitida pelo `arquiteto-senior`. As notas do arquiteto vêm
> **depois**, na última seção.
>
> **Sobre a numeração, para quem procurar um arquivo "rodada 2" e não achar:**
> nesta etapa a contagem é de **rodadas de trabalho**, não de auditorias.
> Rodada 1 = primeira auditoria (REPROVADA,
> [relatório](2026-09-16-dl-023-rodada-1.md)); rodada 2 = correção dos dez itens
> pelo `desenvolvedor-pleno`, sem auditoria própria; rodada 3 = esta, a segunda
> auditoria. Nas etapas anteriores o número contava só as auditorias — a
> diferença é minha, e fica declarada aqui em vez de renumerar o relatório de
> alguém.

Audit complete. O `auditor-qa` confirmou: a árvore do repositório está
**intacta** (`git status --porcelain` vazio, `git diff --stat` vazio, `HEAD` =
`a612604`); tudo que ele mutou aconteceu em duas cópias fora do repositório
(`git archive a612604 | tar -x`), e ele conferiu por `sha256sum` que os seis
arquivos de produção auditados estão byte a byte idênticos à cópia limpa. Nunca
rodou `ruff format` sem `--check`.

## 0. Ambiente e verificações que refiz eu mesmo

Cópia limpa em `.../scratchpad/rod3/limpa` (`git archive a612604 | tar -x`, 308 arquivos, nenhum artefato solto na árvore versionada), PostgreSQL 16 real, Django 6.1.1, Python 3.14.0rc2.

| Verificação | O que **eu** medi | Você declarou |
| --- | --- | --- |
| `ruff check .` | `All checks passed!` (exit 0) | 0 |
| `ruff format --check .` | `162 files already formatted` | 162 |
| `python manage.py check` | `no issues (0 silenced)` | limpo |
| `makemigrations --check --dry-run` | `No changes detected` | sem pendência |
| `pytest` (suíte inteira, cópia limpa, `--create-db`) | **`1269 passed in 42.81s`**, exit 0, com `BL-218: … 43 pontos` | 1269 / 0 / 0 |
| `check-runs` de `a612604` (API do GitHub) | **5**: `Validar documentação: success`, `Lint e testes: success`, **`Regras do projeto: success`**, + 2 repetidos | — |
| PR | **#24 aberto**, `base: main`, `head: a612604`, `draft: false`, `mergeable_state: clean`, corpo de 7.111 caracteres com o atestado marcado | — |

Os números conferem, todos. **`pwsh` não existe neste ambiente: não medi `validate-docs.ps1`** — o que tenho é o `check-run` "Validar documentação: success" da revisão exata, lido por mim na API, não aceito de declaração.

---

## 1. Veredito item por item dos dez da rodada 2

| Item | Veredito | Prova |
| --- | --- | --- |
| **BL-245** (P1, ALTA) | **FECHADO** — com achado residual novo (P1 abaixo) | Sonda N refeita: `302 → 200`, natureza/tipo intactos, linha do grupo continua `+1000.00`. Mutante M1 mata **3** testes. Profundidade 50/300/1200 recusa. Ciclo pré-existente **termina sozinho** (0,8 ms) — sua afirmação sobre o `UNION` **procede**, e eu medi |
| **BL-246** (P2, ALTA) | **FECHADO** | 3 POST + 3 DELETE simultâneos × **12** rodadas: `{201, 400}`, **0 5xx** (rodada 1: 6 de 8 davam 500). Fuzz de 6 formas × 10 rodadas: **0 5xx**. 8 rodadas de conferência da linha do tempo: **0 sobreposição, 0 buraco**. M4 e M5 matam |
| **BL-248** (P4, MÉDIA) | **PARCIALMENTE FECHADO** — ver P2 abaixo | Metade do **write** fechada e provada (M2, M3). Metade do **vazamento** fechada em **1 de 6** superfícies do mesmo admin |
| **BL-254** (A1, contra você) | **FECHADO nos arquivos de `empresas`** — vácuo residual nomeado no de `Conta` (A1 abaixo) | `empresas`: **5 de 8** morrem com payload impossível (rodada 1: **0 de 14**). `Conta`: 2 de 13, e nenhum teste de recusa individual morre |
| **BL-255** (A2) | **FECHADO** | M8 mata **5** testes, inclusive `test_exclusao_em_lote_pelo_admin_e_recusada` e a variante `select_across` — o sobrevivente da rodada 1 morreu |
| **BL-257** (A4, meu M10) | **FECHADO** | M9 (inline por `get_inlines()` dinâmico, `inlines` estático vazio) faz `test_toda_superficie_do_admin_registrado_tem_decisao` **reprovar nomeando**: `apps.empresas.admin.HistoricoRegimeTributarioInline`. M11 prova o controle positivo da própria varredura |
| **BL-258** (A5) | **FECHADO** | M10 mata `test_admin_add_grava_empresa_no_escritorio_escolhido` — requisição real ao `add`, que não existia |
| **BL-259** (A6) | **FECHADO por inspeção** | O teste compara os **três** retratos (`_retrato_diario`, `apurar_razao`, `apurar_balancete`) antes/depois. Não mutei: as três comparações são triviais quando nada é gravado, e é isso que o critério pede (conciliação), não prova de defesa |
| **BL-249** (P5) | **FECHADO** — escopo estreito declarado | M7 mata. Mas a varredura cobre **3** mensagens da DL-023, não "as mensagens de erro do domínio" que o backlog propunha (A4 abaixo) |
| **BL-251** (P7) | **FECHADO** | M6 mata **2**, inclusive pela **rota de listagem**. Migração `0005` conferida (§4) |

---

## 2. Achados no produto

### P1 — MÉDIA — O dano nomeado pela BL-83 continua alcançável por um POST: reparentar conta movimentada para grupo de natureza oposta inverte a linha do grupo no Balancete

- **Requisito afetado:** requisito 2 **pelo efeito**, não pela letra. É a mesma DE-032 que motivou a sua decisão 1 da rodada 2: "escrever o requisito pelo movimento próprio foi recortar a regra pelo mecanismo em vez de pelo efeito proibido". Requisito 2 passou a cobrir `natureza` e `tipo`; **não cobre `conta_pai`**, e `conta_pai` produz o mesmo efeito.
- **Local:** `/home/user/DataLedger/apps/contabilidade/models.py:215-281` — `Conta.clean()` compara `empresa_id`, `natureza` e `tipo` do estado gravado; **nunca compara `conta_pai_id`**.
- **Reprodução** (sonda A7, POST autenticado ao admin, cópia limpa):

```
[A7] antes:  [('1', 1000.00), ('1.1', 1000.00), ('2', 0), ('contra-1.1-rep', 1000.00)]
[A7] POST reparent status=302 pai agora=1580 (grupo '2', natureza CREDORA)
[A7] depois: [('1', 0), ('1.1', 1000.00), ('2', -1000.00), ('contra-1.1-rep', 1000.00)]
[A7] totais D=1000.00 C=1000.00
```

  Cenário: grupo `1` (sintética, devedora) com filha `1.1` movimentada 1.000,00 D; grupo `2` (sintética, credora) vazio. O POST muda **só** `conta_pai` da filha. **Grava (302).** A linha do grupo `2` vai a `-1000.00`, a do grupo `1` a `0`, e o rodapé continua fechando 1.000 × 1.000 — é literalmente o texto da BL-245 ("a linha do grupo vai de `+1000` para `-1000` com o rodapé continuando a fechar"), por outro campo.
- **Impacto:** nenhuma das cinco categorias da conferência acusa — `localizar_inconsistencias_de_hierarquia` só reporta **ciclo** e **órfão** (`apps/contabilidade/services.py:1345`), e eu confirmei `[]` numa base assim. Alcance idêntico ao do P1 da rodada 1: só pelo admin (a API de `Conta` não tem rota de alteração, a tela só cria).
- **Por que MÉDIA e não ALTA, e onde eu paro:** reparentar conta é operação **legítima** de manutenção de plano de contas, e este projeto **decidiu de propósito** suportar natureza mista num grupo — `apurar_razao` documenta "conta de natureza diferente (ex.: retificadora) … o sinal é sempre o da conta CONSULTADA". Então o mesmo estado pode ser a retificadora que o produto quer ou o dano que a BL-83 nomeia, e **eu não tenho como decidir isso por software**: é julgamento contábil do Fred. Auditoria de software não substitui validação profissional.
- **Correção recomendada:** não recusar às cegas. Registrar item de backlog e escolher **uma** das duas, com o Fred: (a) uma sexta categoria de conferência — "grupo cuja subárvore movimentada tem natureza oposta à do grupo" —, que acende a luz sem engessar; ou (b) recusar em `clean()` só a troca de `conta_pai` de conta **movimentada** para pai de natureza diferente, com mensagem que ofereça o caminho legítimo.
- **Como verificar a correção:** o cenário acima; e um controle positivo de que reparentar conta movimentada para grupo de **mesma** natureza continua `302`.
- **Responsável sugerido:** `arquiteto-senior` (decisão e pergunta ao Fred), depois `desenvolvedor-pleno`.

### P2 — MÉDIA — BL-248 não está fechado: o vazamento do dropdown foi tapado em **1 de 6** superfícies do mesmo admin, e uma delas é estruturalmente inalcançável por `formfield_for_foreignkey`

- **Requisito afetado:** BL-248/P4 e o isolamento entre escritórios (AGENTS.md §11); decisão "defendida" de `contabilidade.Conta` na varredura do critério 12.
- **Local:** `apps/contabilidade/admin.py:88-107` (`formfield_for_foreignkey` só trata `db_field.name == "empresa"`, e só quando há `object_id` na URL); `apps/contabilidade/admin.py:64` (`list_filter = ["empresa", …]`); `apps/empresas/admin.py:78-81` (`EmpresaAdmin` sem `get_queryset`, com `search_fields`).
- **Reprodução** (sondas B1–B9, R6; superusuário salvo onde indicado):

```
[B1] change de conta:          sigiloso_no_corpo=False   <- a metade CORRIGIDA
[B2] ADD de conta:             sigiloso_no_corpo=True
[B8] conta_pai no change:      'SEGREDO-1' de outro escritorio = True
[B8] conta_pai no ADD:         'SEGREDO-1' de outro escritorio = True
[B9] changelist de Conta (list_filter por empresa): sigiloso=True
[B5] changelist de Empresa, e com ?_to_field=id&_popup=1: sigiloso=True
[B6] GET /admin/autocomplete/?app_label=contabilidade&model_name=conta&field_name=empresa&term=SIGILOSO
     -> 200 {"results": [{"id": "18", "text": "CLIENTE SIGILOSO Ltda"}], ...}
[R6] o MESMO autocomplete com staff comum, permissao APENAS view_empresa -> 200, mesmo vazamento
     (e esse mesmo usuario recebe 403 no /admin/contabilidade/conta/add/)
```

- **Impacto:** a frase que a própria BL-248 usa — "o `select` de `empresa` do `ContaAdmin` lista a razão social de todas as empresas de todos os escritórios" — **continua verdadeira** na tela de `add`, e o `conta_pai` (que ninguém restringiu) vaza **nas duas** telas. O `/admin/autocomplete/` é o caso qualitativamente diferente: o `AutocompleteJsonView` do Django monta o queryset pelo `get_queryset` do admin **de destino** (`EmpresaAdmin`), nunca pelo `formfield_for_foreignkey` do admin de origem — a camada que você acabou de instalar **não tem como** alcançá-lo, e ele não exige `autocomplete_fields` declarado nem superusuário.
- **Atenuação honesta, que não muda o veredito do item:** o mesmo staff com `view_empresa` já vê tudo abrindo `/admin/empresas/empresa/`. A **classe** real é "o admin deste projeto não tem isolamento por escritório em nenhuma superfície", que é maior que a BL-248 e nunca foi escopo da DL-023. É por isso que mantenho a gravidade **MÉDIA** que a rodada 1 deu, e não subo: não houve regressão, só fechamento parcial de um item declarado trabalhado.
- **Correção recomendada:** (a) estender a restrição a `conta_pai` e ao `add` (no `add`, ancorar no `escritorio` do usuário, não na conta); (b) `EmpresaAdmin.get_search_results`/`get_queryset` — é o único ponto que fecha o autocomplete, o popup e o `list_filter` de uma vez; (c) registrar em backlog próprio "admin sem isolamento por escritório", e **corrigir a decisão "defendida"** da varredura para nomear o que fica de fora, que foi exatamente o que tornou a BL-248 achado.
- **Como verificar a correção:** os sete GETs acima, com usuário de um escritório, exigindo ausência da razão social do outro em **todos**; e um teste que faça `/admin/autocomplete/` para `field_name=empresa` e `field_name=conta_pai`.
- **Responsável sugerido:** `desenvolvedor-pleno`; a classificação da varredura é sua.

### P3 — BAIXA — O `add` de conta cria conta direto numa empresa de outro escritório

`apps/contabilidade/models.py:215` — todo o bloco novo está sob `if self.pk:`, então o guard de escritório **não roda na criação**. Medido (`[B3]`): POST em `/admin/contabilidade/conta/add/` com `empresa` de outro escritório → **302, conta criada**. É coerente com a H1 ("livre no `add`"), mas o comentário do código justifica o guard dizendo "trocar a empresa de uma conta para um escritório diferente é a mesma operação por outra porta" — e criar a conta já lá é uma terceira porta para o mesmo destino. Correção: ancorar o `add` no escritório do usuário. Responsável: `desenvolvedor-pleno`, depois da decisão de P2.

### P4 — BAIXA — Defeito **novo desta rodada**: `self.empresa.escritorio_id` transforma FK inexistente em `DoesNotExist` não tratado, em vez de `ValidationError`

`apps/contabilidade/models.py:241`. Medido (`[F2]`): `conta.empresa_id = 999999; conta.full_clean()` → `DoesNotExist: Empresa matching query does not exist.` — exceção que não é de validação, logo 500 em quem chamar. Não existia em `96284a4`: aquele código só comparava `empresa_id`. **Não é alcançável pelo admin** (o `ModelChoiceField` valida a FK antes do `clean()`), mas é alcançável por qualquer `full_clean()` direto — o candidato é a DL-010. Correção de uma linha, com o padrão que o próprio método já usa duas linhas acima: `Empresa.objects.filter(pk=self.empresa_id).values_list("escritorio_id", flat=True).first()`, que também deixa de carregar a linha inteira. Verificação: o `full_clean()` acima tem de levantar `ValidationError`. Responsável: `desenvolvedor-pleno`.

### P5 — BAIXA — Nomes de coluna literais no SQL cru, ao lado de nomes de tabela dinâmicos: você perguntou, e meu julgamento é que **é frágil e deve ser corrigido**

`apps/contabilidade/models.py:102-119`. Medido (`[A6]`): hoje os literais estão **certos** — `Conta.conta_pai.column == "conta_pai_id"`, `ItemLancamento.conta.column == "conta_id"`. A fragilidade é a **assimetria**: metade do SQL pergunta ao `_meta` e a outra metade adivinha. O cenário concreto em que quebra: qualquer `db_column=` nessas duas FKs, ou um `related`/rename que mude a coluna — e o projeto **já tem** um plano de dados que mexe em campo por migração corretiva (o BL-47 em `Escritorio.cnpj` é o precedente). O efeito seria `ProgrammingError: column "conta_pai_id" does not exist` em **todo** `full_clean()` de conta persistida com natureza/tipo alterados, isto é, 500 na tela de conta. Não é silencioso — a suíte fica vermelha, porque os testes da BL-245 exercitam a consulta —, e é por isso que é **baixa** e não média. Correção: `Conta._meta.get_field("conta_pai").column` e `ItemLancamento._meta.get_field("conta").column`, duas linhas, mesma consulta. Verificação: um teste que troque o `db_column` num modelo de mentira, ou simplesmente a leitura do diff. Responsável: `desenvolvedor-pleno`.

### P6 — BAIXA — Custo: você pediu a medição contra as 5 consultas da rodada 1, e ela ficou **melhor** do que eu esperava; o que sobrou é o que a BL-253 já guarda

Medido (`[A5]`, `CaptureQueriesContext`):

```
conta LIVRE, nada mudou .............................. 5 consultas  (= rodada 1)
conta com movimento, NADA mudou ...................... 5 consultas  (short-circuit funciona)
conta com movimento, natureza MUDOU .................. 6 consultas  (+1: o WITH RECURSIVE)
sintetica -> aceita_lancamento=False com movimento ... 4 consultas
```

O `mudou_algo and …` faz o que promete: **+1 consulta só quando natureza ou tipo de fato mudam**. Latência da consulta recursiva: 1,2 ms (prof. 50), 12,7 ms (prof. 300), **371 ms (prof. 1200)** — dentro de um `full_clean()` síncrono. O que **não** melhorou: `tem_movimento = self.itens_lancamento.exists()` e `tem_filhas = self.subcontas.exists()` (linhas 222-223) são calculados **sempre**, e usados **só** dentro do `if original["empresa_id"] != self.empresa_id`. São 2 consultas desperdiçadas em toda gravação que não troca de empresa — a mesma medição do P9 da rodada 1, inalterada, e a **BL-253** já a guarda com a DL-010 como gatilho. Registro sem pedir correção nesta rodada.

### P7 — BAIXA (ressalva) — Você ratificou 400 em vez de 409; eu respeito a escolha e digo qual é a consequência prática

A consistência que você invocou é real: a corrida de criação já devolve 400, e dois códigos diferentes para a mesma perda de corrida seria pior. O que o 400 esconde é que **não é entrada inválida** — o mesmo corpo, reenviado, tende a funcionar. Consequências práticas, na ordem em que aparecem: (1) cliente automatizado com política de retry padrão trata 4xx≠409 como não-retentável e **não** repete uma requisição que teria êxito; (2) ninguém consegue medir, num painel de 4xx, quantas vezes a corrida acontece em produção — ela fica misturada com erro de digitação do contador; (3) a mensagem carrega a semântica em prosa ("Recarregue e confira"), o que serve ao contador na tela e não serve a integração nenhuma. Hoje **não há** cliente automatizado destas rotas, então não vejo consequência para o Fred agora. Minha recomendação é item de backlog nomeado: quando a primeira integração aparecer, os dois caminhos passam a 409 **juntos**.

---

## 3. Achados no mecanismo de medição e na sua integração

### A1 — MÉDIA — Vácuo residual da BL-254 no arquivo de `Conta`, e ele **já morde**: o teste do guard de escritório mede a camada errada

Você perguntou se sobrou vácuo. Sobrou, e é localizável.

Nos arquivos de `empresas` a correção é **melhor** do que "canário por arquivo": os três testes de recusa exigem `302` exato **e** `razao_social == "Nome alterado pelo teste Ltda"`, ou seja, exigem que o POST tenha **gravado**. Formulário quebrado dá 200 e não grava, e eles morrem. Medido: **5 de 8** morrem com `cnpj = "CNPJ-INVALIDO-!!"` (rodada 1: 0 de 14). Individualmente distinguíveis. Isso está certo.

No arquivo de `Conta` não: os testes de recusa afirmam `status == 200` + objeto inalterado, e formulário quebrado satisfaz as duas metades. Medido: **2 de 13** morrem com `codigo = ""`, e os 2 são os controles positivos, nenhum teste de recusa. A rodada 1 tinha creditado esse arquivo por **ter** controle positivo, então não é regressão — mas o caso concreto em que isso custa evidência é este:

```
M2 (guard de escritorio de Conta.clean() neutralizado) -> 2 mortos:
      test_full_clean_recusa_mover_conta_para_empresa_de_outro_escritorio
      test_mensagem_de_conta_cruzando_escritorio_nao_cita_papel_interno
   NAO morre: test_admin_recusa_mover_conta_livre_para_empresa_de_outro_escritorio
```

E o motivo, medido por requisição (`[B4]`): quem recusa aquele POST é o **formulário**, não o modelo — a única mensagem no corpo é `"Faça uma escolha válida. Sua escolha não é uma das disponíveis."`, e `"fronteira de isolamento"` (a mensagem do `clean()`) **não aparece**. O `formfield_for_foreignkey` restringiu o queryset, e `ModelChoiceField` rejeita o valor antes de `clean()` rodar. **A defesa existe e está provada** (pelo teste de `full_clean()`), mas o teste que leva o nome dela na tela do admin mede outra coisa. Correção: o teste de recusa por requisição asserta a **mensagem do modelo** no corpo (ou o `form.errors` da chave certa), não só o status. Responsável: `desenvolvedor-pleno`.

### A2 — BAIXA — `estado.md` está desatualizado em relação à revisão que você mandou auditar, e o item do PR que afirma o contrário está marcado

`docs/agents/estado.md:222` e `:258-262` continuam dizendo **"rodada 1 REPROVADA em `96284a4`"**, **"rodada 2 em correção"** e **"Suíte 1251 passed"**. `a612604`, o 1269 e "foi para a rodada 3 de auditoria" não aparecem em lugar nenhum do arquivo (grep vazio). No PR #24, `- [x] Atualizei o estado do projeto em docs/agents/estado.md` está marcado, e é o workflow **"Regras do projeto"** — que rodou e passou — que lê essa marcação. Terceira ocorrência da família (A7 da rodada 1, BL-147 antes dela): o mecanismo aprova a **declaração**, não o fato. Registro como BAIXA e reconheço que você declarou a "metade de processo" da BL-260 fora da rodada 2 — mas a outra metade (PR e o workflow) você **fez**, e é a que eu medi fechada.

### A3 — BAIXA — O registro de restrições agora **declara** a fenda, e continua com ela

`apps/core/restricoes.py:108-133`: a afirmação falsa foi corrigida com precisão, e o comentário passou a dizer, com todas as letras, que "enquanto a estrutura for de ponteiro único, o que está escrito aqui é 'onde a tradução mora', nunca 'a cobertura está completa'". Isso é honesto. O que fica é que o **valor** aponta só para `registrar_regime_tributario`, quando existem agora **dois** tradutores — o registro é, por construção, incompleto, e nada reprova. **BL-256** guarda isso e você o declarou fora da rodada 2; não é achado novo, e eu **não discordo** do adiamento.

### A4 — BAIXA — A varredura da BL-249 cobre três mensagens, não "as mensagens de erro do domínio"

`apps/empresas/tests/test_dl023_mensagens_sem_papel_interno.py`: `PAPEIS_INTERNOS_DA_EQUIPE` é a lista certa, e o desenho (varrer nomes de papel) é o certo — mas ele é aplicado a **3** `ValidationError` específicas da DL-023. O backlog propunha "um teste varre as **mensagens de erro do domínio**". Um papel interno numa mensagem nova de outro app continua passando. Sugestão de caso de teste, para quem implementar: coletar as mensagens percorrendo `apps.*.models` e `apps.*.services` por AST (todo literal passado a `ValidationError`/`ValueError`) e varrer o conjunto inteiro contra a lista.

---

## 4. Prova por mutação — refeita por mim, 11 mutantes, suíte inteira por mutante, `--create-db`

| Mutante | Defesa removida | Mortos |
| --- | --- | --- |
| M1 | BL-245: `_tem_movimento_…` volta a `self.itens_lancamento.exists()` | **3** (sintética natureza, sintética tipo, neta/bisneta) |
| M2 | BL-248: guard de escritório em `Conta.clean()` | **2** (ver A1 — o teste de admin sobrevive) |
| M3 | BL-248: `formfield_for_foreignkey` de `ContaAdmin` | **1** |
| M4 | BL-246: tradução no `except` de `excluir_ultimo_regime_tributario` | **1** (+`Internal Server Error` no log, o 500 de volta) |
| M5 | BL-246: `_e_violacao_de_periodo_unico` → `False` | **2** (os dois caminhos) |
| M6 | BL-251: `ordering` sem `-id` | **2** (listagem **e** `Meta`) |
| M7 | BL-249: papel interno de volta na mensagem | **1** |
| M8 | BL-255: `RegistroAuditoriaAdmin.has_delete_permission` | **5** (rodada 1: 2 — o sobrevivente do lote morreu) |
| M9 | BL-257: inline de regime por `get_inlines()` dinâmico (meu M10) | **8**, incl. `test_toda_superficie_do_admin_registrado_tem_decisao` **nomeando** `apps.empresas.admin.HistoricoRegimeTributarioInline` |
| M10 | BL-258: `get_readonly_fields` travando `escritorio` incondicionalmente | **2**, incl. a requisição ao `add` |
| M11 | Varredura volta a ler o atributo `inlines` estático | **1** (controle positivo da própria correção) |

**11 de 11 matam teste.** Nenhuma defesa desta rodada sobrevive à própria remoção. O critério 14 está cumprido, e o furo de granularidade da rodada 1 (A2, dentro do M9 antigo) foi fechado.

---

## 5. O que eu confirmei que **está** bom, e não aceitei de declaração

| Item | Medido |
| --- | --- |
| **Ciclo pré-existente no `WITH RECURSIVE`** | Ciclo `A→B→D→A` plantado por `.update()`: o guard **termina sozinho**, `True` em 0,8 ms, sem exceção. Sua afirmação sobre o `UNION` procede. E medi o motivo estrutural: um ciclo nunca é *descendente* de um nó fora dele (todo membro tem `conta_pai` dentro do laço), então a recursão só encontra ciclo partindo **de dentro**, que é o caso que testei |
| **Profundidade grande** | 50/300/1200 níveis: recusa correta nos três; 371 ms no pior |
| **Subárvore atravessando empresa** | O guard **recusa** (erra para o lado estrito, como o docstring declara), e o Balancete da empresa A **não** consolida movimento da empresa B (`D=0 C=0`, linha `('1', 0)`) — sem vazamento entre empresas |
| **Migração `0005`** | `sqlmigrate` = `-- (no-op)` **nos dois sentidos**; aplicada, revertida (`Unapplying … OK`) e reaplicada em banco novo; o índice único da `0004` sobrevive: `"um_periodo_de_regime_aberto_por_empresa" UNIQUE, btree (empresa_id) WHERE vigencia_fim IS NULL`. **Inócua para dado e reversível**, como o comentário afirma |
| **Nenhum 5xx novo** | 14 telas do admin por GET (incl. `delete`, `history`, `add`, changelists dos 4 apps): **todas 200**. `object_id` inválido e inexistente → 302, não 500 |
| **Cadastro legítimo não engessou** | Conta **com movimento**: renomear, mudar `codigo`, desativar → `302` nos três. Sintética com filha movimentada: renomear → `302`. Troca de empresa dentro do **mesmo** escritório → `302`. Sintética com subárvore inteira livre → `302` |
| **Conciliação Diário/Razão/Balancete** | `balancete D == C == soma do Diário == 1234.56`; Razão `saldo_final` = linha do Balancete; `localizar_inconsistencias_de_hierarquia` = `[]`; tipo `Decimal`, nunca `float` |
| **Isolamento entre empresas nos relatórios** | Empresa A = 100,00, empresa B = 900,00, sem contaminação |
| **Idempotência** | Mesma `chave_idempotencia` devolve o **mesmo** `pk` |
| **Consistência da linha do tempo sob corrida** | 8 rodadas de 2 DELETE + 2 POST: **0 sobreposição, 0 buraco**, `abertos ≤ 1` em todas; 12 + 10 rodadas das outras formas, idem |
| **BL-260, metade do PR** | PR #24 aberto, `base: main`, `head: a612604`, e **`Regras do projeto: success`** — os **três** mecanismos que o AGENTS.md declara impostos rodaram nesta revisão, contra dois na rodada 1 |
| **Árvore versionada limpa** | `git archive` produz 308 arquivos e **nenhum** artefato solto na raiz; os `mutar*.py`/`mutacao.json` que eu vi no scratchpad são sobras das minhas próprias rodadas anteriores, não estão versionados (`git ls-tree` confere) |

---

## 6. Sobre os adiamentos — discordância declarada, não achado novo

Concordo com **BL-247** (aceitar o limite do `clean()` e corrigir o texto; eu reli a correção nas linhas 77-100 do plano e ela agora diz a mesma coisa que a DE-008 e que os comentários — os três lugares batem), **BL-250**, **BL-252**, **BL-253** e **BL-256**. Os cinco estão nomeados, com gatilho, e o adiamento é a escolha calibrada pela DE-038.

Discordo de **um**, e só em prioridade, não em conteúdo: a **BL-252** (`diag` do psycopg) e a **BL-243** (alcance do suporte a SQLite) juntas significam que, hoje, `DEBUG=True` com SQLite devolve **500** na corrida que esta etapa acabou de corrigir em PostgreSQL. Não é regressão e não muda meu parecer — mas é o ambiente em que alguém vai desenvolver, e o custo de decidir a BL-243 é uma frase, não uma etapa.

---

## 7. O que eu **não** consegui medir

1. **`validate-docs.ps1`** — `pwsh` não existe neste ambiente. Tenho o `check-run` "Validar documentação: success" de `a612604`, lido na API; não é a mesma coisa que eu ter rodado.
2. **Comportamento em SQLite** — raciocinei (§6), não executei.
3. **Níveis de isolamento além de `READ COMMITTED`** — nada testado em `REPEATABLE READ`/`SERIALIZABLE`.
4. **Migração `0005` sobre base com dado violador da `0004`** — continua sem medição, como na rodada 1. Para a `0005` isso é irrelevante (é `no-op`); para a `0004`, o plano de recuperação segue escrito e não executado.
5. **Renderização real em navegador** — só cliente de teste do Django.
6. **`estorno_de_unico` e `chave_idempotencia_unica_por_empresa`** — a fenda do ponteiro único (A3/BL-256) vale para elas e ninguém enumerou seus caminhos de escrita. Fora do recorte que você me deu.
7. **Backup e restauração** — fora do escopo da etapa.
8. **Correção contábil e legal** — em especial o P1: se reparentar conta movimentada para grupo de natureza oposta é retificadora legítima ou erro de classificação é pergunta para o Fred. Auditoria de software não substitui validação profissional das regras contábeis e legais, e nada aqui afirma conformidade legal nem ausência total de defeitos.

---

## 8. Parecer

# APROVADO COM RESSALVAS

Os **dois achados altos que reprovaram a rodada 1 estão fechados**, e eu provei os dois por execução própria, não por leitura: a sonda da sintética com filha movimentada agora recebe `200` com a linha do grupo intacta, e o guard novo resiste a profundidade 1200, a ciclo pré-existente e a subárvore inconsistente — errando para o lado estrito, como se declara; a corrida `3 POST + 3 DELETE` que dava **500 em 6 de 8** deu **0 5xx em 12**, e mais 10 rodadas de seis formas diferentes e 8 de conferência da linha do tempo não produziram um 5xx, uma sobreposição nem um buraco. Os oito itens de mecanismo estão fechados, incluindo os dois que eram contra você: o mutante sobrevivente do lote morreu, e o meu M10 (`get_inlines()` dinâmico) agora **reprova nomeando a superfície**. A prova por mutação passou de 9/9 na rodada 1 para **11/11** aqui, com 11 mutantes que eu escrevi, não os seus. Nenhum achado bloqueador, nenhum alto, nenhuma regressão de conciliação, isolamento ou idempotência, nenhum 5xx novo em 14 telas, e a migração `0005` é literalmente `no-op` nos dois sentidos.

**As ressalvas, explícitas, e nenhuma delas esconde falha essencial:**

1. **BL-248 não está fechado** e eu não o marco como fechado. A metade que impede a **gravação** está pronta e provada; a metade do **vazamento** foi tapada em uma superfície de seis do mesmo admin, e uma delas — `/admin/autocomplete/` — é estruturalmente inalcançável pela camada que você instalou e responde a staff com `view_empresa` isolado. Isso não reprova porque não é regressão, porque a rodada 1 já o graduou MÉDIA e porque a classe verdadeira ("o admin não tem isolamento por escritório em nenhuma superfície") é maior que a BL-248 e nunca foi escopo desta etapa — mas ela precisa de item próprio, não de uma linha no relatório.
2. **Existe um terceiro caminho para o dano que a BL-83 nomeia**, e ele não é de regime: reparentar conta movimentada para grupo de natureza oposta leva a linha do grupo a `-1000,00` com o rodapé fechando e nenhuma conferência acusando, por `302`. Pela sua própria régua da DE-032, o requisito 2 continua recortado pelo mecanismo (`natureza`, `tipo`) e não pelo efeito. Fica MÉDIA, e não alta, porque este projeto **decidiu** suportar natureza mista num grupo (retificadora) e eu não tenho como distinguir, por software, a retificadora legítima do erro de classificação. Essa é pergunta para o Fred.
3. **O vácuo da BL-254 sobrou no arquivo de `Conta`**, e já custou evidência: o teste que leva o nome do guard de escritório é satisfeito pelo formulário, não pelo modelo — o mutante M2 não o mata, e a mensagem do `clean()` não aparece no corpo da resposta. A defesa está provada em outro lugar; o teste, não mede o que o nome dele diz.
4. Quatro itens de baixa gravidade: o `DoesNotExist` não tratado que **esta rodada introduziu** (P4, uma linha para corrigir, com o padrão que o próprio método já usa), os nomes de coluna literais (P5 — respondendo à sua pergunta: **é frágil, e o cenário que o quebra é um `db_column` numa dessas duas FKs**, com falha alta e não silenciosa), a escolha do 400 (P7, defensável, com consequência prática nomeada) e o `estado.md` desatualizado em relação à revisão que você mandou auditar, com o item do PR que afirma o contrário marcado (A2).

**Encaminhamentos:** P1 (decisão + pergunta ao Fred), P2 (classificação da varredura), A2, A4 → `arquiteto-senior`. P2 (implementação), P3, P4, P5, A1 → `desenvolvedor-pleno`. P6 e P7 → backlog nomeado, sem rodada. Se eu tivesse de escolher um para tratar primeiro, seria o **A1**: enquanto um teste de recusa por requisição não asserta **qual camada** recusou, a próxima medição daquela superfície não vale como evidência — é a mesma lição da BL-254, uma casa mais funda.

**Arquivos relevantes:** `/home/user/DataLedger/apps/contabilidade/models.py`, `/home/user/DataLedger/apps/contabilidade/admin.py`, `/home/user/DataLedger/apps/empresas/services.py`, `/home/user/DataLedger/apps/empresas/models.py`, `/home/user/DataLedger/apps/empresas/admin.py`, `/home/user/DataLedger/apps/empresas/migrations/0005_desempate_no_ordering_do_regime_tributario.py`, `/home/user/DataLedger/apps/core/restricoes.py`, `/home/user/DataLedger/apps/core/tests/test_dl023_varredura_admin.py`, `/home/user/DataLedger/docs/agents/estado.md`.

---

## Notas do `arquiteto-senior`, escritas DEPOIS do relatório

**Parecer aceito integralmente. Nenhum achado contestado, nenhuma ressalva
suavizada.** Os dois altos da rodada 1 estão fechados por medição do auditor, e
o que sobra são quatro coisas de natureza diferente — que é o motivo de eu não
tratar todas do mesmo jeito.

**1. Uma pergunta CONTÁBIL, que vai para o Fred, e não para o código (P1).**
Reparentar uma conta movimentada para um grupo de natureza oposta produz
exatamente o efeito que a BL-83 nomeia. Mas o auditor parou onde devia:
**retificadora é isso**, e ele não tem como distinguir por software a
retificadora legítima do erro de classificação. Quem distingue é o contador.
Registro como **BL-261**, com as duas saídas que ele propôs, e **pergunto ao
Fred** antes de escolher. Não recuso às cegas: engessar o plano de contas para
evitar um erro que pode ser a operação certa seria trocar um defeito por outro.

**2. Uma classe maior que a etapa, que vira etapa própria (P2).** "O admin não
tem isolamento por escritório em **nenhuma** superfície" é verdade, é maior que
a BL-248 e nunca foi escopo da DL-023. O `/admin/autocomplete/` é a prova de
que tapar porta a porta não resolve: ele é servido pelo `get_queryset` do admin
de **destino**, então nenhuma camada no admin de origem o alcança. Registro como
**BL-262** e **corrijo a classificação da varredura** — a decisão "defendida"
para `contabilidade.Conta` passa a nomear o que fica de fora, porque foi
exatamente essa omissão que tornou a BL-248 achado.

**3. Três correções baratas, que entram numa rodada curta agora (P4, P5, A1).**
O `DoesNotExist` foi **introduzido por esta rodada** e a correção é uma linha,
com o padrão que o próprio método já usa; os nomes de coluna literais são duas
linhas; e o teste que mede a camada errada é o item que o próprio auditor
trataria primeiro — com razão, porque teste que não sabe **quem** recusou não
serve de evidência na próxima medição.

**4. O resto vira backlog nomeado, sem rodada:** P3 (depende da decisão da
BL-262), P6 e P7. E a discordância de prioridade que o auditor declarou sobre a
**BL-243** — decidir o alcance do SQLite — eu acolho: o custo é uma frase, e
hoje o ambiente de desenvolvimento devolve 500 na corrida que acabamos de
corrigir. Vai ao Fred junto com a pergunta contábil.

**Sobre o A2, que é contra mim outra vez:** eu marquei no PR a caixa "atualizei o
estado" e mandei auditar uma revisão cujo `estado.md` ainda descrevia a anterior.
O mecanismo aprovou a **declaração**, não o fato — terceira ocorrência da
família da BL-147. Corrigido neste mesmo commit, e registrado como **BL-263** em
vez de tratado como descuido isolado: a caixa é verificável por máquina, e
enquanto não for, é palavra minha.
