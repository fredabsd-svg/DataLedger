# Auditoria independente — DL-019, rodada 4 (reauditoria de `b2f6112` / `2d1bc37`)

## 1. Parecer

**REPROVADO.**

E preciso ser muito claro sobre **por quê**, porque a razão da reprovação **não
é a varredura**, e porque a minha recomendação sobre encerramento (§9) é
favorável ao encerramento do trabalho técnico.

Um motivo, e é suficiente: **achado D4, gravidade ALTA**. A `origin/main`
recebeu, em 2026-09-15 às 11:32Z (PR #20), **uma outra demanda também chamada
DL-019**, que gravou **os mesmos três nomes de arquivo** em `docs/auditorias/`
com conteúdo diferente e criou **BL-170 a BL-194 com significados diferentes**
dos BL-170 a BL-189 desta branch. Os três relatórios desta etapa — os meus —
estão em rota de colisão com três arquivos homônimos já na `main`, e os
identificadores `BL-xxx` citados **dentro do código de teste** como
justificativa de cada defesa passaram a ser ambíguos. Isso é trilha de auditoria
e rastreabilidade, que é matéria da minha alçada, e `ALTA` reprova.

Sobre o que esta rodada foi de fato auditar, registro com a mesma clareza:
**C1, C2, C3, C5 e C7 estão fechados**, cada um verificado por mim, C1 e C2 pelo
caminho da produção e com o efeito medido. **A correção é a melhor das quatro
rodadas**, e é a primeira em que a fuga que eu encontro **não é a que eu havia
prescrito nem uma variante dela na mesma porta**.

---

## 2. Revisão auditada e estado da árvore

| | |
| --- | --- |
| `git rev-parse HEAD` | `b2f61128afc53b86f708c5528ad6594845c7528e` |
| commit de código | `2d1bc37f1267548890cc4c8caf17b9fd6ba755ab` |
| branch | `claude/accounting-agent-team-setup-mn6lyf` |
| `git status --porcelain` ao começar e ao terminar | **vazio nas duas pontas** |
| `git diff --stat` / `git diff --cached --stat` ao terminar | **vazios** |
| `git log 05c2a6d..b2f6112` | `2d1bc37` (código) e `b2f6112` (só `docs/`) |
| mutante aplicado na árvore auditada | **nenhum** |

O único mutante desta rodada (**M-D1**) rodou em **clone isolado** de `2d1bc37`,
com alvo afirmado por `grep` antes de eu ler qualquer resultado,
`ruff check` + `ruff format --check` + `manage.py check` + `reverse()`
conferidos **antes**, e reversão conferida por `sha256` contra os valores
anotados antes:

```
ANTES e DEPOIS, idênticos:
0da43c754518611fb99346bfb6ed4461088384a882962a5e67e5c4e49081186e  apps/contabilidade/views_web.py
fd76f9c2a056a7ce45926bd8e99413112f62a9d420f184e6b5aaf4010fa78071  apps/contabilidade/urls.py
```

### O meu relatório da rodada 3 está preservado — nesta branch

Verificação mais forte que a das rodadas anteriores: **comparei o blob, não só a
contagem de commits**.

```
sha256 de docs/auditorias/2026-09-15-dl-019-rodada-3.md
  em e0ece34 : 160cf837cee2741e2511085e2d062eb6a5b8ea6905afce445deea57b7da2cf6a
  em 2d1bc37 : 160cf837cee2741e2511085e2d062eb6a5b8ea6905afce445deea57b7da2cf6a
  em b2f6112 : 160cf837cee2741e2511085e2d062eb6a5b8ea6905afce445deea57b7da2cf6a
git diff e0ece34 b2f6112 -- (o arquivo)  ->  vazio
```

**Nada foi editado, suavizado ou reordenado nesta branch.** As "Notas do
`arquiteto-senior`" estão depois do meu texto, na mesma adição. O mesmo limite
honesto de sempre: eu verifico que o arquivo **não mudou depois de gravado**,
não uma comparação byte a byte com o que saiu de mim.

⚠️ **Mas existe, na `origin/main`, um arquivo com esse mesmo caminho e conteúdo
inteiramente diferente** — `sha256 3ba23fb9…`, 370 linhas, intitulado *"Auditoria
DL-019 — rodada 3 — REPROVADO (e a etapa encerra assim mesmo)"*, sobre uma
revisão `2276d0a` que não existe nesta branch. Ver **D4**.

### Números, e em que árvore cada um foi medido

| Onde | Comando | Resultado |
| --- | --- | --- |
| **Cópia limpa de `2d1bc37`** (clone novo; `git status` vazio; **zero** arquivos ignorados) | `pytest -q` | **1105 passed, exit 0**, 38,70 s, com a linha da BL-171 |
| idem | `ruff check .` | All checks passed! |
| idem | `ruff format --check .` | 152 files already formatted |
| idem | `manage.py check` / `makemigrations --check` | 0 issues / "No changes detected" |
| **CI de `2d1bc37`** (lida por mim, `check-runs` + log do job `104395081355`) | — | `Lint e testes = success`, `Validar documentação = success`, **1103 passed, 2 skipped em 62,68 s**, linha da BL-171 presente |
| **CI de `b2f6112`** (lida por mim, job `104395896107`) | — | `Lint e testes = success`, `Validar documentação = success`, **1103 passed, 2 skipped em 56,71 s** |

**O relato do `arquiteto-senior` bate exatamente com o que eu medi**, inclusive
os 62,68 s. Ele declarou a CI de `2d1bc37`; eu li também a de `b2f6112`, que ele
não declarou, e ela também está verde. `pwsh` continua ausente —
`scripts/validate-docs.ps1` segue **bloqueado localmente**, mas o job "Validar
documentação" das duas revisões está `success` e eu li isso. A diferença
1105 × 1103+2 continua sendo integralmente as duas medições de CSS (BL-182).

### A quebra de regra declarada (`ruff format` sem `--check`) — verificação **suficiente, e eu a generalizei**

O `arquiteto-senior` verificou **um arquivo** em `e0ece34`. Eu verifiquei a
**árvore inteira**, que é a verificação que a alegação exige:

```
clone limpo em e0ece34:
  ruff format --check .  ->  152 files already formatted   (exit 0)
  ruff check .           ->  All checks passed!            (exit 0)
git show --stat 2d1bc37  ->  4 arquivos .py, todos os que o implementador escrevia
```

Como **toda** a árvore já estava formatada em `e0ece34`, `ruff format` sem
`--check` **não tinha o que reescrever em lugar nenhum** — não só naquele
arquivo. E o commit não toca nenhum `.py` fora dos quatro do próprio trabalho.
**Dano provadamente nulo.** A verificação dele estava certa e era mais estreita
do que precisava; esta fecha a questão. O registro deve ficar, e ele registrou
em vez de esconder — isso conta a favor.

---

## 3. Cada achado da rodada 3, com a medição que o sustenta

| Achado | Veredito | Medição |
| --- | --- | --- |
| **C1** (alta) | **FECHADO** — e a generalização funciona | mutante M-C1 reconstruído; sonda D (§3.2) |
| **C2** (média) | **FECHADO** — pelo caminho da produção | sonda G (§3.3) |
| **C3** (baixa) | **FECHADO** de ponta a ponta | leitura + prova do teste dedicado |
| **C4** (média, processo) | **FECHADO** — texto corresponde ao que existe | `grep` no backlog |
| **C5** (baixa) | **FECHADO** — e preso a fato, não a palpite | `pyproject.toml` lido |
| **C6** (baixa) | **FECHADO** — A9 é a BL-188, com dono e critério | backlog |
| **C7** (baixa) | **FECHADO** — com teste que mede o silêncio | leitura do teste novo |

### 3.1 C1 — fechado, e o teste de precedência é bom

`AlvoAlcancavel` passou a carregar `initkwargs_das_rotas` em campo **obrigatório
sem padrão** (`test_dl019_varredura_de_contratos.py:624-642`), e
`metodos_permitidos_pelo_despacho` (`:801-827`) resolve
`initkwargs.get("http_method_names", classe.http_method_names)`, unindo entre
rotas e somando o valor da classe. Conferi as três propriedades:

- **Descoberta** (`:2094`): o mutante M-C1 está reconstruído com `path()` real e
  o `initkwargs` vindo de `as_view()` de verdade — não passado à mão, que é o
  erro que deixou a BL-174 se esconder por uma rodada.
- **União entre rotas** (`:2135`): nas **duas ordens**, restringir não apaga
  ampliar.
- **Par negativo** (`:2163`): sem `initkwargs`, quem responde é a classe.

**O teste de precedência (`:2061`) tem furo? Não encontrei nenhum, e ele é o
molde certo.** Ele faz o que nenhuma das três rodadas anteriores tinha: os dois
valores existem (`["get"]` na classe, `["get","post"]` na rota), **divergem**, e
o teste mede **qual vence por comportamento** — 405 pela estreita, 201 pela
ampliada, mesma classe, mesmo corpo — e ainda afirma que **a classe continua
`["get"]` depois de tudo**, que é a metade que explica *por que* ler a classe
nunca respondia à pergunta. Isso é o padrão que eu quero ver exigido, e
subscrevo a exigência.

O único limite que registro, e é limite e não furo: ele mede o comportamento
chamando `as_view()` diretamente com `APIRequestFactory`, e a descoberta por
`path()` está no teste vizinho. Os dois juntos cobrem a cadeia; separados,
nenhum cobre.

### 3.2 A generalização — **atacada, e ela segura o eixo da rota**

Medi as quatro formas que o `arquiteto-senior` nomeou, mais duas minhas. **A
generalização é real e vai além do que eu tinha prescrito:**

| Forma | Resultado medido |
| --- | --- |
| `as_view(post=outra_funcao)` | **COBERTA** — `nao_classificadas`, nomeando `'post'` |
| **`as_view` próprio da classe injetando `http_method_names`** | **COBERTA** — superfície aparece e é acusada |
| Fábrica de views (classe criada em função, molde da DL-010) | **COBERTA** — acusada |
| Handler sem fonte legível, roteado por `path()` real | **COBERTA** (é o C2) |
| `dispatch()` sobrescrito | **FUGA — achado D1** |
| `setup()` ampliando `http_method_names` na instância | **FUGA — achado D1** |
| `http_method_not_allowed` sobrescrito | **FUGA — achado D1** |

O caso do `as_view` próprio é o que mais me interessa a favor da correção: ele
**não** estava na minha prescrição, e passa porque a varredura lê os
`initkwargs` **do callback**, não a assinatura da chamada. Isso é generalidade
de verdade, não cobertura de lista.

**A lista de chaves "inócuas" está certa? Sim — medido, não lido.** Exercitei um
`ViewSet` real com `as_view({"post":"create","get":"list"}, **{chave: valor})`
para cada uma das cinco:

```
sem extras   -> {'acao': 'create'}
suffix       -> 201  {'acao': 'create'}
basename     -> 201  {'acao': 'create'}
detail       -> 201  {'acao': 'create'}
name         -> 201  {'acao': 'create'}
description  -> 201  {'acao': 'create'}
```

Nenhuma muda qual handler responde. **A lista está correta hoje.** O que falta é
o mecanismo que a mantém correta — ver **D2**.

### 3.3 C2 — fechado, e provado pelo caminho da produção

`superficies_de_escrita_sem_politica` (`:1016-1035`) trocou o `continue` por
entrada em `faltando`, com motivo escrito. Medido por mim com um handler cujo
código veio de `compile()`/`exec` e que foi **roteado por `path()` real**:

```
G) handler sem fonte (C2)
   SUPERFICIES : ['apps.ficticio.views_web.SemFonte.post']
   ACUSADAS    : ['apps.ficticio.views_web.SemFonte.post']
   POST real   : 201
```

Antes aprovava em silêncio; agora acusa. O `pragma: no cover` saiu junto. O ramo
gêmeo em `_resolvedor_de_indirecao` (`:749`) continua com `return None`, e ali
está **certo**: devolver `None` faz a indireção não resolver, e a superfície é
**acusada**, que é o lado estrito.

### 3.4 C3, C5, C6, C7 — fechados

- **C3.** `Superficie.nome_efetivo` passou a carregar o método nas três formas
  (`:864-968`), e o registro guarda o método da requisição
  (`conftest.py:157-209`). A invariante *`nome_efetivo` é a própria chave* eu
  **medi nas 14 superfícies reais**: coincide em todas as 14.
  `test_a_conferencia_reprova_o_put_quando_so_o_post_foi_exercitado`
  (`test_dl019_elo_de_execucao.py:363`) faz as duas metades entrarem pelo
  caminho de produção e tem par positivo e negativo. É exatamente o teste que eu
  pedi.
- **C5.** `e_codigo_de_teste` (`conftest.py:126-154`) casa `test_` **só no
  último segmento**, e `apps.test_utils.views` é **produção**, por nome, no teste
  parametrizado. E `apps.fiscal.test_helpers` é teste **porque o
  `python_files = ["test_*.py"]` do `pyproject.toml` o coleta** — fato **lido do
  arquivo** por `test_a_fronteira_entre_teste_e_producao_segue_o_python_files_do_pytest`,
  que reprova se o `pyproject.toml` mudar. Isso é melhor do que eu pedi: eu
  pediria a lista; ele prendeu a fonte.
- **C6.** A9 é a **BL-188**, com dono (`arquiteto-senior`), classe e critério de
  aceite que diz *"ou uma rodada de mutantes dirigida às telas reproduz os dois
  sobreviventes, ou o item é fechado **por decisão escrita**"*. Fecha o achado.
- **C7.** A seção *"O que esta fixture CUSTA"*
  (`test_dl019_estaticos_da_suite.py:52`) declara que a suíte **deixou de
  validar referência de `{% static %}`**, e
  `test_a_suite_deixou_de_validar_referencia_de_static_e_isso_esta_declarado`
  (`:144`) **mede o silêncio** — e reprova se alguém devolver o manifesto à
  suíte sem rever a seção. É a forma certa: a declaração deixou de ser prosa.

### 3.5 C4 — fechado, e o texto agora diz o que é

`docs/projeto/backlog.md:350`, BL-180, passou a dizer **"CUMPRIDA UMA VEZ em
2026-09-15 — ATO, NÃO MECANISMO"**, com a correção creditada ao achado e a
frase *"Enquanto este item não fechar, a BL-180 é ato, não mecanismo"*. O
mecanismo virou **BL-189**, com desenho concreto. O `grep` que eu usei na rodada
3 continua devolvendo nada — e agora o texto **não afirma** que devolveria algo.

---

## 4. As 14 superfícies e os 43 pontos — o que a correção pode ter quebrado

Medido sobre o urlconf real, em `2d1bc37`:

```
ALVOS: 28    SUPERFICIES: 14    NAO CLASSIFICADAS: {}    ACUSADAS: {}
colisoes de chave: {}    initkwargs por alvo (nao vazios): {}
nome_efetivo == chave nas 14: True em todas
```

As 14 são exatamente as de `SUPERFICIES_DE_ESCRITA_CONHECIDAS`, agora com o
método no nome. **Nenhuma superfície real passou a ser vista errado**, nenhuma
sumiu, nenhuma nasceu falsa, e a conferência da BL-171 fecha verde na cópia
limpa e na CI. A união do valor da classe com o das rotas em
`metodos_permitidos_pelo_despacho` erra para o lado **estrito**, e o par
negativo (`:2163`) impede que "passou a ler a rota" virasse "deixou de ler a
classe".

Sobre os **43 pontos**: ver **D3**. Eles não medem o que a frase sugere.

---

## 5. Achados novos

### D1 — MÉDIA — O despacho pode ser trocado **sem passar por `initkwargs`**, e a seção que existe para declarar o que não se cobre não diz isso

**Classe, pelo efeito proibido:** *nenhuma superfície de escrita passa a existir
sem que algum dado enviado na requisição seja lido ou recusado.* É a classe do
B1, num eixo novo.

**Requisito afetado:** BL-149 (R6-2), BL-174 (B1), BL-183 (C1), DE-034 item 2,
AGENTS.md §8 e §11.

**Arquivo e localização.**

- `apps/core/tests/test_dl019_varredura_de_contratos.py:830-843`
  (`_handlers_por_http_method_names`) e `:801-827`
  (`metodos_permitidos_pelo_despacho`) — modelam o despacho como *tabela de
  handlers × `http_method_names`*, que é verdade **enquanto ninguém substituir o
  despacho**;
- `:126-132` — a afirmação que diz mais do que a defesa entrega: *"os handlers
  que a classe de fato tem, **cruzados com os métodos que o DESPACHO
  permite**"*;
- `:170-227` — a seção **"O que ela NÃO cobre"**, que declara middleware, admin,
  ORM, view não roteada, escrita por método de leitura e roteador próprio — e
  **não** declara isto.

**Reprodução medida.** Quatro sondas em processo, pelo caminho da produção
(`_percorrer_callbacks` sobre `path()` real → `_alvos_de` →
`superficies_de_escrita`), com as classes em módulo `apps.`:

```
A) dispatch() sobrescrito        SUPERFICIES [] NAO_CLASSIF [] ACUSADAS []  POST real 201
B) setup() amplia http_method_names  SUPERFICIES [] NAO_CLASSIF [] ACUSADAS []  POST real 201
C) http_method_not_allowed grava     SUPERFICIES [] NAO_CLASSIF [] ACUSADAS []  POST real 201
E) django.views.View + dispatch (3 linhas)  SUPERFICIES [] ACUSADAS []  POST real 201
```

**Mutante M-D1, em clone isolado de `2d1bc37`.** `MutanteDespachoView(View)` em
`apps/contabilidade/views_web.py`, com `http_method_names = ["get"]`, um
`salvar()` que cria `Conta` a partir de `request.POST` sem a política, e três
linhas de `dispatch`:

```python
def dispatch(self, request, *args, **kwargs):
    if request.method == "POST":
        return self.salvar(request, *args, **kwargs)
    return super().dispatch(request, *args, **kwargs)
```

Bem-formado, conferido **antes** de eu ler qualquer resultado:

```
ruff check .          -> All checks passed!
ruff format --check . -> 152 files already formatted
manage.py check       -> System check identified no issues (0 silenced).
reverse(...)          -> /contabilidade/empresas/1/mutante-despacho/
```

O que a varredura vê:

```
O alvo do mutante esta em _alvos_alcancaveis? True
TOTAL de superficies: 14
com Mutante: []
nao classificadas: {}
ACUSADAS: {}
```

**A assinatura exata do B1 e do C1: `TOTAL 14, ACUSADAS {}`.** A rota é
descoberta; ela produz **zero** superfícies.

Que grava, por HTTP real, **inclusive um campo não contratado**:

```
STATUS: 201
CONTAS antes: 0 depois: 1
GRAVOU? True        (com "campo_nao_contratado": "passou" no corpo, aceito em silêncio)
```

E a suíte inteira, com o mutante aplicado:

```
1105 passed in 36.43s        PYTEST_EXIT=0
BL-171: toda superfície de escrita da varredura foi exercitada nesta sessão (43 pontos ...).
```

Revertido; `sha256` conferido contra os valores anotados antes.

**Impacto.** Uma rota que grava no banco nasce sem contrato, com lint verde,
suíte verde e a própria conferência da BL-171 anunciando que cobre tudo.
`colisoes_de_chave` e `test_toda_rota_fora_dos_apps_e_de_terceiro_conhecido`
também ficam mudos. Nenhuma defesa do arquivo dispara.

**Gravidade: MÉDIA, e o raciocínio é meu e explícito, porque é ele que decide o
parecer técnico.** O efeito é idêntico ao do B1 (bloqueador) e ao do C1 (alta).
Três coisas me fazem ficar abaixo de ALTA, e nenhuma delas é conveniência:

1. **Não há instância viva, nem começo de uma.** `grep` por `def dispatch`,
   `def setup`, `def http_method_not_allowed` e `method_decorator` em todo
   `apps/` e `config/` fora de testes devolve **nada**. O produto tem duas
   classes de view e nenhuma sobrescreve despacho.
2. **A fuga exige que o autor conduza a escrita *para fora* da tabela de
   handlers de propósito.** No C1 bastava usar um parâmetro documentado do
   Django cuja finalidade é justamente mudar o despacho; aqui é preciso escrever
   o desvio à mão. O jeito natural de escrever o importador da DL-010 —
   `@action(methods=["post"])` ou um `post` — está **coberto**, e eu medi as
   duas coisas nesta rodada.
3. **O arquivo já aceita, por escrito e como fronteira declarada, um ponto cego
   de forma idêntica**: *"Escrita por método HTTP de LEITURA"* (`:197-203`), uma
   view `@require_safe` que gravasse em GET — também alcançável, também
   invisível, também sem instância viva, classificada **BAIXA** (BL-181/B8). A
   diferença entre aquela fronteira aceita e esta **é só a declaração**, e é a
   declaração que falta.

Declaro que um leitor razoável pode graduar D1 como **ALTA** apenas pelo efeito,
e que, se o `arquiteto-senior` ou o Fred graduarem assim, o parecer técnico
muda. É julgamento, e eu o entrego aberto.

**Correção recomendada.**

1. **O mais barato e o mais honesto:** uma entrada nova em *"O que ela NÃO
   cobre"*, dizendo que a varredura modela o despacho como *tabela de handlers ×
   `http_method_names` (da classe e da rota)* e que **substituir o despacho**
   (`dispatch`, `setup`, `http_method_not_allowed`, ou `as_view` de classe
   própria que não passe por `initkwargs`) sai do modelo — com o efeito medido
   escrito ali. E **ajustar a frase da linha 126-132** para descrever o que o
   código entrega.
2. **O mecanismo, se o Fred quiser mais que declaração:** um teste que varra os
   alvos alcançáveis e **reprove nomeando** toda classe de `apps.` que
   sobrescreva `dispatch`, `setup` ou `http_method_not_allowed` sem estar numa
   lista de exceções declarada — no molde exato de
   `INITKWARGS_SEM_EFEITO_NO_DESPACHO`. Hoje a lista nasceria **vazia**, que é o
   estado mais forte possível, e faz a primeira ocorrência ser decisão de
   alguém. Isso é barato e fecha o eixo inteiro, não a forma.

**Caso de teste, para o responsável implementar** (eu não implemento):

> `test_a_varredura_reprova_classe_que_substitui_o_despacho`: classe `APIView`
> ou `View` com `http_method_names = ["get"]`, um `salvar()` que grava a partir
> do corpo, e `dispatch()` desviando o POST para ele; roteada por `path()` real;
> **par de precedência**: o POST responde **201** (o `dispatch` da classe vence a
> tabela de handlers) enquanto `superficies_de_escrita` devolve `{}` — e o teste
> exige que esse caso entre em `nao_classificadas`, nomeando a classe. Par
> negativo obrigatório: a mesma classe com `dispatch` apenas chamando
> `super().dispatch(...)` **não** é acusada, senão a defesa reprova todo
> `dispatch` legítimo.

**Como verificar a correção.** Reaplicar M-D1 exatamente:
`test_toda_superficie_de_escrita_aplica_a_politica_dos_cinco_dicionarios` (ou o
teste novo) tem de reprovar **nomeando
`apps.contabilidade.views_web.MutanteDespachoView`**.

**Responsável sugerido:** `desenvolvedor-pleno`.

---

### D2 — BAIXA — A resposta à pergunta 2: a leitura estática que **ainda não tem** o molde de precedência é a lista das chaves inócuas

**Classe:** *nenhuma leitura estática de comportamento de framework vale sem
teste de precedência.* É a regra que o próprio `arquiteto-senior` acaba de tornar
permanente, aplicada ao ponto do arquivo que ela ainda não alcançou.

**Arquivo:** `test_dl019_varredura_de_contratos.py:304-313`
(`INITKWARGS_SEM_EFEITO_NO_DESPACHO`) e `:2209`
(`test_todo_initkwargs_que_o_roteador_do_drf_passa_e_conhecido`).

**O fato.** Este é **o único ponto da generalização que erra para o lado
permissivo**: uma chave nesta lista passa **em silêncio**. A afirmação que a
autoriza — *"Nenhum deles altera qual método HTTP chega a qual handler"* — é
**prosa**. O teste que existe mede **quais chaves o roteador passa**, não que
elas sejam inócuas. É exatamente a forma do C1: um fato do framework sustentando
uma decisão de varredura, sem medição.

**Eu medi, e a lista está certa hoje** (§3.2): as cinco chaves não mudam o
handler que responde. Por isso é **BAIXA** e não mais: não há erro, há ausência
de mecanismo, e a lista é curta e estável.

**Correção recomendada.** Parametrizar sobre as cinco chaves: para cada uma,
`as_view(actions, **{chave: valor})` responde **o mesmo status e a mesma ação**
que sem ela. Se o DRF passar a dar efeito de despacho a qualquer uma, isso
reprova nomeado em vez de a chave continuar passando em silêncio. Duas linhas de
teste por chave, e a lista deixa de ser a única afirmação não medida do arquivo.

**Como verificar.** Mutar uma das cinco para um valor que altere o despacho, ou
simular o DRF dando efeito a `detail` — o teste tem de reprovar.

**Responsável sugerido:** `desenvolvedor-pleno`.

---

### D3 — BAIXA — "43 pontos de produção chegaram à política" não mede pontos de produção, e o número **triplicou sem a cobertura mudar**

**Classe:** *nenhum número declarado mede coisa diferente do que a frase que o
acompanha diz.*

**Arquivo:** `conftest.py:423-424` — a mensagem usa
`len(CHAMADORES_DA_POLITICA)`.

**O fato, medido pelos dois lados.** `CHAMADORES_DA_POLITICA` é o conjunto de
**chaves de registro**, e o C3 passou a registrar cada quadro **duas vezes** (com
e sem o método HTTP), além do nome efetivo da classe da instância —
`test_o_registro_guarda_o_metodo_http_da_requisicao_em_curso` mostra **três**
nomes para **uma** chamada. Resultado:

| revisão | a mensagem diz | superfícies reais |
| --- | --- | --- |
| `05c2a6d` | **18** pontos | 14 |
| `2d1bc37` | **43** pontos | **14** |

A cobertura **não mudou**: são as mesmas 14 superfícies, todas exercitadas nas
duas revisões. O número que aparece **no log da CI** e que a narrativa usa como
evidência inflou 139% por mudança de chaveamento interno.

**Impacto.** Baixo em si, e da família exata que esta etapa existe para matar: um
número que se lê como medida de cobertura e mede outra coisa. A próxima pessoa
que comparar "18 → 43" entre dois relatórios vai concluir que a cobertura
cresceu.

**Correção recomendada.** A mensagem passa a dizer o que é verdade e o que
interessa: *"as N superfícies de escrita da varredura foram exercitadas"*, com
`N = len(superficies)` — que é 14 e que **muda quando a cobertura muda**. Se o
tamanho do registro interessar, ele vai entre parênteses com o nome certo
("chaves de registro").

**Como verificar.** Acrescentar uma superfície de escrita: o número tem de ir de
14 para 15. Hoje ele se move por motivo errado.

**Responsável sugerido:** `desenvolvedor-pleno`.

---

### D4 — ALTA — Existem **duas demandas DL-019** e elas colidem em `docs/auditorias/`, no `backlog.md` e nos `BL-xxx` citados dentro do código

**Classe:** *nenhum relatório de auditoria e nenhum identificador de item pode
ser destruído ou tornado ambíguo por uma integração.*

**Requisito afetado:** CLAUDE.md, *"os relatórios são registrados em
`docs/auditorias/` pelo `arquiteto-senior`, que deve preservar integralmente os
achados"*; AGENTS.md (rastreabilidade); `docs/planos/DL-019-…:130-134`, que
declara `main` como branch de destino.

**O fato, medido.**

```
PR #19  claude/accounting-agent-team-setup-mn6lyf -> main  merged 2026-09-15T01:45:34Z  (DL-017)
        => cria 60cbcff, de onde ESTA branch reiniciou

PR #20  claude/multi-model-ia-structure-k1um7i   -> main  merged 2026-09-15T11:32:45Z
        titulo: "DL-019: papéis de agente com fonte única, gerados para Claude Code e Codex"
        => 7e9dc56, pai unico 60cbcff

git merge-base --is-ancestor 7e9dc56 b2f6112  ->  NAO   (a branch nunca foi rebaseada)
```

As duas linhas partiram **do mesmo commit** e escreveram nos mesmos lugares:

| Caminho | nesta branch (`b2f6112`) | na `origin/main` (`7e9dc56`) |
| --- | --- | --- |
| `docs/planos/DL-019-*.md` | `DL-019-consolidacao-pos-auditoria.md` | `DL-019-portabilidade-entre-ferramentas-de-ia.md` |
| `docs/auditorias/2026-09-15-dl-019-rodada-1.md` | o meu | outro, conteúdo diferente |
| `docs/auditorias/2026-09-15-dl-019-rodada-2.md` | o meu | outro, conteúdo diferente |
| `docs/auditorias/2026-09-15-dl-019-rodada-3.md` | o meu, `sha256 160cf837…`, 806 linhas | outro, `sha256 3ba23fb9…`, 370 linhas |
| `BL-170` | "A1: a varredura parte do urlconf" | "Guarda de sincronia não detecta CRLF" |
| `BL-183` | "C1: `http_method_names` ampliado pela rota" | "A10: RC-81 enumerava quatro ferramentas" |
| `BL-186` | "C5: fronteira teste/produção" | "R3-1: o marcador perdia parágrafo" |
| `BL-188` | "A9 ganha dono" | "R3-7: o rodapé mandava rodar `--escrever`" |
| `BL-189` | "o mecanismo da CI" | "R3-8: quinta ocorrência sobre o Gemini" |

`git diff b2f6112 7e9dc56 -- docs/auditorias/` : **1795 deleções, 736
inserções** nos três arquivos.

**Impacto.**

1. **Trilha de auditoria.** Na integração, os três relatórios entram em conflito
   *add/add*. Quem resolver terá de **escolher um lado**, e o lado não escolhido
   deixa de existir. São três relatórios de auditoria independentes de cada
   lado — exatamente os documentos que o projeto manda preservar na íntegra.
2. **Rastreabilidade do código.** O código de teste desta etapa cita `BL-171`,
   `BL-174`, `BL-176/B3`, `BL-179/B6`, `BL-181/B7`, `BL-183/C1`, `BL-185/C3`,
   `BL-186/C5`, `BL-187/C7` **como justificativa escrita de cada defesa**, em
   dezenas de pontos. Depois da integração, esses números apontam para itens de
   outra demanda. **Essa metade é silenciosa** — o Git não avisa.
3. **O identificador "DL-019" deixa de identificar uma demanda**: dois planos, e
   os dois sobrevivem à integração porque têm nomes de arquivo diferentes.

**Gravidade: ALTA.** Não é bloqueador porque **nada foi perdido ainda** — não há
PR aberto e o dano é integralmente prospectivo, e porque a metade maior do dano
aparece como conflito ruidoso. É ALTA porque atinge a trilha de auditoria e os
identificadores, com certeza de ocorrência se a integração for feita como está,
e com uma metade que **não** é detectada por nada.

**Registro explícito do que eu NÃO estou afirmando:** não afirmo que alguém
editou, suprimiu ou substituiu relatório meu. Nesta branch os três estão
intactos, e eu verifiquei por `sha256`. Também não sei qual das duas linhas tem
precedência sobre o nome "DL-019" — isso é decisão do Fred, não minha.

**Correção recomendada** (é decisão do `arquiteto-senior`, e é barata **agora**;
deixa de ser depois de integrar):

1. Renumerar **uma** das duas demandas — esta é a que ainda não está na `main`,
   então o caminho de menor dano é renomear **esta** para o próximo `DL-xxx`
   livre, e com ela os três arquivos de `docs/auditorias/` (o conteúdo fica
   intacto; muda o nome).
2. Renumerar os itens **BL-170 a BL-189 desta branch** para uma faixa livre
   acima da que a `main` já ocupa (`BL-194`), **atualizando as citações dentro do
   código de teste no mesmo passo** — são elas que ninguém vê quebrar.
3. Rebasear a branch sobre `origin/main` **antes** de abrir o PR, para que o
   conflito apareça agora, com quem sabe o que cada arquivo é, e não na hora do
   merge.
4. Se algum dos três relatórios da outra linha também for meu ou de outro
   auditor, **os dois conjuntos ficam**, com nomes distintos. Nenhum relatório de
   auditoria é resolvido por descarte.

**Como verificar a correção.** `git ls-tree origin/main docs/auditorias/` e
`git ls-tree <branch> docs/auditorias/` não têm nenhum nome em comum que não seja
byte a byte idêntico; `git merge-tree` (ou o rebase) sobre `origin/main` não
produz conflito em `docs/auditorias/`; e `grep -rn "BL-1[78][0-9]"` nos `.py`
bate com o `backlog.md` resultante.

**Responsável sugerido:** `arquiteto-senior`, e a decisão sobre o nome é do Fred.

---

## 6. Medido × inspecionado

**Medido** (comando rodado, saída lida por mim): a suíte em cópia limpa de
`2d1bc37` — **1105 passed, exit 0**, zero arquivos ignorados — e a sequência
`ruff check` / `ruff format --check` / `manage.py check` / `makemigrations
--check` ali; os `check-runs` **e os logs de job** de `2d1bc37` e de `b2f6112`
por API, sem imprimir a credencial; `ruff check` e `ruff format --check` na
**árvore inteira** em `e0ece34`, que é a verificação da quebra de regra; o
mutante **M-D1** em clone isolado, com alvo afirmado antes, verificações de
boa-forma antes de ler resultado, suíte inteira verde com ele aplicado, gravação
real por HTTP (201, `Conta` criada, campo não contratado aceito) e reversão
conferida por `sha256`; sete sondas de fuga em processo pelo caminho da produção
(quatro cobertas, três fugindo); as cinco chaves "inócuas" exercitadas uma a uma
contra um `ViewSet` real; as 14 superfícies reais com `nome_efetivo`, colisões e
`initkwargs`; a integridade do meu relatório da rodada 3 por `sha256` do blob em
três commits; a colisão com `origin/main` por `git ls-tree`, `git diff`,
`merge-base` e a API de PRs.

**Inspecionado** (li, não executei teste dedicado): o texto da BL-180 corrigida,
da BL-188 e da BL-189; a BL-164 com A2 e A3, palavra por palavra, contra o que
eu registrei; a BL-182; o `estado.md`; a seção "O que ela NÃO cobre"; os testes
de C3, C5 e C7, cuja execução eu confirmei apenas como parte dos 1105.

---

## 7. O que eu **não** consegui verificar

1. **`scripts/validate-docs.ps1`.** `pwsh` ausente. **Bloqueado localmente** — o
   job "Validar documentação" está `success` nas duas revisões e eu li isso, o
   que é substituto parcial.
2. **BL-188** (a antiga A9), os dois sobreviventes originais. Continua aberta;
   fechá-la não é decisão minha, e agora ela tem dono e critério, que era o que
   faltava.
3. **BL-189** — o mecanismo da leitura da CI ainda não existe. Ele está
   corretamente declarado como não existente, o que era o achado C4.
4. **BL-164 (A2 e A3)** — não reexecutei as reproduções desta vez. O texto do
   item continua fiel ao que eu medi nas rodadas anteriores, e nada no diff toca
   o admin.
5. **BL-182** — as duas medições de CSS **continuam pulando na CI**, com o mesmo
   diagnóstico que eu li na rodada 3. O critério 3 da etapa segue atendido pelo
   ramo *"pula com motivo declarado"*, **nunca** pelo ramo *"roda"*.
6. **Concorrência real** na exclusão de regime, com duas conexões. Não medi, como
   nas três rodadas anteriores.
7. **Se D1 tem forma mais idiomática que eu não achei.** Testei sete; a
   gravidade que atribuí depende de eu ter julgado bem a raridade do idioma, e
   isso é julgamento, não medição.
8. ⚠️ **As regras contábeis em si. Auditoria de software não substitui a
   validação profissional das regras contábeis e legais.** **Matéria para o
   Fred:** a ambiguidade de regime tributário do **A2** — dois períodos abertos
   ao mesmo tempo, com o produto escolhendo um sem dizer qual — continua viva em
   `2d1bc37`, e **o que fazer com dado já gravado nesse estado é decisão
   contábil, não de software**. Segue valendo o alerta da BL-164 de que **A2 e A3
   não devem atravessar a DL-010**, porque o regime governa a apuração fiscal.

**Sobre isolamento, autorização, precisão monetária, idempotência e segredos:**
o diff `05c2a6d..b2f6112` toca **sete arquivos** — quatro de teste, o
`conftest.py` e três de `docs/`. Nenhum modelo, serializer, service, permissão,
`get_queryset`, migração ou cálculo monetário. `grep` confirma que nada em
`apps/`, `config/` ou `manage.py` importa o `conftest.py`. **Não há regressão
possível dessas áreas nesta rodada**; elas foram fechadas antes e não foram
tocadas. Não encontrei CPF, token, chave, certificado, `.env` nem nome de
cliente nos arquivos novos; a cópia limpa tem **zero** arquivos ignorados. Não
imprimi em momento nenhum o valor de `GH_TOKEN`/`GITHUB_TOKEN`.

---

## 8. O que a correção fez bem, e precisa ser preservado

Cada item é coisa que eu tentei quebrar e não consegui.

1. **A generalização é generalidade, não cobertura de lista.** O caso que me
   convenceu não estava na minha prescrição: uma classe com `as_view` **próprio**
   injetando `http_method_names` é vista corretamente, porque a varredura lê os
   `initkwargs` **do callback** e não a chamada. Isso é o eixo inteiro da rota
   fechado, e não a forma que eu nomeei.
2. **O teste de precedência é o molde certo, e mede as três coisas certas:** os
   dois valores existem, divergem, e **o comportamento** decide (405 × 201) — e
   ainda prova que **a classe continua intocada**, que é a metade que explica o
   defeito.
3. **`initkwargs` desconhecido reprova nomeado, e a acusação não retira
   exigência.** `superficies_de_escrita:911-924` acrescenta a acusação e
   **continua emitindo** as superfícies que consegue ver. Errar para o lado
   estrito sem abrir mão do que já sabia é a decisão difícil, e foi tomada certa.
4. **A lista das chaves inócuas é medida contra um `DefaultRouter` real**, e não
   presumida — se o DRF passar uma chave nova ela aparece **nomeada**, em vez de
   o `ViewSet` inteiro começar a reprovar sem explicação.
5. **C2 foi provado pelo caminho da produção**, com fábrica de views roteada por
   `path()` real — a lição da BL-174 aplicada por conta própria, sem eu pedir.
6. **C5 foi preso a um fato lido do `pyproject.toml`, não a um palpite.** Eu
   pediria a lista de casos; ele prendeu a **fonte** da regra. É melhor do que o
   que eu prescrevi, e é o molde da BL-172 num lugar novo.
7. **C3 tem par positivo e negativo e entra pelos dois caminhos de produção.**
   `test_a_conferencia_reprova_o_put_quando_so_o_post_foi_exercitado` é
   exatamente o teste que eu pedi, e o `test_o_registro_sem_metodo_legivel_nao_
   inventa_um` é o cuidado que eu **não** tinha pedido: sem método legível,
   reprova por falta em vez de chutar.
8. **C7 virou teste que mede o silêncio.** A declaração de fronteira deixou de
   ser prosa e passou a reprovar se alguém a tornar falsa.
9. **O achado do implementador que ninguém previu** — `as_view(post=outra_funcao)`
   aceito e respondendo 202 — foi **medido, preso como asserção sobre o Django e
   encaminhado para `nao_classificadas`**. Encontrar por conta própria uma forma
   que o auditor não nomeou, e fechá-la, é a primeira vez que isso acontece nesta
   etapa.
10. **A CI foi lida, e bate.** Segunda rodada seguida sem uma única divergência
    entre o declarado e o que a CI diz. E o `estado.md` diz **em que árvore** cada
    número foi medido.
11. **A quebra de regra foi declarada em vez de escondida**, e verificada. O
    comportamento certo depois de errar.

---

## 9. A pergunta de encerramento — a minha leitura, e ela é minha

O `arquiteto-senior` perguntou se a etapa está em condição de encerrar com itens
menores nomeados e abertos, no molde da **DE-038**, ou se existe algo que
**exige** outra rodada. Respondo às duas metades.

**Sobre a varredura — o mecanismo que produziu os três achados anteriores — a
minha recomendação é ENCERRAR, e não é resignação.** As razões:

1. **A curva é real e é a curva certa.** Bloqueador → bloqueador → ALTA → MÉDIA,
   e o eixo da fuga foi estreitando a cada rodada: de "não vê `ViewSet` nenhum",
   para "lê o lugar errado do DRF", para "lê a classe em vez da rota", para "não
   modela a substituição do despacho". A cada rodada o que sobra é mais estreito
   e mais deliberado de escrever.
2. **Esta rodada mudou de natureza.** Nas três anteriores, a correção fechava
   **a forma que eu tinha nomeado** e eu achava a forma vizinha. Desta vez a
   correção fechou **o eixo** — toda configuração de rota, inclusive formas que
   eu não nomeei — e o que sobrou está num eixo **diferente**, que é o interior
   da classe. Isso não é a quarta fuga da mesma trinca; é o fim daquela trinca.
3. **D1 não tem instância viva e não tem caminho natural para nascer.** O jeito
   canônico de escrever o importador da DL-010 está coberto, e eu medi isso nesta
   rodada, não deduzi.
4. **O que sobra é da família que o arquivo já aceita por escrito.** A escrita em
   método de leitura é o mesmo ponto cego, foi aceita como fronteira declarada em
   BAIXA, e ninguém achou isso desonesto — porque **estava escrito**. D1 vira a
   mesma coisa no instante em que for escrito, e um parágrafo resolve.
5. **O custo de outra rodada é conhecido e o retorno não.** Eu gastaria a rodada
   5 procurando a quinta forma num eixo ainda mais estreito, e a encontraria —
   sempre se encontra. O que ela **não** faria é tocar em regra contábil,
   cálculo, isolamento entre empresas ou permissão, que é onde o dano ao Fred
   mora. **Quatro rodadas, quatro achados no meu mecanismo de medição, zero no
   produto.** A DE-038 foi escrita para exatamente esta situação.

**Mas existe algo que exige outra passagem, e eu nomeio: é o D4, e ele não é
técnico.** Não se resolve auditando código; resolve-se renomeando a demanda,
renumerando os itens e rebaseando **antes** de abrir o PR. Se isso for feito,
não vejo motivo técnico para uma rodada 5 da varredura — vejo motivo para uma
**conferência curta** de que a renumeração não quebrou as citações dentro do
código, que é meia hora de trabalho e não uma auditoria.

**Por isso o parecer é REPROVADO e a recomendação é encerrar.** As duas coisas
são verdadeiras ao mesmo tempo, e eu prefiro dizer as duas a arredondar uma para
caber na outra. Reprovar aqui não é dizer "faça de novo"; é dizer **"não integre
assim"**. O que falta para eu aprovar é:

1. **D4 resolvido** — nome da demanda, nomes dos três relatórios, faixa de
   `BL-xxx` e rebase. Nenhum relatório de auditoria resolvido por descarte.
2. **D1 declarado** na seção "O que ela NÃO cobre", com o efeito medido escrito,
   e a frase da linha 126-132 corrigida. O mecanismo (a varredura de `dispatch`
   sobrescrito) pode virar item de backlog com dono, no molde da DE-038 — não
   exijo o mecanismo agora; exijo que a fronteira pare de ser silenciosa.

**D2 e D3 são itens de backlog com dono. Nenhum deles bloqueia.** E **A2 e A3
continuam não bloqueando a DL-019 e continuam não devendo atravessar a DL-010.**

A decisão é do Fred. Esta é a recomendação técnica, e o **encerrar não é
silenciar** vale nos dois sentidos: nomeei o que fica aberto, e nomeei também
que, na minha leitura, o que fica aberto não justifica uma quinta rodada de
auditoria do mesmo mecanismo.

---

*Auditoria de software não substitui a validação profissional das regras
contábeis e legais. Não afirmo ausência de defeitos, segurança absoluta nem
conformidade legal. O que está neste relatório é o que eu medi, separado do que
eu apenas li, e cada declaração de suíte verde diz em que árvore foi medida. A
árvore auditada, `/home/user/DataLedger`, está em `b2f6112` com `git status` e
`git diff` vazios — nenhum mutante foi aplicado nela.*

---

## Notas do `arquiteto-senior` — acrescentadas após o relatório, sem editá-lo

Relatório **preservado integralmente**.

### O D4 ele achou sozinho, e isso é a melhor notícia do relatório

Eu já sabia da colisão — descobri ao ensaiar a renumeração, e o Fred autorizou
resolvê-la. **Não contei ao auditor.** Ele a encontrou por conta própria, mediu
por `git ls-tree`, `merge-base` e pela API de PRs, e chegou **à mesma
recomendação** que eu havia levado ao Fred: renumerar **esta** branch, porque é a
que ainda não está na `main`, e **nenhum relatório de auditoria se resolve por
descarte**.

Duas cabeças independentes chegando à mesma conclusão a partir de evidências
diferentes é o que eu queria do processo, e é raro.

E ele viu uma metade que eu **não** tinha visto: os `BL-xxx` citados **dentro do
código de teste** como justificativa de cada defesa. *"Essa metade é silenciosa —
o Git não avisa."* O meu ensaio já renumerava o código junto, mas eu tinha
tratado isso como higiene; ele mostrou que é **rastreabilidade**, e que é a parte
que ninguém vê quebrar.

### Sobre a gravidade do D1, eu concordo com MÉDIA — e digo por que não me
### escondo atrás disso

Ele ofereceu a graduação ALTA a quem quisesse, e declarou que o parecer mudaria.
Eu fico em MÉDIA, pelas razões 1 e 2 dele, que são medidas e não opinativas: não
há instância viva, e o caminho canônico da DL-010 está coberto **por medição
desta rodada**. E aceito a condição dele: **a fronteira para de ser silenciosa**.
A declaração é barata e a omissão é que era o defeito.

### O que eu levo ao Fred

O parecer é REPROVADO **e** a recomendação é encerrar, e ele preferiu dizer as
duas coisas a arredondar uma. Eu levo as duas ao Fred exatamente assim, com a
frase dele que resume o que esta etapa custou e entregou:

> **"Quatro rodadas, quatro achados no meu mecanismo de medição, zero no
> produto."**
