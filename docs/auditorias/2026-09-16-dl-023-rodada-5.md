# Auditoria DL-023 — rodada 5 (focada), revisão `edae1bf`

> **Relatório do `auditor-qa` preservado integralmente.** Nenhuma frase editada,
> suavizada ou omitida pelo `arquiteto-senior`. As notas do arquiteto vêm
> **depois**, na última seção. Numeração: nesta etapa o número conta **rodadas
> de trabalho**, não auditorias — rodada 1 (auditoria, REPROVADA), 2 (correção),
> 3 (auditoria, aprovada com ressalvas), 4 (correção), 5 (esta auditoria,
> focada).
>
> **Esta auditoria foi FOCADA por decisão registrada do `arquiteto-senior`**, e
> o próprio auditor mediu o recorte e concordou com ele: a rodada 4 mexeu em
> dois arquivos, fechando itens de gravidade baixa e média, e refazer a
> varredura completa custaria mais que o dano possível (DE-038). O recorte foi
> oferecido para ser ampliado se a medição indicasse; não indicou.

Medições completas. Árvore intacta, bancos de auditoria removidos.

## 0. Integridade e ambiente

Árvore do repositório **intacta**: `git status --porcelain` vazio e `git diff --stat` vazio ao início e ao fim; `HEAD` = `edae1bfb3cc22ff4163abbca49641afc52d78468`. Toda mutação e toda sonda ocorreram em **três cópias fora do repositório** (`git archive edae1bf | tar -x`, 309 arquivos), em bancos PostgreSQL criados e depois removidos por mim (`dl5probe`, `dl5mut`). Conferi por `sha256sum` que `apps/contabilidade/models.py`, `apps/contabilidade/admin.py` e o arquivo de teste alterado estão byte a byte idênticos entre o repositório e a cópia limpa. Nunca rodei `ruff format` sem `--check`.

**Erro meu, declarado:** minha primeira execução da suíte usou `DEBUG=False`, o que desliga o `MD5PasswordHasher` de `config/settings.py:202` e fez a suíte levar 14 minutos de CPU sem terminar. Não é defeito do produto — é a proteção dupla da própria configuração funcionando. Refiz com `DEBUG=True`.

| Verificação | O que **eu** medi | Você declarou |
| --- | --- | --- |
| `ruff check .` | `All checks passed!` | 0 |
| `ruff format --check .` | `162 files already formatted` | 162 |
| `python manage.py check` | `no issues (0 silenced)` | limpo |
| `makemigrations --check --dry-run` | `No changes detected` | sem pendência |
| `pytest` (cópia limpa, `--create-db`) | **`1272 passed in 39.93s`**, exit 0, com `BL-218: … 43 pontos` | 1272 / 0 / 0 |
| `check-runs` de `edae1bf` (API do GitHub) | **2**: `Validar documentação: success`, `Lint e testes: success` | — |

Os números que você me deu conferem todos. A contagem de `check-runs`, **não** — ver A2.

**Sobre o recorte:** medi e **concordo com ele**. A mudança da rodada 4 é local a `Conta.clean()` e ao `WITH RECURSIVE`, e as sondas de regressão que rodei (custo, 5xx, conciliação, isolamento, cadastro legítimo) não acharam nada fora do recorte que a rodada 3 já não tivesse nomeado. Não amplio.

---

## 1. Veredito item por item

| Item | Veredito | Prova |
| --- | --- | --- |
| **BL-264** | **PARCIALMENTE FECHADO** — as duas causas que você pediu estão distinguidas e nenhuma levanta exceção fora de validação; sobrou uma **terceira** forma da mesma entrada que ainda estoura `ValueError` (P1) | Sondas S6/U1 |
| **BL-265** | **FECHADO no código**; a verificação adicionada junto **não verifica nada do código** (A1) | Grep + mutantes M4 (0 mortos) e M5 (8 mortos) |
| **BL-266** | **FECHADO** | M2 = 3 (era 2), M2total = **4**, M3 = **2** (era 1) |
| **A afirmação do implementador** | **NÃO REFUTADA** — 19 formas de requisição, nenhuma alcança o guard do modelo. Com duas precisões a registrar (A3, A4) | Sondas S1–S5, V0–V5 |

---

## 2. Achados no produto

### P1 — MÉDIA — BL-264 fechou a forma **menos** provável do defeito e deixou aberta a **mais** provável: `empresa_id` não-inteiro continua levantando `ValueError`, e isso também foi introduzido por esta etapa

- **Requisito afetado:** AGENTS.md §8 ("não converter falhas em sucesso aparente", tratar erro de forma consistente) e a classe que o próprio backlog escreveu na BL-264: *"guarda de validação nunca levanta exceção que não seja de validação"*.
- **Arquivo e localização:** `/home/user/DataLedger/apps/contabilidade/models.py:267-271` — `Empresa.objects.filter(pk=self.empresa_id)` com `self.empresa_id` não conversível a inteiro.
- **Evidência** (sonda U1, o **mesmo** `models.py` de três revisões, mesma sonda, mesmo banco):

```
########## models.py de edae1bf (auditado) ##########
[U1 empresa_id='abc'   ] !!! ValueError: Field 'id' expected a number but got 'abc'.
[U1 empresa_id='1e3'   ] !!! ValueError: Field 'id' expected a number but got '1e3'.
[U1 empresa_id='  '    ] !!! ValueError: Field 'id' expected a number but got '  '.
[U1 empresa_id=[]      ] !!! TypeError: Field 'id' expected a number but got [].
[U1 empresa_id=2**70   ] ValidationError [... 'a empresa informada não existe ...']   <- CORRIGIDO

########## models.py de a612604 (rodada 3) ##########
[U1 empresa_id='abc'   ] !!! ValueError: Field 'id' expected a number but got 'abc'.
[U1 empresa_id=2**70   ] !!! DoesNotExist: Empresa matching query does not exist.

########## models.py de 96284a4 (ANTES desta etapa) ##########
[U1 empresa_id='abc'   ] ValidationError ['O valor “abc” deve ser inteiro.']          <- era CORRETO
[U1 empresa_id='1e3'   ] ValidationError ['O valor “1e3” deve ser inteiro.']
[U1 empresa_id=[]      ] ValidationError ['O valor “[]” deve ser inteiro.']
```

  Reprodução mínima: `conta = Conta.objects.get(pk=<qualquer>); conta.empresa_id = "abc"; conta.full_clean()`.
- **Impacto:** exatamente o que a BL-264 descrevia — exceção que não é de validação, logo 500 em quem chamar. E é a forma **mais** provável no candidato que o próprio comentário do código nomeia (`models.py:261-263`, "a importação em lote da DL-010, que pode chamar `full_clean()` linha a linha"): numa planilha ou CSV, uma coluna numérica vindo com texto é mais comum do que um id inteiro que não existe. A BL-264 corrigiu o segundo caso e deixou o primeiro. **Não é alcançável hoje** pelo admin (`construct_instance` não atribui campo com erro de validação — medido na sonda S5: `codigo=""` + empresa de outro escritório devolve 200 pela camada do formulário, com `empresa_id` intacto), nem pela tela nem pela API, que não têm rota de alteração de `Conta`. Só por chamada programática — a mesma alcançabilidade que me fez graduar a BL-264 como BAIXA.
- **Por que MÉDIA e não BAIXA, e onde eu paro:** pelo **dano puro** (DE-038) isto é BAIXA, idêntico à BL-264. Subo um degrau por um motivo só, e ele é de processo, não de dano: **o item foi declarado fechado numa rodada dedicada a fechá-lo, e a classe que o próprio backlog escreveu não foi cumprida.** Se você preferir manter BAIXA por coerência de gravidade, não discuto — o que eu não aceito é a linha "BL-264 fechado" sem ressalva.
- **Nota menor, dentro do mesmo achado:** `empresa_id = None` produz `['Este campo não pode ser nulo.', 'Não é possível mudar esta conta: a empresa informada não existe. …']`. A segunda mensagem nomeia uma causa imprecisa — nada foi *informado*. O contador vê as duas, então o prejuízo é pequeno, mas é a mesma família que a rodada 4 existiu para corrigir.
- **Correção recomendada:** envolver a consulta e tratar as duas exceções do ORM como o que elas são — entrada inválida:

```python
try:
    escritorio_novo_id = (
        Empresa.objects.filter(pk=self.empresa_id)
        .values_list("escritorio_id", flat=True)
        .first()
    )
except (TypeError, ValueError) as erro:
    # `empresa_id` não é sequer um identificador válido (texto numa coluna
    # numérica, lista, etc.). É entrada inválida, não falha do sistema.
    raise ValidationError(
        "Não é possível mudar esta conta: o identificador de empresa "
        "informado não é válido. Confira o valor enviado."
    ) from erro
```

  Terceira mensagem, terceira causa — mantendo o princípio que a rodada 4 aplicou. Alternativa mais enxuta, se o Fred preferir duas mensagens em vez de três: reusar a mensagem de "não existe", que ao menos não nomeia causa errada.
- **Como verificar a correção — caso de teste, como texto:**

```
def test_full_clean_recusa_empresa_id_que_nao_e_identificador_valido(cenario):
    """BL-264, terceira forma: `empresa_id` não-inteiro. Antes: ValueError
    (não é exceção de validação, 500 em quem chamar). Em `96284a4`, antes
    desta etapa, o mesmo cenário devolvia ValidationError — é regressão da
    etapa, não limitação antiga."""
    for valor in ["abc", "1e3", "  ", []]:
        conta = _conta(cenario["empresa"], codigo=f"9-{valor!r}")
        conta.empresa_id = valor
        with pytest.raises(ValidationError):   # e NÃO ValueError/TypeError
            conta.full_clean()
```

  Mais um controle de que a distinção de três causas se mantém: as mensagens de "não é válido", "não existe" e "outro escritório" têm de ser **duas a duas diferentes**, no mesmo molde do `test_full_clean_distingue_…` que a rodada 4 já escreveu.
- **Responsável sugerido:** `desenvolvedor-pleno`.

### Observação (não é achado novo) — `conta_pai_id` inválido tem o mesmo comportamento, e é **anterior** a esta etapa

`models.py:135` e `:150` resolvem `self.conta_pai` diretamente. Medido: `conta_pai_id="abc"` → `ValueError`; `conta_pai_id=999999` → `Conta.DoesNotExist`. **Idêntico em `96284a4`** — não é regressão desta etapa e está fora do recorte. Registro só para que, quando a BL-264 for reaberta, o mesmo tratamento cubra os dois campos numa passada.

---

## 3. A afirmação que você mandou auditar — **não refutada**, com o que eu tentei

O implementador afirma: *"não existe cenário de requisição ao admin em que o guard de escritório do MODELO seja a camada que recusa — ele é estruturalmente inalcançável por ali"*.

**Não achei contraexemplo.** Tentei **19 formas** de requisição. Em todas, quem recusou foi o formulário (`Faça uma escolha válida`), com `empresa` inalterada no banco, `200`, zero 5xx e zero conta criada:

```
[S1 plain/_continue     ] 200 FORMULARIO  empresa inalterada  contas_criadas=0
[S1 _save               ] 200 FORMULARIO  ...
[S1 _addanother         ] 200 FORMULARIO
[S1 _saveasnew          ] 200 FORMULARIO  contas_criadas=0
[S1 _popup              ] 200 FORMULARIO
[S1 _to_field=id (GET)  ] 200 FORMULARIO
[S1 _to_field=id (POST) ] 200 FORMULARIO
[S1 _saveasnew+_popup   ] 200 FORMULARIO
[S4 ação em lote disparada da própria tela de change] 200 FORMULARIO
[S5 formulário quebrado por OUTRO campo (codigo="")] 200 FORMULARIO, empresa intacta
[V1 empresa enviada DUAS vezes (A e B)] 200 FORMULARIO
[V1 empresa com espaços " B "]          200 FORMULARIO
[V2 conta ANCORADA no escritório B, POST trazendo para A] 200 FORMULARIO
[V3 empresa de destino migrou p/ B por .update() depois do render] 200 FORMULARIO
[V3b empresa DA CONTA migrou p/ B depois do render]               200 FORMULARIO
[V4 object_id "00041" / "+41" / "41%20"]                          200 FORMULARIO
[V5 staff comum (change/view/add_conta + view_empresa)]           200 FORMULARIO
```

E medi **por que** é estrutural, em vez de aceitar por leitura:

1. `to_field_allowed` do Django só admite `id` para `Conta` (as duas FKs que apontam para ela — `Conta.conta_pai` e `ItemLancamento.conta` — miram a chave primária), então `_to_field` nunca faz o âncora do dropdown apontar para uma conta diferente da que está sendo validada.
2. `_saveasnew` é honrado pelo Django **mesmo com `save_as = False`** (`django/contrib/admin/options.py:2075`, sem gate) — mas ele zera `object_id` *local*, não o `resolver_match`, de modo que o dropdown continua ancorado no escritório da conta da URL, e o bloco do modelo nem roda (a instância é nova, `if self.pk:` falso).
3. `ContaAdmin` não tem superfície que construa a instância por fora do `ModelChoiceField`. Medido em produção, não lido:

```
[V0] inlines=[] get_inlines=() list_editable=[] save_as=False actions=[]
     autocomplete_fields=[] raw_id_fields=[] radio_fields={}
```

   Consequência direta: o item do seu recorte *"com o formset de itens em estados variados"* é **estruturalmente vazio** para `ContaAdmin` — não existe formset nessa tela. O único formset de `contabilidade` é o `ItemLancamentoInline` do `LancamentoContabilAdmin`, que tem `has_add_permission` e `has_change_permission` em `False`.

**Isto passa a ser o limite declarado da defesa de modelo naquela superfície**, com duas precisões que eu preciso registrar porque a docstring nova as afirma de forma mais ampla do que eu consegui medir — ver A3 e A4.

---

## 4. Achados no mecanismo e na sua integração

### A1 — MÉDIA — O teste que a rodada 4 acrescentou para a BL-265 **não exercita uma linha do código auditado**, e sua própria docstring afirma o contrário

- **Requisito afetado:** AGENTS.md §7 ("não aceitar testes que apenas repitam a implementação sem verificar o comportamento esperado"); é a família da **BL-142** (comentário falso) e da **BL-246** (cobertura declarada que não existia) — a mesma que esta rodada foi aberta para fechar.
- **Arquivo e localização:** `/home/user/DataLedger/apps/contabilidade/tests/test_dl023_conta_nao_muda_de_empresa_ou_natureza.py:346-364`, em especial a linha do comentário *"o que este teste prova é a FONTE (o `_meta`), não um valor diferente"* e as duas asserções:

```python
assert Conta._meta.get_field("conta_pai").column == "conta_pai_id"
assert ItemLancamento._meta.get_field("conta").column == "conta_id"
```

- **Evidência — mutação, suíte inteira por mutante, `--create-db` na primeira e `--reuse-db` nas demais:**

| Mutante | O que removi | Mortos |
| --- | --- | --- |
| **M4** | BL-265 **revertida a literal** (`coluna_conta_pai = "conta_pai_id"`, `coluna_conta_do_item = "conta_id"`) | **0** — `1272 passed` |
| **M5** | literal **errado** (`"coluna_que_nao_existe"`), controle positivo | **8**, com `Internal Server Error` em 7 requisições ao admin |

  M4 é a prova: o código pode voltar ao estado que a BL-265 corrigiu e **nenhum teste acusa**, inclusive o teste que leva o nome da correção. Ele não importa nem executa `_tem_movimento_proprio_ou_de_descendente`; ele consulta o `_meta` do Django e compara com uma constante. Passaria idêntico com o literal de volta no SQL.
- **Impacto, em três camadas:**
  1. A asserção **não prova a fonte**. Prova que o Django deriva `conta_pai` → `conta_pai_id`, que é comportamento do framework, não do DataLedger.
  2. O comentário **afirma** que prova. Quem ler o arquivo daqui a seis meses vai acreditar que a BL-265 tem guarda, e não tem.
  3. **O teste falha exatamente no cenário que a BL-265 existe para suportar.** Se alguém acrescentar `db_column="…"` a `Conta.conta_pai` — a mudança que motivou o item — o código de produção acompanha corretamente e **este teste reprova**, com um nome ("lê nomes de coluna pelo meta, não por literal") que descreve o oposto do que aconteceu. A guarda penaliza a mudança legítima e ignora a regressão.
- **Atenuação honesta, que não muda o veredito:** **a correção em si está certa e verificada.** Grep confirma que não sobrou literal de coluna no SQL cru (`models.py:118-128`; as demais ocorrências de `conta_pai_id`/`conta_id` no arquivo são comentário e atributo do ORM, não SQL) e `apps/core/views.py:17` é o único outro `cursor.execute` do projeto, um `SELECT 1`. M5 prova que a suíte detecta **valor** errado de coluna, de forma alta e não silenciosa — que foi exatamente o que eu disse na rodada 3. E eu mesmo escrevi, na rodada 3, que a verificação aceitável era *"um teste que troque o `db_column` num modelo de mentira, **ou simplesmente a leitura do diff**"*. Se nenhum teste tivesse sido acrescentado, eu não teria achado nada. O achado é o teste que foi acrescentado **e a frase que ele carrega**.
- **Correção recomendada:** ou remover o teste e deixar a justificativa no comentário do código (honesto, e era o que eu tinha aceitado), ou substituí-lo por um que amarre a consulta ao `_meta` de verdade.
- **Como verificar a correção — caso de teste, como texto:**

```
def test_a_consulta_recursiva_segue_o_db_column_declarado_no_meta(cenario):
    """Prova a FONTE, não o valor: troca o `column` do campo em tempo de
    execução e exige que a CONSULTA acompanhe. Com literal escrito à mão,
    a consulta ignoraria o patch e este teste não falharia — é essa
    diferença que o teste mede."""
    conta = _com_movimento(cenario, _conta(cenario["empresa"], codigo="1"))
    campo = Conta._meta.get_field("conta_pai")
    with mock.patch.object(campo, "column", "coluna_que_nao_existe"):
        with pytest.raises(ProgrammingError):
            conta._tem_movimento_proprio_ou_de_descendente()
    # controle positivo: sem o patch, a mesma chamada funciona
    assert conta._tem_movimento_proprio_ou_de_descendente() is True
```

  E o critério de aceite verificável: **M4 (reverter a literal) tem de matar esse teste.** Hoje mata zero.
- **Responsável sugerido:** `desenvolvedor-pleno`.

### A2 — MÉDIA (condição de encerramento, não defeito) — `edae1bf` tem **2** `check-runs`, não 3: o guarda "Regras do projeto" **não rodou** nesta revisão, e as correções da rodada 4 **não estão na `main`**

- **Requisito afetado:** AGENTS.md §5 (validar → commitar → push → **abrir o PR** → conferir a CI) e a tabela "Como estas regras são impostas", que nomeia `Regras do projeto` como um dos quatro mecanismos impostos tecnicamente.
- **Evidência** (API pública do GitHub, lida por mim — `gh` não existe neste ambiente):

```
check-runs de edae1bf: total 2
  Validar documentação: completed / success
  Lint e testes:        completed / success
  (ausente: "Regras do projeto")

PR #24: state=closed merged=True merged_at=2026-09-16T13:01:50Z
        head=a612604 base=main  merge_commit=82356358
git ls-remote: refs/heads/claude/accounting-agent-team-setup-mn6lyf -> edae1bf
git branch --contains edae1bf: só a branch de trabalho (NÃO main)
origin/main: 8235635 "Merge pull request #24 …"
```

- **Causa medida:** `.github/workflows/regras-do-projeto.yml` dispara **só** em `pull_request`. O PR #24 foi mesclado com head `a612604` (o estado da rodada 3, **com as ressalvas**), e `edae1bf` foi empurrado para a mesma branch depois disso, sem PR aberto. Nenhum evento `pull_request` alcançou esta revisão.
- **Impacto:** a `main` hoje contém a etapa **no estado da rodada 3** — com a BL-264, a BL-265 e a BL-266 **abertas**. As correções que eu acabei de auditar estão só na branch. E a revisão que você quer declarar encerrável é a única das três auditadas em que o terceiro mecanismo não rodou (na rodada 1 rodaram dois; em `a612604` rodaram três).
- **Correção recomendada:** abrir PR de `claude/accounting-agent-team-setup-mn6lyf` para `main` com o escopo da rodada 4, conferir os **três** checks verdes no commit mais recente e mesclar. Não é defeito de código; é o passo do ciclo que falta.
- **Como verificar:** `check-runs` da revisão de topo do PR devolvendo `Regras do projeto: success` junto com os outros dois, e `git branch --contains` da revisão listando `main`.
- **Responsável sugerido:** `arquiteto-senior`.

### A3 — BAIXA — A docstring nova diz que o guard do modelo "é MAIS FORTE, cobrindo o que a porta única de escrita de hoje não alcançaria sozinha". **No admin, não é.** Os dois buracos são o mesmo buraco

`test_dl023_conta_nao_muda_de_empresa_ou_natureza.py:403-441`. A única superfície do admin em que o guard do **formulário** não se aplica é o `add` — porque `_escritorio_da_conta_em_edicao` devolve `None` sem `object_id` (`admin.py:80-81`). E é exatamente a superfície em que o guard do **modelo** também não se aplica, porque todo o bloco está sob `if self.pk:` (`models.py:226`). Medido:

```
[S2 add] status=302  criada=True  escritorio_da_criada=4 (esc_B=4)
[S3 GET add]    SIGILOSO_no_corpo=True
[S3 GET change] SIGILOSO_no_corpo=False
```

Ou seja: no admin, os dois guards **não se cobrem**, coincidem. A frase é verdadeira só para quem chama `full_clean()` fora do admin — que é, aliás, o que o teste de `full_clean()` prova. Correção: qualificar a frase ("mais forte **fora do admin**; no admin, a única superfície descoberta pelo formulário — o `add` — também está descoberta pelo modelo, ver BL-267"). Isto não é achado novo de produto: o `add` criando conta em escritório alheio já é a **BL-267**, e o vazamento do dropdown do `add` e do `/admin/autocomplete/` já é a **BL-262** — confirmei os dois ainda presentes, como declarado, incluindo com staff comum:

```
[V5 autocomplete] 200 {"results": [{"id": "39", "text": "SIGILOSO"}], "pagination": {"more": false}}
```

### A4 — BAIXA — A asserção **negativa** `assert "fronteira de isolamento" not in corpo` congela em teste a camada que a BL-262 e a BL-267 estão planejadas para mudar

`…:440`. Hoje ela é verdadeira e eu a medi verdadeira. Mas ela proíbe, por teste, que a mensagem do modelo apareça naquela resposta — e o caminho de correção que eu mesmo recomendei na rodada 3 (ancorar o dropdown no escritório do **usuário**, não no da conta) é justamente o que faria o modelo passar a ser a camada que recusa em parte dos casos. Quem fizer a BL-262 vai encontrar este teste vermelho com uma docstring afirmando "não existe cenário", e terá de decidir se é regressão ou a mudança pretendida.

Existe asserção que nomeia a camada **sem** proibir a outra. Medi as duas chaves, lado a lado, no mesmo POST:

```
[admin, dropdown restrito]    form.errors = {'empresa': ['Faça uma escolha válida. Sua escolha não é uma das disponíveis.']}
[ModelForm sem a restrição]   form.errors = {'__all__': ['Não é possível mudar esta conta para uma empresa de outro escritório: …']}
```

Proposta, como texto: `assert "empresa" in resposta.context["adminform"].form.errors` (camada do formulário) — positiva, explícita quanto à camada, e continua sendo morta pelo M3. Trocar a linha `not in` por um comentário que diga que hoje `__all__` está vazio **e por quê**, em vez de uma asserção que proíbe.

---

## 5. Prova por mutação — refeita por mim

Suíte inteira por mutante, cópia fora do repositório, banco dedicado. Restaurei e conferi o `sha256` do original ao fim de cada série.

| Mutante | Defesa removida | Mortos | Rodada 3 | Você declarou |
| --- | --- | --- | --- | --- |
| **M2** | só a comparação de escritório (`original["empresa__escritorio_id"] != escritorio_novo_id` → `False`) — **refazendo fielmente o meu M2 da rodada 3** | **3** | 2 | 4 |
| **M2total** | **todo** o guard de escritório, incluindo o ramo `escritorio_novo_id is None` da BL-264 | **4** | — | 4 |
| **M2b** | só o ramo da BL-264 (`if escritorio_novo_id is None` → `False`) | **2** | — | — |
| **M2c** | BL-264 revertida (`escritorio_novo_id = self.empresa.escritorio_id`) | **2** | — | — |
| **M3** | `ContaAdmin.formfield_for_foreignkey` | **2** | 1 | 2 |
| **M4** | BL-265 revertida a literal | **0** | — | — |
| **M5** | coluna literal **errada** (controle positivo) | **8** | — | — |

**Sobre o "4" que você me mandou conferir:** o número depende da definição do mutante, e a diferença é minha com você, não erro dele. Refazendo o **meu** M2 da rodada 3 ao pé da letra — aquele guard era uma comparação só, porque o ramo da BL-264 ainda não existia — dá **3**. Tomando "o guard de escritório" como o bloco inteiro que hoje decide a fronteira, dá **4**, e **a declaração dele confere**. Registro os dois com a definição colada, porque número de mutação sem a definição do mutante é a mesma armadilha que a BL-266 acabou de fechar.

Mortos do M2total, nominalmente:

```
test_full_clean_recusa_mover_conta_para_empresa_de_outro_escritorio
test_full_clean_recusa_conta_com_empresa_inexistente_com_mensagem_propria
test_full_clean_distingue_empresa_inexistente_de_empresa_de_outro_escritorio
test_mensagem_de_conta_cruzando_escritorio_nao_cita_papel_interno
```

Mortos do M3:

```
test_admin_recusa_via_formulario_mover_conta_livre_para_empresa_de_outro_escritorio
test_admin_change_nao_lista_empresa_de_outro_escritorio_no_dropdown
```

O teste renomeado **agora morre** com o mutante da camada que ele nomeia. Era o pedido da BL-266, e está cumprido. E os seis irmãos que receberam asserção de mensagem do modelo estão entre os 8 mortos do M5, o que confirma que passaram a ser sensíveis à camada do modelo e não só ao status.

---

## 6. Regressão, no recorte — nada encontrado

| Verificação | Medido |
| --- | --- |
| **Custo de consulta** (`CaptureQueriesContext`, `full_clean()`) | `conta LIVRE, nada mudou` **5**; `com movimento, NADA mudou` **5**; `com movimento, natureza MUDOU` **6**; `sintética→aceita_lancamento=False com movimento` **4**. **Idênticos aos 5/5/6/4 da rodada 3** — o `values_list` novo não custou nada |
| **Custo do caminho novo** | Troca de empresa (mesmo escritório **ou** outro) = **6**, isto é **+1** sobre a base — e o código anterior (`self.empresa.escritorio_id`) pagava a mesma consulta, só que carregando a linha inteira. Sem regressão; ligeira melhora |
| **5xx novo** | 15 GETs ao admin (raiz, listagens dos 4 apps, `add`, `change`, `delete`, `history`, `list_filter` com id inexistente): **nenhum 5xx**. `object_id` = `999999`, `abc`, `-1`, `0` → **302**, não 500 |
| **Cadastro legítimo engessado** | 6 operações, **302 nas seis**: renomear conta com movimento, mudar código dela, desativá-la, renomear sintética com filha movimentada, trocar empresa dentro do mesmo escritório, mudar natureza de conta livre. Filha permanece `devedora` |
| **Conciliação Diário/Razão/Balancete** | `total_debitos = total_creditos = soma do Diário = 1234.56`; `razão.saldo_final = 1234.56` = linha do Balancete; `localizar_inconsistencias_de_hierarquia = []`; tipo `Decimal`, nunca `float` |
| **Isolamento entre empresas** | Empresa A = 1234,56 e empresa B = 900,00, sem contaminação nos dois balancetes |
| **Isolamento entre escritórios (gravação)** | 19 formas de requisição, zero gravação cruzando escritório no `change` |

---

## 7. O que eu **não** consegui medir

1. **`scripts/validate-docs.ps1`** — `pwsh` não existe neste ambiente. Tenho o `check-run` "Validar documentação: success" de `edae1bf`, lido por mim na API; não é a mesma coisa que eu ter rodado.
2. **Comportamento em SQLite** — a BL-243 continua indecidida; não executei.
3. **Concorrência / níveis de isolamento** — nada nesta rodada; o recorte é de validação síncrona, e a rodada 3 já mediu a corrida da BL-246.
4. **Renderização real em navegador** — só cliente de teste do Django.
5. **`_saveasnew` como porta de duplicação de conta** — confirmei que o Django o honra mesmo com `save_as = False` (`options.py:2075`) e que **nada foi criado** nas minhas sondas, porque o dropdown recusou antes. Não explorei o caso em que o payload é legítimo, porque está fora do recorte. Se quiser, é uma pergunta de uma sonda.
6. **Correção contábil e legal.** Auditoria de software não substitui validação profissional das regras contábeis e legais, e nada aqui afirma conformidade legal nem ausência total de defeitos.

---

## 8. Parecer

# APROVADO COM RESSALVAS

Restrito ao recorte, item por item: **BL-266 está fechado** — o teste que media a camada errada agora morre com o mutante da camada que nomeia (M3 passou de 1 para 2, exatamente o que você declarou), e os seis irmãos ficaram sensíveis ao modelo, provados pelos 8 mortos do M5. **BL-265 está fechado no código** — não sobrou literal, a consulta continua correta e os testes da BL-245 a exercitam de verdade, como o controle positivo M5 demonstra com 7 erros de servidor quando a coluna está errada. **BL-264 está fechado pela metade**: as duas causas que você mandou medir estão distinguidas, com mensagens próprias, e nenhuma das duas levanta exceção fora de validação — mas a mesma entrada numa terceira forma (`empresa_id` não-inteiro) ainda estoura `ValueError`, e eu provei, rodando a mesma sonda contra três revisões, que em `96284a4` aquilo devolvia `ValidationError` limpa: é regressão desta etapa, sobrevivente à rodada que existiu para removê-la.

**A afirmação do implementador eu não aceitei por leitura, e não consegui refutá-la.** Ataquei com 19 formas de requisição — `_to_field`, `_popup`, `_saveasnew` (que o Django honra mesmo com `save_as = False`), `_save`, `_addanother`, ação disparada da própria tela de change, `empresa` enviada duas vezes, com espaços, `object_id` com zero à esquerda, com `+`, com `%20`, conta ancorada no escritório oposto, empresa migrando de escritório entre o render e o POST nos dois sentidos, staff comum em vez de superusuário, e o caminho `add`. Em todas, quem recusou foi o formulário. Medi também os três motivos estruturais, em vez de deduzi-los: `to_field_allowed` só admite a chave primária para `Conta`; `_saveasnew` zera o `object_id` local mas não o `resolver_match`, então o dropdown continua ancorado e o bloco do modelo nem roda; e `ContaAdmin` não tem inline, `list_editable`, ação própria, `autocomplete_fields` nem `raw_id_fields` — o que torna **vazio** o item "formset de itens em estados variados" do seu recorte, e isso é uma medição, não uma omissão. **Isto passa a ser o limite declarado da defesa de modelo naquela superfície**, com a ressalva A3: a docstring afirma mais do que eu medi, porque no admin os dois guards não se cobrem, coincidem — a única superfície que o formulário não alcança (`add`) é a mesma que o modelo não alcança.

**As ressalvas, explícitas, e nenhuma esconde falha essencial:**

1. **P1 (MÉDIA)** — `empresa_id` não-inteiro continua levantando `ValueError`/`TypeError`. Pelo dano puro é BAIXA e eu digo isso com todas as letras; subo um degrau só porque o item foi declarado fechado e a classe escrita no próprio backlog não foi cumprida, e porque a forma que sobrou é a **mais** provável no candidato que o código nomeia (importação em lote da DL-010: texto em coluna numérica). Correção de um `try/except`, com caso de teste pronto acima.
2. **A1 (MÉDIA)** — o teste acrescentado para a BL-265 não toca o código de produção: reverter a correção a literal (M4) mata **zero** testes, e o comentário afirma que ele "prova a FONTE". Ele ainda falha no exato cenário (`db_column=`) que a BL-265 existe para suportar. A correção está certa; a verificação não verifica, e a frase é falsa. Isto é a família da BL-142 e da BL-246 reaparecendo **dentro** da rodada que as estava fechando — é a ressalva que eu trataria primeiro, pelo mesmo motivo que dei na rodada 3: guarda que não guarda é pior que guarda ausente, porque desliga a próxima medição.
3. **A2 (MÉDIA, condição)** — `edae1bf` tem **2** `check-runs`, não 3. "Regras do projeto" dispara só em `pull_request`, o PR #24 foi **mesclado** com head `a612604`, e a `main` hoje contém a etapa **no estado da rodada 3, com BL-264/265/266 abertas**. As correções que acabei de auditar não estão na `main` e não passaram pelo terceiro mecanismo.
4. **A3 e A4 (BAIXA)** — a docstring generaliza além do medido, e a asserção negativa `"fronteira de isolamento" not in corpo` congela em teste a camada que a BL-262 e a BL-267 estão planejadas para mudar. Há substituto medido e melhor: `form.errors` tem chave `'empresa'` na camada do formulário e `'__all__'` na do modelo.

**Continuam fora e não são achado novo, confirmados ainda presentes como declarado:** BL-261, BL-262 (medi de novo o `/admin/autocomplete/` respondendo `SIGILOSO` e o dropdown do `add` vazando), BL-267 (medi de novo: `add` cria conta em escritório alheio, `302`), BL-247, BL-250, BL-252, BL-253, BL-256, BL-263, BL-268, BL-269.

**Pronta para levar ao Fred como encerrável?** **Sim, condicionada a duas coisas objetivas**, e nenhuma delas é rodada nova de ciclo completo — pela DE-038, P1 e A1 são de dano baixo e cabem em backlog nomeado:

- **Condição 1, indispensável:** `edae1bf` precisa chegar à `main` por PR com os **três** checks verdes (A2). Enquanto isso não acontecer, a etapa está encerrada só na branch, e o que está integrado é o estado **com** as três ressalvas da rodada 3 abertas. Isso não é opinião; é o que a API devolve.
- **Condição 2, de honestidade do registro:** a linha "BL-264 fechado" e a seção de BL-265 no arquivo de teste precisam parar de afirmar mais do que entregam. Ou se corrige P1 e A1 no mesmo commit — as duas são pequenas, e eu deixei o código e os casos de teste prontos —, ou se registram como itens novos de backlog com a classe escrita, e a BL-264 e a BL-265 passam a constar como **parcialmente fechadas**, não fechadas.

Minha recomendação, se for para escolher: **corrija P1 e A1 no mesmo commit da PR** — juntas são menos de trinta linhas e evitam abrir dois itens de backlog para dívida que o `desenvolvedor-pleno` fecha em minutos — e leve ao Fred com as quatro ressalvas nomeadas: o `ValueError` residual, o teste decorativo, a `main` desatualizada até o merge, e as duas precisões de docstring. Não declaro ausência de defeitos, segurança absoluta nem conformidade legal, e a decisão contábil da BL-261 continua sendo do Fred, não minha.

**Encaminhamentos:** P1 e A1 → `desenvolvedor-pleno`. A2 → `arquiteto-senior`. A3 e A4 → `desenvolvedor-pleno`, na mesma passada de A1.

**Arquivos relevantes:**
`/home/user/DataLedger/apps/contabilidade/models.py` (`:111-132` consulta recursiva; `:226-300` guards de empresa/escritório; `:267-271` o ponto do P1)
`/home/user/DataLedger/apps/contabilidade/admin.py` (`:67-107` âncora e restrição do dropdown)
`/home/user/DataLedger/apps/contabilidade/tests/test_dl023_conta_nao_muda_de_empresa_ou_natureza.py` (`:346-364` o teste do A1; `:403-441` docstring e asserção negativa de A3/A4; `:493-534` os dois testes da BL-264)
`/home/user/DataLedger/.github/workflows/regras-do-projeto.yml` (gatilho só em `pull_request`, causa do A2)
`/home/user/DataLedger/docs/projeto/backlog.md` (`:800-808`, BL-261 a BL-269)
`/home/user/DataLedger/docs/agents/estado.md` (`:222`)

---

## Notas do `arquiteto-senior`, escritas DEPOIS do relatório

**Parecer aceito integralmente.** Segui a recomendação do próprio auditor:
**corrigir P1 e A1 na mesma passada**, em vez de abrir dois itens de dívida para
trabalho de minutos — e os dois ficam registrados assim mesmo (**BL-270** e
**BL-271**), porque item corrigido na rodada seguinte à que o declarou fechado é
justamente o que não pode sumir do registro.

**Sobre o A2, e ele não é culpa de ninguém:** o **Fred mesclou o PR #24** em
2026-09-16, marcando-o como pronto. Ele estava aberto como rascunho e com o
corpo dizendo, com todas as letras, *"este PR NÃO deve ser mesclado como está"*
— mas rascunho no GitHub não impede merge, e a palavra do dono do produto vale
mais que a minha marcação. O efeito prático é o que o auditor mediu: a `main`
tem a etapa no estado da rodada 3, com três ressalvas abertas, e as correções
da rodada 4 ficaram na branch. **A lição, para mim:** confiei numa marcação que
não é mecanismo. A `main` deste projeto não tem proteção (BL-02), o que faz
"rascunho" ser sinal social, não guarda. Enquanto a BL-02 não for fechada pelo
Fred, PR aberto é PR mesclável, e eu escrevo os corpos partindo disso.

**Sobre o A1, que é o achado mais fino das cinco rodadas:** um teste que passa
sempre, com comentário afirmando que prova o que não prova, e que **reprovaria
na mudança legítima que ele deveria proteger**. É a família que custou a BL-142
e a BL-246 reaparecendo **dentro** da rodada que as estava fechando. O auditor
tem razão em chamá-lo de primeiro da fila: guarda que não guarda desliga a
próxima medição, porque quem vier depois vai confiar nela.
