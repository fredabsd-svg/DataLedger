# Auditoria DL-024 — rodada 3, revisão `23c6ac8`

> Relatório do `auditor-qa`. Toda mutação foi feita em **cópias fora do repositório**, geradas por `git archive 23c6ac8`. Bancos PostgreSQL isolados (`aud_r3_dl024`, `aud_r3_med`, `aud_r3_b`, criados e apagados por ele), portas 8913/8914. Chromium 1194.
>
> **Preservado integralmente pelo `arquiteto-senior`, sem edição, supressão ou suavização de achado.** As notas do arquiteto estão no fim, separadas, e não alteram uma linha do texto acima delas.

## 0. Condições da auditoria — conferidas, não aceitas

Você pediu que eu conferisse o estado declarado. **Confere, e não houve divergência durante a auditoria.**

| Item | Declarado | Medido por mim |
| --- | --- | --- |
| Revisão | `23c6ac84915269851093e6b0c4b458f14e2546b1` | idêntica |
| Branch | `claude/accounting-agent-team-setup-mn6lyf` | idêntica |
| `git status --porcelain` | vazio | vazio, no início **e no fim** |
| `LOCAL == origin` | sim | `23c6ac8` nos dois |
| Agentes rodando | nenhum | nenhum processo `manage.py` alheio; árvore intacta do início ao fim |

A mudança de método do §0 da rodada 2 funcionou: **desta vez a árvore não se mexeu debaixo de mim.** Registro porque a rodada passada foi o oposto.

Reprodução dos seus números, em cópia limpa:

| Verificação | Seu número | O meu |
| --- | --- | --- |
| `pytest` | 1451 passed, 12 skipped, 59,85s | **1451 passed, 12 skipped em 49,33s** |
| `ruff check .` | limpo | "All checks passed!" |
| `ruff format --check .` | 174 arquivos | "174 files already formatted" |
| `manage.py check` | limpo | "no issues (0 silenced)" |
| `git diff -- templates/ static/` após a suíte | vazio | vazio — conferido por `diff -r` contra um `git archive` fresco |
| `validate-docs.ps1` | não rodou (`pwsh` ausente) | **não rodei: `pwsh` ausente.** Repliquei as cinco regras em Python: 95 `.md`, 0 problemas |

O scratchpad tinha rastro da execução perdida. **Refiz toda a medição do zero**, com base nova, banco novo e servidor novo; reaproveitei só a ideia dos instrumentos.

---

## 1. O que eu confirmo, medido

| Item | Como medi | Resultado |
| --- | --- | --- |
| **Base versionada (BL-300)** | rodei `scripts/semear_base_de_medicao.py` num banco vazio | **73 contas, 4 níveis, 60 lançamentos**, D = C = 241.709,66. **Reproduzível de verdade** — o achado B2 está fechado |
| **As duas travas do script** | quatro tentativas de burlar | sem senha → recusa; senha vazia → recusa; sem confirmação → recusa; confirmação genérica `sim` → recusa; nome errado → recusa. **Zero escrita no banco em todas** (`escritorio`/`empresa`/`conta`/`lancamento` = 0) |
| **Densidade (BL-299)** | Chromium 1194, 1280×800, a base versionada | Balancete **9** com período filtrado, **4** na entrada padrão, Plano de contas **14**, Diário **11**. **Os quatro números da §4.8 batem exatamente** |
| **§4.8 responde às três perguntas** | leitura | sim: 73 contas / 4 níveis / 60 lançamentos, 1280×800, linha inteiramente visível, **e o qualificador "com período filtrado"**. B1 fechado |
| **Conciliação do balancete** | soma das 73 linhas × `tfoot` × faixa × banco | **241.709,66 nos quatro**, débito = crédito |
| **Contraste** | 7 telas, alfa de fundo e de texto compostos | **0 pares reprovados** |
| **Tabulação** | largura `111111` vs `888888` no elemento-folha | 444/444 balancete, 122/122 diário, 2/2 lançamento |
| **Sem JavaScript, sem rede externa** | `document.querySelectorAll('script').length`, interceptação de requisições | **0 e 0** em todas as telas |
| **Rolagem horizontal a 1280px** | `scrollWidth > innerWidth` | **falsa** em todas |
| **Foco visível (critério 7)** | `getComputedStyle` antes/depois de `focus()` | muda de fato: `0px none` → **`3px solid rgb(29,58,110)`** em todo elemento focável |
| **Nome acessível (BL-295)** | árvore de acessibilidade do Chromium | limpo: `link nome='Plano de contas' keyshortcuts=['Alt+C']` — o `Alt+C` **não** vaza para o rótulo |
| **BL-291 (A3)** | a sabotagem exata da rodada 2, `apps/fiscal/templates/fiscal/apuracao.html` mal formado | **5 failed** — extends, caption, scope, valor e estilo. A porta fechou |
| **BL-292 (M1)** | `<td style=color:red;font-size:22px>` no `balancete.html` | **reprova**. `data-style="x"` continua não sendo falso positivo |
| **BL-293 (M2)** | as **cinco** linhas de sabotagem do meu relatório, uma a uma | **as cinco reprovam** |
| **BL-294 (M3)** | `<td><br class="valor-monetario">{{ ..._ptbr }}` | **reprova** |
| **BL-295 (M4)** | `class="tecla destaque"` sem `aria-hidden`; `accesskey="c d"` | **os dois reprovam** |
| **BL-296 (M5)** | nono template em `templates/contabilidade/` sem a parcial | **reprova**, nomeando o arquivo |
| **BL-297 (M6)** | `texto-apoio` → `texto-apoio-legenda` | **reprova** |
| **BL-298 (M7)** | `gravar` com `0,00`/`0,00` | frase própria, sem "precisam ser iguais" |
| **BL-301 (B3)** | estado de erro do balancete | agora navegável: *"O período informado não pôde ser usado… **Voltar ao período padrão (mês atual)**"* |
| **BL-302 (B4)** | entrada padrão | **"Nada a conferir neste período"**, não mais "Fecha" verde sobre 0,00 |
| **BL-303 (B5)** | leitura do critério 4 | escrita no plano, com Diário/Razão fora **e a limitação declarada** (medi de novo: **1604px** no Diário) |
| **Isolamento** | usuário de outro escritório e papel `cliente`, 7 telas + POST | **404 em 7+POST sem vazar** razão social, nome de escritório nem nome de conta; **403 com `sem_permissao`** em 7 para `cliente`; conta de outra empresa num POST → **400 "conta inválida"**, sem nomear a empresa alheia |

O trabalho desta rodada é bom. Quinze achados endereçados, e a maior parte fechada **de verdade** — provei por mutação, não por leitura. O que segue é o que sobrou, e uma parte é grave.

---

## 2. Achados

### A1 — ALTA — O veredito "Fecha" continua **mentindo**: a correção do BL-289 cobriu `adicionar_linha` e deixou o caminho `gravar` inteiro de fora

**Requisito afetado:** critério de aceite **5**; BL-289; DE-042 §3. **É o achado A1 da rodada 2, vivo, no segundo caminho.**

**Arquivo e localização:** `apps/contabilidade/views_web.py:1824-1839` — a chamada de `_contexto_form_lancamento` no ramo de recusa do `gravar` **não passa `linhas_excluidas_do_total`**, que cai no padrão `0`. Comparar com `:1626-1640`, o ramo `adicionar_linha`, que passa.

**Reprodução** (cliente de teste real, cópia de `23c6ac8`, quatro casos independentes):

| POST `acao=gravar` | HTTP | `veredito_fechamento` | Frase na página |
| --- | --- | --- | --- |
| 2 linhas válidas batendo + 1 linha com valor `"abc"` | **400** | `'fecha'` | **Fecha** |
| 2 linhas válidas batendo + 1 linha incompleta | **400** | `'fecha'` | **Fecha** |
| 2 linhas válidas batendo + histórico de 400 caracteres | **400** | `'fecha'` | **Fecha** |
| 2 linhas válidas batendo + data `"abacaxi"` | **400** | `'fecha'` | **Fecha** |

Saída literal do primeiro caso, a página inteira que o contador recebe:

```
<li class="mensagem mensagem-error" role="alert">
  <strong>Erro:</strong> Linha 3: valor "abc" inválido. …
</li>
…
<td colspan="2" class="linha-total__veredito linha-total__veredito--fecha">
    Total conferido antes de gravar
    <br>
    <strong class="veredito-fechamento">Fecha</strong>
</td>
<td class="valor-monetario">Débito: 100,00</td>
<td class="valor-monetario">Crédito: 100,00</td>
```

**A invariante que você mesmo definiu está violada nos quatro casos**: *"se a tela diz Fecha, `gravar` com o mesmo corpo tem de devolver 302, nunca 400"*. Aqui a tela diz Fecha **na própria resposta 400**.

Agravante: com `linhas_excluidas_do_total = 0`, o aviso R2-5 (*"N linha(s) … ainda NÃO entram no total abaixo"*) **também some** nesse caminho. O contador vê um erro no topo, um "Fecha" verde embaixo, e nenhuma menção a que uma linha foi descartada. É **pior** que o estado 2 do A1 da rodada 2, onde ao menos o aviso estava lá.

**Por que nenhum teste pegou — e esta é a quinta ocorrência que você pediu:** `test_invariante_fecha_implica_gravar_302_nunca_400` lê o veredito de uma resposta de `adicionar_linha` e, **em seguida, num POST separado**, verifica o status de `gravar`. Nunca lê o veredito **da resposta do `gravar`**. As três camadas do BL-306 também não: `test_pagina_real_lancamento_*` usa `acao=adicionar_linha` e `GET`. A guarda ficou, de novo, **um passo antes de onde o defeito aparece** — desta vez a um `acao=` de distância.

**Correção recomendada:** passar `linhas_excluidas_do_total=len(erros_incompletas)+len(erros_itens)` na chamada de `:1824`, como já se faz em `:1636`; e, acima disso, fazer a invariante ser testada **na mesma resposta**: para qualquer POST `acao=gravar`, se o HTML contiver `veredito-fechamento` com "Fecha", o status **tem de ser 302**. Uma asserção só, aplicada à resposta que o servidor devolve, mata os quatro casos e qualquer quinto.

**Como verificar:** os quatro POSTs da tabela, exigindo que a página de 400 **não** contenha "Fecha"; mais um controle positivo (2 linhas batendo, sem erro) exigindo 302.

**Responsável:** `desenvolvedor-pleno`.

---

### A2 — ALTA — A sabotagem A2 da rodada 2 **ainda passa**: trocar o crédito da faixa pelo débito dá `1451 passed`, e no estado divergente a faixa **esconde** a divergência que ela existe para denunciar

**Requisito afetado:** critérios **4**, **10** e **13**; BL-290; DE-042 §3.

**Arquivo e localização:** o teste que fechou o BL-290, `apps/contabilidade/tests/test_dl017_telas.py:630-635`:

```python
assert _ptbr_para_decimal(valores_faixa[0]) == total_debitos_rodape == Decimal("1700.00")
assert _ptbr_para_decimal(valores_faixa[1]) == total_creditos_rodape == Decimal("1700.00")
```

**Os dois números são 1700,00.** Por partida dobrada, num balancete real eles são **sempre** iguais. Um teste que compara a faixa com o `tfoot` só nesse estado **não consegue distinguir débito de crédito** — ele é vácuo exatamente para a sabotagem que motivou sua existência.

**Reprodução:** apliquei, em cópia, **metade** da minha sabotagem da rodada 2 — só a troca do número, sem tocar no veredito:

```
templates/contabilidade/balancete.html
-  <strong class="valor-monetario">{{ total_creditos_ptbr }}</strong>
+  <strong class="valor-monetario">{{ total_debitos_ptbr }}</strong>
```

Suíte inteira: **`1451 passed, 12 skipped`**.

**E o efeito, no único estado em que importa.** Forcei a apuração divergente (`total_creditos + 0,01`, o mesmo `monkeypatch` que o projeto usa), sob a sabotagem:

```
FAIXA:  "Não fecha — diferença de 0,01 entre débitos e créditos próprios do período"
        Débitos próprios do período  300,00
        Créditos próprios do período 300,00     ← os dois iguais
RODAPÉ: 300,00   300,01                          ← a divergência real
```

A faixa **contradiz a si mesma** — anuncia 0,01 de diferença e exibe dois números idênticos — e **esconde** a corrupção que o `tfoot` mostra. `test_balancete_veredito_nao_fecha_e_exercitado_com_totais_divergentes` (`:844-857`) é o único teste que alcança esse estado e confere **apenas a classe e o texto** da faixa; os números divergentes ele confere no **rodapé**, nunca na faixa.

**Impacto:** o comentário de `_veredito_balancete` diz, com todas as letras, que o ramo "não fecha" existe porque *"é exatamente no dia em que algo corromper o dado que esta tela precisa gritar"*. A faixa pode gritar o texto certo mostrando os números errados, e nada acusa. É a mesma frase que escrevi na rodada 1 e na rodada 2: **mecanismo que declara guardar e não guarda** — agora na terceira versão do mesmo mecanismo.

**Correção recomendada:** conferir os números da faixa **no estado divergente**, onde eles são distinguíveis: sob o `monkeypatch`, exigir `faixa[0] == 300,00` **e** `faixa[1] == 300,01`, isto é, iguais ao `tfoot` e **diferentes entre si**. O teste do estado balanceado pode ficar, mas ele não conta como guarda dessa troca, e convém que o comentário diga isso.

**Como verificar:** repetir a troca acima; ela tem de matar o teste.

**Responsável:** `especialista-frontend`.

---

### A3 — ALTA — A varredura fechou **cinco** das seis guardas para `apps/<app>/templates/` e deixou justamente a **última cláusula do critério 13**: módulo novo nasce sem linha no "momento da verdade", com a CI verde

**Requisito afetado:** critério **13**, literalmente *"…ou quando módulo novo aparece sem a sua linha na tabela do 'momento da verdade'"*; BL-291.

**Arquivo e localização:** `apps/core/tests/test_dl024_varredura_de_interface.py:797`:

```python
for pasta in sorted(p for p in TEMPLATES.iterdir() if p.is_dir()):
```

`TEMPLATES` ainda é `RAIZ / "templates"` (`:43`). O BL-291 trocou `_templates()` por `_arquivos_do_projeto(RAIZ, "*.html")` e ligou as cinco guardas de **conteúdo** à raiz — esta, que é de **existência de módulo**, continuou na pasta antiga.

**Reprodução, com o controle ao lado** (cópia de `23c6ac8`, módulo "Cobrança", que não tem linha no §3):

| Onde o template mora | Varredura |
| --- | --- |
| `apps/cobranca/templates/cobranca/titulos.html` | **74 passed** |
| `templates/cobranca/titulos.html` (o **mesmo arquivo**) | **1 failed** — `"Módulos com tela mas sem LINHA NA TABELA do 'momento da verdade' (§3)… cobranca"` |

O template é idêntico e bem formado (estende `base.html`, tem `<caption>`, `<th scope>`, `valor-monetario`). A única diferença é a pasta — e o Django serve as duas.

**Por que ninguém viu:** `test_controle_positivo_detector_de_templates_em_apps_do_projeto` (`:995-1046`), escrito para fechar o BL-291, verifica **exatamente cinco** guardas: extends, caption, scope, valor, estilo. A sexta, que é uma cláusula própria do critério 13, não entra no controle. É o mesmo padrão do A1 e do A2 desta rodada: **a guarda foi escrita para o relato, não para o requisito**.

**Impacto:** é o A3 da rodada 2 sobrevivendo na metade que mais interessa ao futuro. Você escreveu que queria saber disso *"agora, não quando o Fiscal estiver construído em cima dela"* — o Fiscal, a Folha e o Lalur ainda podem nascer em `apps/<app>/templates/` sem nunca responder qual pergunta a tela deles responde antes de gravar, que é a coisa que a DL-024 inteira existe para institucionalizar.

**Correção recomendada:** a guarda passa a enumerar as pastas de módulo a partir de `_templates(RAIZ)` — o diretório-pai de cada template encontrado, com a mesma normalização já usada (`NOME_DO_MODULO_NA_TABELA`, `PASTAS_QUE_NAO_SAO_MODULO`) —, não de `TEMPLATES.iterdir()`. E acrescentar a **sexta** asserção ao controle positivo de `tmp_path`, com o caso "Cobrança".

**Como verificar:** os dois passos da tabela acima; os dois têm de reprovar, nomeando a pasta.

**Responsável:** `desenvolvedor-pleno` (mecanismo).

---

### M1 — MÉDIA — `apps/core/marcacao.py` é cego para atributo **sem aspas**, e o M4/BL-295 volta inteiro com `1451 passed` e o vazamento medido no navegador

**Requisito afetado:** critério **7**; BL-295/BL-297; família BL-288.

**Arquivo:** `apps/core/marcacao.py:49-53` — `_padrao_atributo` só casa `nome="valor"` e `nome='valor'`:

```python
rf'\s{re.escape(nome_atributo)}\s*=\s*(?:"([^"]*)"|\'([^\']*)\')'
```

Medido, função por função:

```
'<kbd class="tecla">'             -> tem_classe = True
"<kbd class='tecla'>"             -> True
'<kbd class = "tecla">'           -> True
'<kbd CLASS="tecla">'             -> True
'<kbd class="tecla destaque">'    -> True
'<kbd class=tecla>'               -> False     ← HTML5 válido, e o navegador aplica
```

**Reprodução no produto** (cópia, `templates/contabilidade/_navegacao_empresa.html`, cinco ocorrências):

```
-  <kbd class="tecla" aria-hidden="true">Alt+C</kbd>
+  <kbd class=tecla>Alt+C</kbd>
```

- `test_dl024_atalhos_e_acessibilidade.py` → **19 passed**
- `test_dl024_varredura_de_interface.py` → **74 passed**
- suíte inteira → **1451 passed, 12 skipped**

**E o efeito, na árvore de acessibilidade do Chromium**, produto contra mutante, mesma base, mesmo navegador:

```
== PRODUTO 23c6ac8
   link nome='Plano de contas'         keyshortcuts=['Alt+C']
   link nome='Diário'                  keyshortcuts=['Alt+I']
== MUTANTE (class=tecla, sem aspas)
   link nome='Plano de contas Alt+C'   keyshortcuts=['Alt+C']
   link nome='Diário Alt+I'            keyshortcuts=['Alt+I']
```

É o **achado A2 da rodada 1** de volta, pela terceira vez, e a tela continua **visualmente idêntica** — o navegador aplica `.tecla` normalmente. O mesmo vale para `PADRAO_TAG_COM_ACCESSKEY` (`test_dl024_atalhos_e_acessibilidade.py:114-117`), que exige aspas: `accesskey=c` sai por inteiro das **três** guardas de accesskey.

**O que torna isto notável:** na **mesma rodada**, o BL-292 corrigiu exatamente este caso — atributo sem aspas — para `style=`, com comentário de sete linhas explicando por que o HTML5 permite e o Chromium aplica. A lição foi escrita e não atravessou para o módulo novo criado ao lado dela. É a família BL-288 outra vez, agora no arquivo que existe para acabar com a família.

**Correção recomendada:** acrescentar a terceira alternativa ao padrão, como `PADRAO_ESTILO_EMBUTIDO` já faz — ``(?:"[^"]*"|'[^']*'|[^\s"'=<>`]+)`` — e o mesmo em `PADRAO_TAG_COM_ACCESSKEY`. Controle positivo com `<kbd class=tecla>` e `accesskey=c`.

**Nota de duplicação:** o docstring de `marcacao.py` diz que a varredura *"hoje já tem a versão correta escrita localmente e vai passar a importar daqui"*. Ela não importa: `_tem_classe` e `_PADRAO_ATRIBUTO_CLASS` continuam duplicados em `test_dl024_varredura_de_interface.py:120-134`. Três implementações viraram **duas**, e o módulo criado para unificá-las **não tem arquivo de teste próprio** (`apps/core/tests/` não tem `test_marcacao.py`). Um defeito nele erra nos dois consumidores de uma vez — que é exatamente o que este achado mostra.

**Responsável:** `desenvolvedor-pleno`.

---

### M2 — MÉDIA — Os testes de mutação do BL-306 **corrompem o repositório de forma permanente** sob duas execuções concorrentes, e uma delas ainda relata sucesso

**Requisito afetado:** AGENTS.md §7 ("não deixar artefatos"); BL-273; BL-306.

**Arquivo:** `apps/contabilidade/tests/test_dl024_veredito_no_html_renderizado.py:619-718` — as duas mutações escrevem em `templates/contabilidade/{lancamento_form,balancete}.html` **na árvore real**, restaurando em `try/finally`.

**Você perguntou se o risco é real. É, e eu medi as duas metades.**

*A janela.* Vigiei os dois arquivos a cada 0,5 ms durante a execução do arquivo de teste:

```
[2.40s] MUTADO no disco: templates/contabilidade/lancamento_form.html (+46 bytes)
[2.42s] restaurado — ficou mutado  22 ms
[2.42s] MUTADO no disco: templates/contabilidade/balancete.html (+46 bytes)
[2.46s] restaurado — ficou mutado  32 ms
```

*A corrupção.* Rodei o mesmo arquivo de teste **duas vezes em paralelo na mesma árvore**, com bancos distintos, seis vezes seguidas. Resultado final:

```
rodada 1: lancamento=*** MUTADO ***  balancete=INTACTO
rodada 2: lancamento=*** MUTADO ***  balancete=*** MUTADO ***
…
rodada 6: lancamento=*** MUTADO ***  balancete=*** MUTADO ***

templates/contabilidade/lancamento_form.html:304
-  {% if veredito_fechamento == "fecha" %}
+  {% if nunca_definido_no_contexto %}{% if nunca_definido_no_contexto %}{% if nunca_definido_no_contexto %}{% if veredito_fechamento == "fecha" %}
```

O mecanismo é o clássico: a segunda execução lê como "original" o conteúdo **já mutado** pela primeira e restaura para ele. A mutação **acumula** — rodar de novo não conserta. E na rodada 1 **uma das duas execuções terminou com código de saída 0**: é possível ter um `pytest` verde e a árvore corrompida ao mesmo tempo, que é precisamente a condição que a sua conferência (`git diff -- templates/ static/` vazio depois de rodar) não distingue.

**A favor da entrega, e medi:** a corrupção **não é silenciosa**. Rodei a suíte sobre a árvore corrompida: **20 failed, 1431 passed**, e o `git diff` acusa. Não é um defeito que passa despercebido; é um defeito que precisa de reparo manual e que só aparece depois.

**Impacto:** o conteúdo corrompido é exatamente o defeito que esta etapa existe para impedir — o veredito **some das duas telas**. E o gatilho não é hipotético: este projeto tem **BL-273** justamente porque já houve agentes escrevendo na mesma árvore ao mesmo tempo, inclusive durante a rodada 2 desta etapa.

**Correção recomendada:** não mutar a árvore real. O Django permite resolver o mesmo template por nome a partir de um diretório temporário — `override_settings(TEMPLATES=[... "DIRS": [tmp_path] ...])` com uma cópia do `templates/` inteiro, mais `reset_loaders()`, prova a mesma coisa (a URL real, a view real, o loader real) sem tocar no repositório. Se a mutação no lugar for mantida, ela precisa no mínimo de um arquivo-trava exclusivo que faça a segunda execução **pular** em vez de escrever.

**Como verificar:** repetir as seis rodadas concorrentes; a árvore tem de ficar intacta nas seis.

**Responsável:** `especialista-frontend` (autor) com decisão do `arquiteto-senior`.

---

### M3 — MÉDIA — A condição `linhas_excluidas_do_total == 0` do veredito **não tem teste**: removê-la dá `1451 passed`

**Requisito afetado:** BL-289.

**Arquivo:** `apps/contabilidade/views_web.py:984-989`.

**Reprodução:** removi a condição do `fecha`:

```
-        and linhas_excluidas_do_total == 0
         and num_partidas_validas >= 2
```

Suíte inteira: **`1451 passed, 12 skipped`**.

A condição é uma das **quatro** que o docstring de `_veredito_fechamento` declara garantir a invariante. O caso em que ela é a única que trabalha — **duas linhas válidas batendo mais uma descartada** — não existe em teste nenhum. `test_duas_linhas_com_valor_invalido_descartadas_nao_e_fecha` cobre o caso em que **todas** as linhas são inválidas, e ali `num_partidas_validas == 0` já decide sozinho.

Confirmei que o produto acerta esse caso hoje, no caminho `adicionar_linha` (`veredito='nao_conferido'`, `linhas_excluidas_do_total=1`, página: *"Ainda não conferido"*). É a mesma condição que o **A1** mostra não ser aplicada no caminho `gravar` — os dois achados são o mesmo buraco visto de dois lados.

**Nota de honestidade:** testei também trocar `num_partidas_validas >= 2` por `>= 1` e a suíte ficou verde. **Não conto como achado:** com uma partida só, `debito == credito` implica ambos zero, e `debito > 0` já reprova. É mutante equivalente, não cobertura ausente.

**Correção recomendada:** um teste com duas partidas válidas batendo mais uma descartada, exigindo `veredito_fechamento == "nao_conferido"`, nos **dois** valores de `acao`.

**Responsável:** `desenvolvedor-pleno`.

---

### M4 — MÉDIA — O detector de medida fechou as cinco sabotagens do relatório e continua cego para a **família** delas

**Requisito afetado:** critérios **8** e **13**; BL-293.

**Arquivo:** `test_dl024_varredura_de_interface.py:270-301`.

As cinco linhas do meu relatório da rodada 2 reprovam, uma a uma — confirmado. O que continua passando, cada linha sozinha em `static/css/base.css`:

| Sabotagem | Varredura |
| --- | --- |
| `.sab { max-width: 517px; min-height: 42px; box-shadow: 0 0 7px var(--c); flex-basis: 250px; text-indent: 19px; }` | **74 passed** |
| `.sab { font-size: 2lh; gap: 3rlh; padding: 1.5cap; margin: 4ic; }` | **74 passed** |

Causa: `(?<![\w-])` antes de `width`/`height` faz `max-width`, `min-width`, `max-height` e `min-height` não casarem — o hífen imediatamente antes nega o *lookbehind*. E a lista de unidades, embora ampliada, não tem as relativas à linha e ao glifo (`lh`, `rlh`, `cap`, `ic`, `rex`, `rch`).

**Ressalva a favor da entrega, e eu conferi:** varri `base.css` com um detector meu **sem nenhuma restrição** de propriedade e com todas as unidades. **Zero literais fora do `:root`.** O arquivo está limpo de verdade. O problema é a promessa do critério 13 para o próximo módulo — e `min-width`/`max-width` não é caso exótico: é o que se escreve para uma coluna de valor.

**A classe, que vale mais que a lista:** a correção reproduziu **exatamente** a tabela do meu relatório e parou ali. Um detector que enumera casos relatados sempre estará uma auditoria atrás. A direção robusta é a inversa — acusar **qualquer** propriedade cujo valor contenha unidade de comprimento, com lista de exceções nomeadas, como já se faz em `NOMES_QUE_NAO_SAO_TINTA` e `PASTAS_QUE_NAO_SAO_MODULO`.

**Como verificar:** as duas linhas da tabela; cada uma tem de reprovar nomeando `static/css/base.css`.

**Responsável:** `desenvolvedor-pleno`.

---

### M5 — MÉDIA — O veredito "em três camadas" é a mesma checagem em três lugares: `display: none` passa nas três

**Requisito afetado:** critério **5**; BL-306.

**Reprodução:** acrescentei ao fim de `static/css/base.css`:

```css
.veredito-fechamento, .faixa-fechamento { display: none; }
```

Suíte inteira: **`1451 passed, 12 skipped`**.

As três camadas — chave do contexto, fragmento renderizado, página real — leem **texto em HTML**. Nenhuma pergunta se o contador vê alguma coisa. A terceira camada é uma fronteira nova e legítima (ela mata a sabotagem do bloco inalcançável, confirmei), mas ela não é uma terceira **espécie** de garantia.

**Registro com a ressalva que ela merece:** a §4.8 e o docstring da varredura declaram, honestamente, que medição em navegador **não roda na CI** porque exige Chromium. Não é ocultação. O que eu aponto é a **redação**: "veredito guardado em três camadas" descreve uma profundidade que a defesa não tem, e é essa frase que vai ser lida daqui a seis meses. Uma camada que morre porque *o veredito não está visível* teria de ser o juiz do gauntlet, na obrigação escrita do §4.8 — e o §4.8 hoje manda rodá-lo por **densidade**, não por visibilidade de elemento.

**Correção recomendada:** ou acrescentar ao juiz do gauntlet a conferência de que os elementos do "momento da verdade" estão visíveis e com contraste (ele já mede contraste), ou corrigir a redação para "três pontos da mesma cadeia", declarando que visibilidade é medida fora da CI.

**Responsável:** `arquiteto-senior` (redação) / `especialista-frontend` (se for ampliar o juiz).

---

### B1 — BAIXA — `docs/agents/estado.md` descreve o estado da DL-024 em dois lugares, e os dois divergem; e o cabeçalho erra a revisão da `main`

**Arquivo:** `docs/agents/estado.md:3-4` e `:222` contra `:255-282`.

1. O cabeçalho diz *"com a `main` em **`8235635`** (PR #24 integrado)"*. Medido: `origin/main` está em **`bfe9814`** (PR #25), 22 minutos depois — e o PR #25 chama-se `doc-estado-pos-dl023`, isto é, **o próprio commit que mesclou este arquivo tornou a afirmação dele falsa**.
2. A tabela de etapas (`:222`) descreve a DL-024 como *"Em desenvolvimento — **rodada 1 REPROVADA** em `5c7303e`"*, listando BL-274/275/276. A seção "Próximo passo" (`:258-282`), no mesmo arquivo, registra **duas** rodadas reprovadas e a rodada 4 em execução, com BL-289/290/291.

O `CLAUDE.md` é explícito: *"Não descreva o estado do projeto em outro arquivo… A causa do problema não foi distração, foi **duplicação**."* A duplicação aqui é **interna ao arquivo**, que o teste de CI não alcança — ele exige que o `DL-xxx` apareça, não que as duas descrições concordem. É o achado 7 da DL-017 rodada 1 e o achado 8 da DL-015 rodada 2, no mesmo arquivo, pela terceira vez.

Complemento da mesma família: o número de cegueiras do BL-274 aparece como **cinco** em `estado.md:222`, **seis** em `README.md:67` e **sete** no meu relatório da rodada 2. As três leituras são defensáveis (seis formas + o detector inexistente), mas três números para a mesma coisa em três arquivos é o sintoma, não o problema.

**Responsável:** `arquiteto-senior`.

---

### B2 — BAIXA — A segunda trava do script de semeadura **imprime o nome do banco** que ela pede para você digitar, e o relatório do script mente ao repetir

**Arquivo:** `scripts/semear_base_de_medicao.py:94-102` e `:257-286`.

1. O comentário justifica a trava assim: *"Exigir o NOME do banco, e não um 'sim', é deliberado: confirmação genérica é aceita sem ler; **digitar o nome obriga a olhar para ele**."* Medido, a mensagem de recusa é:

```
Recusado: este script grava 60 lançamentos no banco 'aud_r3_med'.
Para seguir, defina DL_CONFIRMO_BANCO_DESCARTAVEL='aud_r3_med'.
```

O nome vem pronto para copiar. Na prática a trava é "rode, copie da mensagem, rode de novo" — o que **é** uma defesa (impede execução por engano num único comando), mas **não é** a defesa que o comentário descreve. Vale corrigir o texto ou a mensagem, no mesmo padrão de "comentário não afirma mais do que a defesa entrega" que este projeto aplica em todo lugar.

2. Rodei o script **duas vezes** no mesmo banco. As duas imprimem `lançamentos gravados: 60`; o banco tem **60** (a idempotência funciona, conferi). Ou seja: a segunda execução gravou **zero** e relatou 60. O comentário de `:282-283` diz *"Repetição idempotente cai aqui"*, apontando para `recusados` — e ela **não cai**: `criar_lancamento` devolve o existente sem erro, e o contador `gravados` incrementa. Um script escrito para fechar um achado de honestidade de medição não devia relatar 60 escritas que não aconteceram.

3. Menor, mas registro: `VinculoUsuarioEscritorio` usa `papel=Papel.values[0]` — o papel do usuário de medição depende da **ordem de declaração** do enum. Se ela mudar, a base de medição pode passar a medir telas sob outro papel (e `cliente` recebe 403). Vale nomear o papel.

Nada disso é bloqueio: a base é reproduzível e as travas seguram. São acabamentos do instrumento.

**Responsável:** `arquiteto-senior`.

---

### B3 — BAIXA — `_valores_sem_classe` aceita a classe declarada **dentro de outro atributo**

**Arquivo:** `test_dl024_varredura_de_interface.py:120` / `apps/core/marcacao.py:49`.

```
<td title="ver class='valor-monetario' aqui">{{ x_ptbr }}</td>   ->  reprova = False
```

`\sclass\s*=\s*'...'` casa dentro do valor de `title`. Contrived, e não há caso real hoje; registro porque é a **direção permissiva** do mesmo casamento por expressão regular dos M1 acima, e porque o arquivo declara, com razão, que não é um parser de HTML. Se for aceito como limitação, convém escrevê-la ao lado da limitação já declarada.

**Responsável:** `desenvolvedor-pleno`.

---

## 3. O que eu **não** consegui medir

1. **`scripts/validate-docs.ps1`** — `pwsh` ausente no container. Repliquei as cinco regras em Python: 95 `.md`, 0 problemas. Não é a mesma coisa que rodar o script da CI.
2. **Leitor de tela real** — nenhum. Medi a árvore de acessibilidade calculada pelo Chromium, que é marcação correta, não uso real.
3. **Firefox e Safari** — nenhum. Só Chromium 1194.
4. **Paginação A4 real** — gerei o PDF A4 (95.929 bytes) e confirmei que a navegação some na impressão e a faixa permanece; **não conferi quebra de página, repetição de `thead` entre folhas nem margens**. BL-282 segue aberto. A impressão continua saindo com **"DataLedger."** no topo, com o nome do escritório logo abaixo — o critério 9 pede a identidade **do escritório**, e o item 4 da pesquisa do plano nomeia a marca do fornecedor no relatório como vício a não herdar. Está no BL-281/BL-282, abertos por decisão.
5. **1920×1080 e modo escuro** — não medidos (o segundo não existe, por decisão declarada).
6. **Concorrência de escrita contábil** — nenhum teste nesta superfície; ela não foi tocada nesta etapa. A concorrência que medi foi a de **execução de testes** (M2).
7. **A medição que decidiu o gauntlet** — os HTML dos protótipos continuam fora do repositório, com a limitação declarada na §7.
8. **BL-305** (cor em atributo de template) — confirmei que segue aberto (`<svg><rect fill="#ff0000">` num template → 74 passed), como você declarou. Não conto como achado novo.

---

## 4. Respostas diretas ao que você perguntou

**"A mesma classe de defeito apareceu quatro vezes. Procure a quinta."**
Achei **três**, e as três são a mesma junta: **A1** (o veredito é testado em `adicionar_linha` e mente em `gravar` — a guarda ficou a um `acao=` de distância); **A2** (o teste da faixa compara dois números que são iguais por construção, e por isso não consegue distinguir a troca que motivou a sua existência); **A3** (o controle positivo do BL-291 enumerou as cinco guardas do meu relato e deixou a sexta, que é cláusula própria do critério). O padrão comum não é "um passo antes" — é **a guarda foi escrita contra o relatório, não contra o requisito**. Os três casos fecham exatamente a sabotagem descrita e nada além dela.

**"Ataque o veredito em três camadas. Encontre a terceira sabotagem."**
Encontrei duas que sobrevivem. A primeira é o **A1** — nenhuma das três camadas visita a resposta de `gravar`. A segunda é o **M5**: `display: none` no CSS passa nas três, porque as três são a mesma espécie de checagem. A sua terceira camada é real e mata a sabotagem do bloco inalcançável — confirmei —, mas ela amplia o **alcance**, não a **natureza** da garantia.

**"`apps/core/marcacao.py`: se estiver errado, erra em todos de uma vez."**
Está errado, e erra nos dois (não quatro — só dois arquivos importam dele; a varredura mantém a cópia local). O erro é o atributo **sem aspas** (M1), fechado no mesmo dia para `style=` e não para `class=`/`accesskey=`. E o módulo criado para unificar a regra **não tem teste próprio**.

**"O detector de medida e o de estilo: confirme se fecharam e procure as que sobraram."**
O de estilo fechou: as três formas de aspas reprovam e o falso positivo do `data-style` sumiu. O de medida fechou **as cinco que eu relatei** e continua aberto para `max-width`/`min-width`/`max-height`/`min-height`, `box-shadow`, `flex-basis`, `text-indent`, `column-width`, e para as unidades `lh`/`rlh`/`cap`/`ic` (M4). O `base.css` está limpo de verdade — conferi com detector irrestrito, **zero** literais.

**"Densidade: o piso de 9 se sustenta com a base versionada?"**
**Sim.** Semeei a base versionada num banco vazio e medi: 9 / 4 / 14 / 11, os quatro números da §4.8, exatos. E a §4.8 **responde às três perguntas que ela exige** — quantas contas, qual largura, em que estado. B1 e B2 fechados.

**"Isolamento entre empresas e escritórios: `views_web.py` mudou de novo."**
Confirmado e preservado. 404 em 7 telas + POST para usuário de outro escritório, sem vazar razão social, nome de escritório nem nome de conta; 403 com `erros/sem_permissao.html` em 7 telas para o papel `cliente`; conta de outra empresa num POST → 400 "conta inválida", sem nomear a empresa alheia. As mudanças desta rodada em `views_web.py` são de veredito e de mensagem; não tocam consulta nenhuma.

**"Os testes de mutação escrevem o template real. Isso é achado?"**
**É (M2), e mais do que você supôs.** Não é só o risco de interrupção: **duas execuções concorrentes na mesma árvore corrompem o repositório de forma permanente e acumulativa**, e pelo menos uma delas ainda relata sucesso. Reproduzi seis vezes seguidas. A favor da entrega: a corrupção é **barulhenta** (20 failed na execução seguinte, e o `git diff` acusa), não silenciosa.

---

## 5. Parecer

# REPROVADO

**Critério:** não aprovo havendo falha de gravidade alta. Há três, e as três são a mesma família.

- **A1** — o veredito "Fecha" **continua mentindo**, agora no caminho `gravar`, em quatro estados alcançáveis, com a invariante que você mesmo escreveu (*"Fecha implica 302"*) violada na própria resposta 400, e o aviso de linha descartada suprimido junto. É o A1 da rodada 2, fechado em um dos dois caminhos.
- **A2** — a **metade numérica** da minha sabotagem da rodada 2 ainda passa: `1451 passed` com o crédito da faixa trocado pelo débito. E no único estado que importa — dado corrompido — a faixa **contradiz a si mesma e esconde a divergência** que o rodapé mostra. O teste escrito para fechar o BL-290 compara dois números que a partida dobrada torna sempre iguais; ele não consegue reprovar a troca.
- **A3** — a varredura fechou cinco das seis guardas para `apps/<app>/templates/` e deixou **justamente a última cláusula do critério 13**: o mesmo módulo, bem formado, reprova em `templates/` e passa em `apps/<app>/templates/`. É o caminho pelo qual o Fiscal ainda nasce sem nunca declarar a pergunta que a tela dele responde antes de gravar.

Pela DE-038, **nada aqui está na lista de dano**: nenhum lançamento desbalanceado é gravado, nenhum dado vaza entre empresas ou escritórios, nenhum cálculo erra, nenhum período encerrado se altera. O servidor continua recusando toda gravação que não fecha — conferi. É legítimo converter M1–M5 e B1–B3 em backlog nomeado. **A1, A2 e A3, não**: o A1 é uma afirmação falsa na superfície de decisão do módulo Contábil, e o A2 e o A3 são mecanismo que declara guardar e não guarda.

**E registro o que esta rodada fez bem, porque foi muito.** As cinco sabotagens do detector de medida fecharam; o `style=` sem aspas fecha nas três formas; o invólucro vazio reprova; o `kbd` com segunda classe e o `accesskey` com dois tokens reprovam; o nono template sem a parcial reprova nomeando o arquivo; o `texto-apoio-legenda` reprova; a mensagem de recusa de total não positivo ganhou frase própria; o estado de erro do balancete ficou navegável; "Nada a conferir neste período" substituiu o "Fecha" verde sobre zero; a leitura do critério 4 está escrita, com a limitação de Diário e Razão declarada e medida; a base sintética é **reproduzível por qualquer pessoa** e eu reproduzi os quatro números da §4.8 exatamente; o contraste tem zero reprovações; a tabulação é 444/444; o foco muda de estilo de fato; zero JavaScript e zero requisição externa; o balancete concilia com os lançamentos de origem até o centavo; e o isolamento entre empresas e escritórios está preservado em todas as superfícies que testei.

A distância entre isto e a aprovação não é de esforço. É de **onde a guarda é posta**: três vezes nesta rodada ela foi posta contra a frase do meu relatório em vez de contra o requisito, e as três vezes o defeito continuou do outro lado da linha.

Não afirmo ausência de defeitos, segurança absoluta nem conformidade contábil ou legal: **auditoria de software não substitui validação profissional das regras contábeis e legais.**

---

**Evidências e instrumentos** (todos fora do repositório, no scratchpad da sessão):

- Cópias auditadas: `R3/`, `PRISTINO/`, `MED/` (base de medição), `SAB1/`, `SAB2/`, `MUT/`, `JAN/` (regeneradas por `git archive 23c6ac8` a cada sabotagem).
- Sondas: `test_zz_sonda_auditor_r3.py` (veredito nos caminhos de `gravar`), `test_zz_sonda_html_r3.py`, `test_zz_sonda_isolamento_r3.py`, `test_zz_sonda_faixa.py`.
- Medição: `medir_r3aud.py` (densidade, contraste, tabulação, rolagem, scripts, 7 telas), `erro_r3.py` (estados de erro/vazio/conferência), `ax_r3.py` e `ax_mut_r3.py` (árvore de acessibilidade, produto × mutante), `concilia_r3.py`/`conc2.py` (conciliação corpo × rodapé × faixa × banco), `vigia.py` (janela de mutação no disco, amostragem de 0,5 ms).
- Capturas e PDF: `bal_a4_r3.pdf`.
- Semeadura: **`scripts/semear_base_de_medicao.py` do próprio projeto** — não a minha.

**Nenhum arquivo do repositório foi alterado por mim.** Não tenho `Write` nem `Edit`, não usei `ruff format` sem `--check`, e nunca escrevi dentro de `/home/user/DataLedger`. Os três bancos (`aud_r3_dl024`, `aud_r3_med`, `aud_r3_b`) e os dois servidores foram encerrados e removidos. Estado final conferido: `HEAD` em `23c6ac8`, `git status --porcelain` vazio, `git diff --stat` vazio, `git diff -- templates/ static/` vazio.

---

## Notas do `arquiteto-senior`, escritas DEPOIS e sem alterar nada acima

**1. Aceito o parecer integralmente. Os três altos são reais e eu não contesto nenhum.**

**2. A frase que define esta rodada é dele, e é a mais útil que recebi neste projeto:**

> *"A distância entre isto e a aprovação não é de esforço. É de **onde a guarda é posta**: três vezes nesta rodada ela foi posta contra a frase do meu relatório em vez de contra o requisito."*

Na rodada anterior eu tinha nomeado o padrão como *"o teste fica um passo antes de onde o defeito aparece"*. Estava incompleto, e a correção dele é mais precisa: o problema não é distância, é **referência**. Uma guarda escrita contra o texto de um achado fecha exatamente aquele achado e nada mais — e por construção estará sempre uma auditoria atrás.

A causa é minha, e é de como eu distribuo: eu entrego aos implementadores **o achado**, com reprodução e "como verificar", e eles otimizam para o que eu entreguei. Preciso entregar o **requisito** e usar o achado como exemplo, não como especificação.

**3. O A1 é o mesmo defeito que reprovou a rodada anterior, sobrevivendo no segundo caminho.** Eu verifiquei os quatro estados do formulário — e todos pelo `adicionar_linha`. Escrevi a invariante *"diz Fecha ⇒ grava 302"* e não percebi que o teste dela nunca lê o veredito **da resposta do `gravar`**. A invariante estava certa; o teste dela mediu duas requisições diferentes.

**4. O M2 é consequência direta de uma decisão minha.** Eu autorizei mutação na árvore real por considerar o risco aceitável, com `try/finally` e conferência byte a byte. Ele mediu: **duas execuções concorrentes corrompem o repositório de forma acumulativa, e uma delas termina com sucesso**. O projeto tem BL-273 justamente porque agentes já escreveram na mesma árvore ao mesmo tempo — inclusive por minha causa, na rodada 2. Eu aprovei um mecanismo cujo modo de falha o próprio backlog já documentava.

**5. O B1 é o defeito fundador deste projeto, no arquivo criado para eliminá-lo.** O `estado.md` descreve a DL-024 em dois lugares e os dois divergem, e o cabeçalho afirma uma revisão da `main` que o próprio PR que o mesclou tornou falsa. A instrução permanente do Fred, de 2026-09-13, foi escrita por causa disso. Agora é dentro de um arquivo só, onde o teste de CI não alcança.
