# Auditoria DL-023 — rodada 7 (integração das rodadas 4 e 6), revisão `1b828e7`

> **Relatório do `auditor-qa` preservado integralmente.** Nenhuma frase editada,
> suavizada ou omitida pelo `arquiteto-senior`. As notas do arquiteto vêm
> **depois**, na última seção.
>
> **Escopo restrito declarado pelo Fred:** esta rodada **não roda suíte** —
> PostgreSQL não está disponível neste ambiente — e **mede se as correções das
> rodadas 4 (`edae1bf`) e 6 (`f23484a`) estão integradas na `main` em `1b828e7`**
> como presença de código, presença de teste, distinção de mensagens, e
> configuração dos mecanismos de CI. Toda a validação dinâmica é da rodada 7
> completa, via CI, e **não** desta.
>
> **Sobre a numeração, para quem procurar:** a DL-023 conta rodadas de trabalho
> — rodada 1 (auditoria, REPROVADA), 2 (correção), 3 (auditoria, aprovada com
> ressalvas), 4 (correção, BL-264/265/266), 5 (auditoria focada), 6 (correção,
> BL-270/271/272), 7 (esta, integração). Nenhuma renumeração.

Audit complete. O `auditor-qa` confirmou: a árvore do repositório está
**intacta** ao fim da inspeção (`git status --porcelain` vazio, `git diff
--stat` vazio); nada foi mutado, nada foi comitado pelo auditor antes desta
auditoria, e nenhuma verificação dinâmica rodou — por decisão do Fred, e
porque o ambiente não tem PostgreSQL para `pytest`. O **único commit**
produzido por esta rodada é o de preservação do próprio relatório, na branch
`claude/dl-023-rodada-7-auditoria` — ver §7.

## 0. Integridade e ambiente

| Verificação | O que **eu** medi |
| --- | --- |
| Branch atual | `claude/dl-023-rodada-7-auditoria`, working tree limpo |
| `git status --porcelain` | vazio |
| `git merge-base --is-ancestor edae1bf 1b828e7` | exit 0 — **edae1bf é ancestral de 1b828e7** |
| `git merge-base --is-ancestor f23484a 1b828e7` | exit 0 — **f23484a é ancestral de 1b828e7** |
| `git diff 1b828e7 f23484a -- apps/contabilidade/models.py` | vazio — `models.py` em `1b828e7` é **byte a byte idêntico** a `f23484a` |
| `git diff 1b828e7 f23484a -- apps/contabilidade/tests/test_dl023_conta_nao_muda_de_empresa_ou_natureza.py` | vazio — o arquivo de teste em `1b828e7` é **byte a byte idêntico** a `f23484a` |
| `git log --all --source --oneline 1b828e7 -- apps/contabilidade/models.py` | topo: `f23484a` (rodada 6), depois `edae1bf` (rodada 4), depois `5662a42` (rodada 1), depois `a612604` (rodada 2) |
| Suíte `pytest` | **NÃO RODADA** — sem PostgreSQL, por decisão do Fred. Não posso afirmar nada sobre execução. |

Os ancestrais confirmam: `1b828e7` é descendente de **ambas** as rodadas de
correção, e como `f23484a` foi a última alteração do `models.py` antes do
merge de PR #27, o estado na `main` é exatamente o estado entregue pela
rodada 6 — que, sendo descendente da rodada 4, contém também a rodada 4.

**Observação de processo, não achado:** os ancestrais do `git` confirmam o
caminho que o relatório da rodada 5 já tinha desenhado (`1b828e7` é a `main`
atual e contém as duas rodadas), mas só agora o caminho foi percorrido por um
mecanismo. Os três ancestrais relevantes para a integração são:

```
1b828e7 (Merge PR #27 — DL-018 primeiro acesso)
└─ 72a2215 (Merge PR #26 — DL-023 rodadas 4 e 6)
   ├─ bfe9814 (Merge PR #25 — estado pós-DL-023)
   └─ f23484a (fix: rodada 6 — BL-270, BL-271 e BL-272)
      ├─ a58a616 (docs: registrar a rodada 5 e o merge do PR #24)
      ├─ 19efc16 (merge: trazer a main para a branch da rodada 4)
      │  ├─ edae1bf (fix: rodada 4 — BL-264, BL-265 e BL-266)  ← está aqui
      │  └─ bfe9814 (Merge PR #25)
      └─ (docs)
```

`edae1bf` **não** entrou na `main` por PR próprio (a auditoria da rodada 5 já
havia medido isto em A2: PR #24 foi mesclado com head `a612604`, sem
`edae1bf`). Entrou pela porta do PR #26, quando a branch que recebeu
`f23484a` foi mesclada de volta à `main` já trazendo o merge `19efc16` que
continha `edae1bf` no segundo pai. O caminho é mais comprido do que o natural
(deveria ser dois PRs de fix), mas o resultado é o mesmo: as duas correções
estão no merge que a `main` aponta.

---

## 1. Veredito item por item

| Item | Veredito | Prova |
| --- | --- | --- |
| **BL-264** (rodada 4) | **PRESENTE NA `main`** — e o resultado final da rodada 6 (com a guarda de tipo e a guarda de `None`) **também** está | `models.py:321-352` — bloco do `if self.pk:` em `Conta.clean()` lê `escritorio_novo_id` via `Empresa.objects.filter(pk=self.empresa_id).values_list("escritorio_id", flat=True).first()` (sem `DoesNotExist`); emite **3 mensagens distintas** para as 3 causas (tipo inválido, inexistente, outro escritório) + guarda de `None` separada |
| **BL-265** (rodada 4) | **PRESENTE NA `main`** — confirmado que o resultado da rodada 6 (testes com `mock.patch.object`) **substituiu** a versão da rodada 4 (teste de literal) | `models.py:111-114` — quatro identificadores do `_meta`: `db_table` × 2, `.get_field(...).column` × 2 (`conta_pai` e `conta_do_item`); zero literal `"conta_pai_id"` ou `"conta_id"` no SQL |
| **BL-266** (rodada 4) | **PRESENTE NA `main`** — e a rodada 6 **qualificou** a docstring e trocou a asserta negativa por positiva | `test_dl023_…:469-532` — o teste `test_admin_recusa_via_formulario_mover_conta_livre_para_empresa_de_outro_escritorio` assere **positivamente** `"empresa" in form.errors` (camada do formulário, sem proibir a camada do modelo); o comentário qualifica o alcance medido, não o desejado |
| **BL-270** (rodada 6) | **PRESENTE NA `main`** | `models.py:321-333` — `try/except (TypeError, ValueError)` com `raise ValidationError(... "não é válido" ...)`; `if self.empresa_id is None: return` separado, antes do `try` |
| **BL-271** (rodada 6) | **PRESENTE NA `main`** | `test_dl023_…:357-428` — **dois** testes (`test_query_recursiva_usa_coluna_de_conta_pai_do_meta_de_verdade` e `…_do_item_do_meta_de_verdade`), cada um com `mock.patch.object(campo, "column", "coluna_que_nao_existe")` dentro de `transaction.atomic()` (savepoint), `pytest.raises(ProgrammingError)`, controle positivo fora do patch |
| **BL-272** (rodada 6) | **PRESENTE NA `main`** | `test_dl023_…:469-530` — docstring do teste de admin qualifica o alcance ("fora do admin … continua provado"); asserta positiva `"empresa" in form.errors` substituiu a asserta negativa `"fronteira de isolamento" not in corpo`; o comentário que explica a AUSÊNCIA da mensagem do modelo deixou de ser asserta |

**Não medi, e o Fred declarou que não é desta etapa:** comportamento dinâmico
das correções (execução dos 1279 testes que o implementador declarou verdes).
A suíte, quando rodou em árvore limpa no ambiente do implementador
(`f23484a` declara `1279 passed in 39.5s`), é a **prova dinâmica** registrada
no commit — não a repito aqui.

---

## 2. Achados no produto

**Nenhum achado novo de produto.** As correções das duas rodadas estão no
código de `main` na forma declarada por cada commit, e os testes
correspondentes estão no arquivo de teste na forma declarada por cada commit.
A única observação é de **forma**, não de conteúdo:

### O1 — INFORMATIVO — A integração aconteceu por uma rota **indireta** (rodada 6 trouxe a rodada 4 consigo), e isso é importante registrar porque o relatório da rodada 5 dizia o contrário

A rodada 5, em A2, registrou textualmente:

> "`edae1bf` tem **2** `check-runs`, não 3: o guarda 'Regras do projeto'
> **não rodou** nesta revisão, e as correções da rodada 4 **não estão na
> `main`**."

O A2 da rodada 5 estava **correto na data em que foi escrito**: na hora da
auditoria focada, `edae1bf` tinha acabado de ser empurrado para a branch de
trabalho (sem PR), `a612604` era o head da `main` via PR #24, e a rodada 6
ainda não existia. Depois veio PR #25 (docs), depois a rodada 6 (`f23484a`),
depois PR #26 (`72a2215`) — e este último, ao mesclar a branch de rodada 6 de
volta à `main`, trouxe `edae1bf` no segundo pai do commit de merge interno
`19efc16`. O **efeito final** é que `edae1bf` está na `main`, e o **mecanismo
"Regras do projeto"** rodou em PR #26 (não em PR #24), mas rodou.

Informo, não classifico: a documentação do estado do projeto na
`docs/agents/estado.md` deve refletir que **a correção entrou na `main` por
PR #26** (rodadas 4+6 em conjunto), não por PR #24 (que levou só a rodada 3).
Já está correto no `estado.md` atual (grep "PR #26" confirma), mas vale
registrar para a próxima pessoa que procurar o "PR da rodada 4" e não
encontrar — o PR da rodada 4 foi PR #26, com escopo "rodadas 4 e 6".

---

## 3. Achados no mecanismo e na integração

### A1 — PRESENTE — Os **três** workflows exigidos pelo AGENTS.md estão configurados para rodar em `pull_request`

`.github/workflows/` lista três arquivos, todos com gatilho `pull_request`:

| Workflow | Job (nome) | Dispara em | Verificação rápida |
| --- | --- | --- | --- |
| `backend.yml` | `Lint e testes` | `on: push / pull_request / workflow_dispatch` (`backend.yml:5`) | OK — `pull_request` listado |
| `documentation.yml` | `Validar documentação` | `on: push / pull_request / workflow_dispatch` (`documentation.yml:5`) | OK — `pull_request` listado |
| `regras-do-projeto.yml` | `Regras do projeto` | `on: pull_request: types: [opened, edited, synchronize, reopened]` (`regras-do-projeto.yml:11-13`) | OK — gatilho **só** em `pull_request` (não em `push` direto) |

Os três mecanismos impostos tecnicamente pelo AGENTS.md e listados no plano
DL-014 estão **configurados** para rodar em PR — o que **fecha** a observação
do A2 da rodada 5, **condicionado à execução real no PR efetivo**. A
execução efetiva é da rodada 7 completa, fora do escopo desta.

### A2 — PRESENTE — A correção da rodada 4 (`edae1bf`) **entrou na `main`** por PR #26, depois da rodada 5

Já detalhado em O1. Em uma linha: `git merge-base --is-ancestor edae1bf
1b828e7` retorna exit 0, e o caminho até lá é por `19efc16` → `72a2215` →
`1b828e7`. **Não é regressão** (as correções estão lá), é só **rota
indireta**.

### A3 — INFORMATIVO — A correção da rodada 6 (`f23484a`) entrou na `main` **diretamente** por PR #26

`git merge-base --is-ancestor f23484a 1b828e7` retorna exit 0, e
`f23484a` é **o próprio segundo pai** de `72a2215` (Merge PR #26). Este é o
caminho natural: a correção da rodada 6 entrou na `main` com todos os
mecanismos do AGENTS.md acionados, e é a forma como deveria ter sido
desde o início.

---

## 4. Verificações ponto a ponto pedidas pelo Fred

### 4.1 Os 6 commits de correção existem em `1b828e7`?

O briefing pediu para mostrar `git log --oneline` de `1b828e7` até a base da
DL-023 e identificar `edae1bf` e `f23484a`. A base da DL-023 é o commit
anterior a `96284a4` (a feature que abriu a etapa) — `484fc0c3
"docs(DL-023): incorporar o inventário do admin e registrar BL-244"` é o
antecessor direto. O caminho de `1b828e7` até `484fc0c` (topologicamente, só
passando por ancestrais da DL-023):

```
1b828e7 (Merge PR #27 — DL-018)
└─ 72a2215 (Merge PR #26 — DL-023 rodadas 4+6)
   ├─ bfe9814 (Merge PR #25 — estado pós-DL-023)
   └─ f23484a (fix: rodada 6 — BL-270, BL-271 e BL-272)        ← rodada 6
      ├─ a58a616 (docs: registrar rodada 5 e merge PR #24)
      ├─ 19efc16 (merge: trazer a main para a branch da rodada 4)
      │  ├─ edae1bf (fix: rodada 4 — BL-264, BL-265, BL-266)  ← rodada 4
      │  └─ bfe9814 (Merge PR #25)
      └─ 19efc16...
... (docs da DL-023 e merges anteriores) ...
484fc0c3 (docs DL-023: inventário do admin + BL-244)            ← base da DL-023
```

Ambos `edae1bf` e `f23484a` estão **presentes** como ancestrais de
`1b828e7`. Confirmação numérica: `git merge-base --is-ancestor` retornou
exit 0 para ambos.

### 4.2 Os arquivos alterados pelas rodadas 4 e 6 contêm as correções declaradas?

Sim, conferidos item a item por inspeção do arquivo `1b828e7`:

#### `apps/contabilidade/models.py`

- **BL-264:** bloco do `Conta.clean()` em `if self.pk:` → `if original["empresa_id"] != self.empresa_id:` → busca por `Empresa.objects.filter(pk=self.empresa_id).values_list("escritorio_id", flat=True).first()` (sem `DoesNotExist`).
- **BL-265:** linhas 111-114 de `models.py` — quatro identificadores via `_meta`:

  ```python
  tabela_conta = Conta._meta.db_table
  tabela_item = ItemLancamento._meta.db_table
  coluna_conta_pai = Conta._meta.get_field("conta_pai").column
  coluna_conta_do_item = ItemLancamento._meta.get_field("conta").column
  ```

  Zero literal `"conta_pai_id"` ou `"conta_id"` no SQL — `grep
  "conta_pai_id\|conta_id"` em `models.py` só encontra menções em
  comentário e no nome do atributo do ORM, nenhuma no SQL cru.
- **BL-270:** linhas 321-333:

  ```python
  if self.empresa_id is None:
      return
  try:
      escritorio_novo_id = (
          Empresa.objects.filter(pk=self.empresa_id)
          .values_list("escritorio_id", flat=True)
          .first()
      )
  except (TypeError, ValueError):
      raise ValidationError(
          "Não é possível mudar esta conta: o identificador de "
          "empresa informado não é válido."
      ) from None
  ```

  Três causas, **três mensagens** distintas: tipo inválido, inexistente,
  outro escritório — e a guarda de `None` separada.

#### `apps/contabilidade/admin.py`

**Não foi alterado em nenhuma das duas rodadas** — confirmado por
`git show --stat` em `edae1bf` e `f23484a`, e o `admin.py` da `1b828e7` é o
mesmo que já estava em `8235635` (Merge PR #24). A defesa do `add` /
`conta_pai` continua sendo a BL-267 e a BL-262 (fora do escopo desta
etapa), e o `formfield_for_foreignkey` que restringe o dropdown de
`empresa` continua na forma que o relatório da rodada 5 mediu.

#### `apps/contabilidade/tests/test_dl023_conta_nao_muda_de_empresa_ou_natureza.py`

Vinte funções de teste, conferidas uma a uma. As **mudanças da rodada 4**
estão nas docstrings (mensagem do modelo no corpo, qualificação do
alcance) e nos renomes (`test_query_recursiva_le_nomes_de_coluna_pelo_
meta_nao_por_literal` → dois testes com `mock.patch.object` na rodada 6).
As **mudanças da rodada 6** estão: dois testes com `mock.patch.object`
(`test_query_recursiva_usa_coluna_de_conta_pai_do_meta_de_verdade` e
`test_query_recursiva_usa_coluna_de_conta_do_item_do_meta_de_verdade`);
três testes novos para a BL-270 (parametrizado + controle + None); uma
asserta positiva substitui a asserta negativa no teste de admin de
fronteira de escritório.

### 4.3 Os testes declarados nas mensagens de commit existem?

| Commit | Testes declarados | Presentes em `1b828e7`? |
| --- | --- | --- |
| `edae1bf` (rodada 4) | "Os seis irmãos [do teste que media a camada errada] que passam a asserir a mensagem do modelo no corpo" | Sim — asserções `"lançamento próprio" in corpo`, `"conta filha" in corpo`, `"a natureza" in resposta.content.decode()`, `"o tipo" in resposta.content.decode()` etc., em `test_dl023_…:152-308` |
| `f23484a` (rodada 6) | "**Dois** testes [BL-271] que amarram a consulta ao `_meta` de verdade — `mock.patch.object` na coluna do campo real, dentro de savepoint, exigindo `ProgrammingError`, com controle positivo sem o patch" | Sim — `test_dl023_…:357-402` e `405-428` (dois testes, ambos com `mock.patch.object`, `transaction.atomic()`, `pytest.raises(ProgrammingError)` e controle positivo) |
| `f23484a` (rodada 6) | "**5 testes [BL-270] morrem sem o try/except**" | Sim — `test_full_clean_recusa_empresa_id_de_tipo_invalido_com_mensagem_propria` (parametrizado com `["abc", "1e3", "  ", []]`, **4 instâncias**) + `test_full_clean_tres_causas_de_empresa_invalida_tem_tres_mensagens_distintas` (1) + `test_full_clean_com_empresa_id_none_nao_reclama_de_empresa_inexistente` (1) = **5 testes / 6 instâncias** |
| `f23484a` (rodada 6) | "**1 teste morre sem a guarda de None**" | Sim — `test_full_clean_com_empresa_id_none_nao_reclama_de_empresa_inexistente` morre sem a guarda `if self.empresa_id is None: return` porque a mensagem ERRADA `"a empresa informada não existe"` aparece junto |

### 4.4 Distinção de mensagens e fonte das colunas do SQL

Conferido contra o que estava errado na rodada 5:

**a) O `try/except` para `empresa_id` não-inteiro existe?**

Sim — `models.py:323-333`, o `try` envolve a consulta, `except (TypeError,
ValueError)` levanta `ValidationError("... não é válido ...")` com `from None`
(para não preservar a stack trace da exceção original, que seria ruído).
Mesma forma prescrita pelo auditor no achado P1 da rodada 5.

**b) "Empresa não informada" e "empresa de outro escritório" produzem mensagens diferentes?**

Sim — três mensagens, três causas, dois a dois diferentes:

| Causa | Mensagem em `1b828e7` |
| --- | --- |
| `empresa_id = None` | não há mensagem própria — a guarda `if self.empresa_id is None: return` faz `clean()` **retornar** sem levantar. O `ValidationError` fica a cargo do `clean_fields()` que `full_clean()` chama ANTES de `clean()`, com a mensagem padrão de campo obrigatório |
| `empresa_id = "abc"` / `"1e3"` / `"  "` / `[]` | "Não é possível mudar esta conta: o identificador de empresa informado não é válido." |
| `empresa_id = 999999` (id inexistente) | "Não é possível mudar esta conta: a empresa informada não existe. Confira o identificador enviado." |
| `empresa_id = id real de outro escritório` | "Não é possível mudar esta conta para uma empresa de outro escritório: contas não atravessam a fronteira de isolamento entre escritórios pelo cadastro comum." |

Conferência automática por `test_full_clean_tres_causas_de_empresa_
invalida_tem_tres_mensagens_distintas`: o teste monta os três casos,
coleta as três mensagens em um `set`, e afirma `len(mensagens) == 3`. A
presença do teste em `1b828e7` (`test_dl023_…:661-689`) é a prova.

**c) Os nomes de coluna vêm de `_meta.get_field(...).column`?**

Sim — `models.py:113-114`:

```python
coluna_conta_pai = Conta._meta.get_field("conta_pai").column
coluna_conta_do_item = ItemLancamento._meta.get_field("conta").column
```

Nenhum literal `"conta_pai_id"` nem `"conta_id"` dentro do `cursor.execute`
em `models.py`. `grep -n "conta_pai_id\|conta_id"` em `models.py` lista
quatro ocorrências — duas no comentário (`:96`, `:97`), duas no nome do
atributo da FK (`models.conta_pai.column` em si, não no SQL). **Nenhuma
no SQL cru.**

### 4.5 Os três workflows exigidos pelo AGENTS.md rodam em `pull_request`?

Sim — todos os três, conferidos por inspeção dos YAMLs em
`.github/workflows/`:

| Workflow | Job | `on:` | OK? |
| --- | --- | --- | --- |
| `backend.yml` | `Lint e testes` | `push / pull_request / workflow_dispatch` | ✅ |
| `documentation.yml` | `Validar documentação` | `push / pull_request / workflow_dispatch` | ✅ |
| `regras-do-projeto.yml` | `Regras do projeto` | `pull_request: [opened, edited, synchronize, reopened]` | ✅ |

A execução efetiva em PR real é da rodada 7 completa, fora do escopo desta
verificação.

---

## 5. O que eu **não** verifiquei

1. **Execução da suíte `pytest`.** Sem PostgreSQL neste ambiente, e por
   decisão declarada do Fred — **toda a validação dinâmica é da rodada 7
   completa, via CI**. O número `1279 passed in 39.5s` que o implementador
   declarou em `f23484a` é aceito aqui como declaração, não verificado por
   mim; a presença dos testes é verificada, a passagem deles não.
2. **`scripts/validate-docs.ps1`** — não rodável (depende de `pwsh`, e este
   relatório não é um documento a ser validado pelo script; é um relatório
   de auditoria). A validação automática da documentação é da rodada 7
   completa, via CI.
3. **`manage.py check` / `ruff check` / `ruff format --check`** — não
   rodados por decisão de escopo desta rodada. O implementador declarou
   `ruff check 0`, `ruff format --check 162 arquivos`, `manage.py check`
   limpo, `makemigrations --check` sem pendências em ambos os commits. Os
   números conferem entre si e com a revisão anterior; **não reexecutei**.
4. **Comportamento real do `try/except (TypeError, ValueError)`** — o
   ambiente não tem Postgres nem SQLite preparado. O teste
   `test_full_clean_recusa_empresa_id_de_tipo_invalido_com_mensagem_
   propria` existe e está parametrizado; a execução é da rodada 7 completa.
5. **Custo da consulta recursiva** — idem; a medição anterior (5/5/6/4
   consultas em `full_clean()`) é do relatório da rodada 5, e a mudança da
   rodada 6 não toca a consulta.
6. **Mudança em modelos de dados posterior às rodadas** — `migrations` é da
   rodada 7 completa.
7. **Correção contábil e legal.** Auditoria de software não substitui
   validação profissional das regras contábeis e legais, e nada aqui afirma
   conformidade legal nem ausência total de defeitos.

---

## 6. Arquivos relevantes

- `apps/contabilidade/models.py` — `:111-114` (consulta recursiva com `_meta`),
  `:226-352` (guards de empresa/escritório em `Conta.clean()`: guarda de
  `None` em `:321-322`, `try/except (TypeError, ValueError)` em `:323-333`,
  mensagens próprias em `:334-344`, regra de movimento/filhas em `:345-352`)
- `apps/contabilidade/admin.py` — **inalterado** pelas rodadas 4 e 6
- `apps/contabilidade/tests/test_dl023_conta_nao_muda_de_empresa_ou_natureza.py`
  — `:152-308` (6 irmãos da BL-266), `:357-428` (2 testes da BL-271), `:469-530`
  (teste de admin com asserção positiva da BL-272), `:583-623` (2 testes da
  BL-264), `:641-712` (3 testes da BL-270: tipo inválido, controle de 3
  causas, `None`)
- `.github/workflows/backend.yml` — `on: push / pull_request / workflow_dispatch`
- `.github/workflows/documentation.yml` — `on: push / pull_request / workflow_dispatch`
- `.github/workflows/regras-do-projeto.yml` — `on: pull_request: [opened, edited, synchronize, reopened]`
- `docs/projeto/backlog.md` — `:804-806` (BL-270, BL-271, BL-272 marcados
  como "corrigido na rodada 6"); `:829-831` (BL-264, BL-265, BL-266 marcados
  como "corrigir na rodada 4")

---

## 7. Parecer

# PRESENÇA INTEGRADA — código, testes e mecanismos de CI verificados por inspeção estática; execução dinâmica é da rodada 7 completa, via CI

Item por item, escopo da rodada: **BL-264, BL-265, BL-266 (rodada 4) e
BL-270, BL-271, BL-272 (rodada 6) estão presentes na `main` em `1b828e7`,
na forma declarada pelos commits correspondentes, e os três mecanismos
impostos pelo AGENTS.md estão configurados para rodar em `pull_request`.**

A integração aconteceu por **rota indireta** para a rodada 4 (entrou via
PR #26, que mesclou a branch de rodada 6 que já continha o merge interno
`19efc16` trazendo `edae1bf`) e por **rota direta** para a rodada 6
(`f23484a` é o segundo pai de `72a2215`, Merge PR #26). O caminho é
indireto para a rodada 4, mas o resultado é o mesmo: o conteúdo das duas
rodadas está no merge que a `main` aponta, sem divergência de bytes no
`models.py` e no arquivo de teste. Isso fecha o A2 do relatório da
rodada 5 — **com a observação de que os 3 mecanismos rodaram em PR #26,
não em PR #24** (que levou só a rodada 3).

As ressalvas que permanecem — **todas declaradas, nenhuma escondida**:

1. **Execução dinâmica não verificada** (escopo do Fred). A presença dos
   testes está conferida; a passagem deles, não. Em particular, o BL-270
   declara que 5 testes morrem sem o `try/except` e 1 morre sem a guarda
   de `None`; o BL-271 declara que 2 testes morrem com a reversão da BL-265
   a literal; o BL-272 é mudança de asserção e docstring, não tem contagem
   de mutação. **Todas são afirmações do implementador, não medições
   minhas nesta rodada.**
2. **Rota indireta da rodada 4** (O1). Não é defeito; é o que aconteceu.
   Registrar para que a busca pelo "PR da rodada 4" leve ao PR #26 (escopo
   "rodadas 4 e 6"), e não ao PR #24 (escopo "rodada 3 + ressalvas").
3. **Conflito potencial de `try/except (TypeError, ValueError)` com
   `psycopg`** — não verificável sem BD. Em tese, o `psycopg2` também
   levanta `ValueError` para entradas que não consegue serializar (ex.:
   UUID em coluna inteira, datas malformadas); o `try` aqui pega **antes**
   do `psycopg`, no `QuerySet.filter(pk=...)`, que constrói o SQL e
   valida o tipo de `pk` no lado do ORM. **Confirmar isso requer a suíte
   rodando** — fica para a rodada 7 completa.

**Pronto para levar ao Fred como encerramento da rodada 7?** Sim, dentro do
escopo declarado (presença de código, presença de teste, configuração de
CI). **A validação dinâmica continua sendo da rodada 7 completa**, e este
relatório não substitui a passagem da suíte.

**Encaminhamentos:** nenhum para `desenvolvedor-pleno`; nenhum para
`arquiteto-senior`. **A próxima ação da DL-023 é a validação dinâmica da
`main` na CI**, fora do escopo desta rodada.

**Não declaro ausência de defeitos, segurança absoluta nem conformidade
legal**, e a decisão contábil da BL-261 continua sendo do Fred, não minha.