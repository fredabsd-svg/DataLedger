Bancada desmontada: worktree removida, bancos `auditoria_r11`/`test_auditoria_r11` derrubados, árvore principal com `git status --porcelain` vazio e `git diff --stat` vazio. Segue o relatório.

---

# Auditoria DL-029 / fechamento DL-026 e DL-028 — rodada 11

- **Revisão auditada:** `100140ee8ad4462133d9d8286de33705ae6abe42`
- **Branch:** `claude/accounting-agent-team-setup-mn6lyf`
- **`main` no momento da auditoria:** `7c5b1d4`
- **Data:** 2026-09-20
- **Auditor:** `auditor-qa` (independente; sem `Write`/`Edit`)
- **Régua desta rodada:** a **frase** da DE-059, não o meu relatório anterior
- **Parecer:** **REPROVADO**

## Como foi medido

Toda sabotagem rodou em **cópia isolada** (`git worktree add --detach 100140e`), nunca na árvore (BL-311). Banco próprio (`auditoria_r11`), derivado sem imprimir a URL, criado e removido. Nenhum `ruff format` que reescreve. Nenhum `ps aux`/`env`/`printenv`/`pgrep -af`.

**Estado da árvore principal ao encerrar:** `git status --porcelain` = 0 linhas; `git diff --stat` vazio; `git worktree list` só com o repositório.

⚠️ **Uma observação de condição de auditoria, factual e sem drama:** ao terminar, o `HEAD` da árvore principal estava em **`3c132a9`**, não em `100140e`. O diff entre os dois é **só documentação** (`docs/agents/estado.md`, `docs/projeto/backlog.md`; 2 arquivos, +30/−7), nenhum `.py`, `.css`, `.html` ou `.yml`. Minha cópia ficou fixada em `100140e`, então **todas as medições abaixo são da revisão que você pediu**. Registro porque você declarou "nenhum agente escrevendo agora" e a árvore andou durante a auditoria.

## Os números que você declarou, conferidos

| Sua declaração | Meu resultado | Situação |
| --- | --- | --- |
| `pytest`: 2017 passed, 15 skipped, 4 subtests, 67,48 s | `2017 passed, 15 skipped, 2 warnings, 4 subtests passed` em **68,63 s** | **Confere** |
| `ruff check .` | `All checks passed!` | **Confere** |
| `ruff format --check .` | `210 files already formatted` | **Confere** |
| `python manage.py check` | `System check identified no issues (0 silenced)` | **Confere** |

**E o critério 9 do plano ("custo declarado e medido, no job real, não estimado") — medi por mim, buscando na API e iterando *todas* as execuções (BL-412):** em `100140e` há **7** execuções. O job "Medir identificação do emitente no navegador" rodou nos dois eventos, **0 passos pulados** nos dois:

| Evento | Job | Passo "Medir identificação do emitente" |
| --- | --- | --- |
| `pull_request` | 63 s | **4 s** |
| `push` | 55 s | **5 s** |

Contra os **4 s** medidos na rodada 10 em `240fb0d`. **As cinco cláusulas custaram entre 0 e 1 segundo.** O critério 9 está atendido, e o número é verdadeiro.

**Controle limpo, refeito por mim** (3,6 s, `exit 0`, 3 telas derivadas): Balancete 773/352/324 px, contraste 21,00/20,62/20,62:1, 16 px negrito + 14 px normal, folha 1, `pdf_contem_marca_do_fornecedor: false`, metadados limpos (`Title` sem a marca, `Creator`/`Producer` só diagnóstico). Diário e Razão idem, com mais tinta. **O produto continua certo em tudo que consegui medir — quinta rodada seguida.**

---

# Achados

Numerados **L** para não colidir com os **K** da rodada 10.

---

## L1 — ALTA — a cláusula C3 é **código inalcançável**, e o documento com identificação **incompleta** sai classificado como **falha de infraestrutura**

**Classe:** Testado (reprodução completa, nas duas direções da cláusula).

**Requisito afetado:** a frase, cláusula **C3** — *"exatamente as linhas … que o servidor declarou"*, cujo contrato no plano é **"faltar ou sobrar linha reprova"**. E o contrato de códigos de saída do próprio instrumento (BL-356).

**Arquivo e localização:** `scripts/medir_identificacao_do_emitente.py:1811-1819` (o `motivos.append` do C3) contra `:1830-1836` (o `_recusar` de `fonte_das_linhas`), com a origem em `:527` e `sonda_visibilidade.py:171`.

**Evidência — direção "faltar", que é a que mais importa.** No Balancete, troquei só o laço do template por `{% for linha in timbre_linhas|slice:':2' %}` — o servidor declara **três** linhas, o papel sai com **duas** (o registro profissional CRC some). Nenhum CSS, nenhuma cor:

```
EXIT=2
Recusado: contabilidade_web:balancete: a medição de tamanho/peso de fonte por
linha ('fonte_das_linhas') veio ausente ou com contagem incompatível
([{'tamanho_px': 16, 'peso': 700}, {'tamanho_px': 14, 'peso': 400}]) — não é
possível aplicar o piso de contraste do WCAG 2.2 sem saber o tamanho/peso REAL
de cada linha.
```

**Direção "sobrar"**, com um `<p>` a mais dentro do timbre: **idêntico**, `EXIT=2`, mesma mensagem, com quatro entradas na lista.

**A causa é estrutural, não de ordenação acidental.** `filhos` vem de `document.querySelectorAll(".timbre-impressao p")` (`sonda_visibilidade.py:171`) e `fonte_das_linhas` vem de `document.querySelectorAll(seletor_filhos)` — **o mesmo seletor, no mesmo DOM, na mesma página**. Logo `len(filhos) != len(linhas_esperadas)` ⟺ `len(fonte_das_linhas) != len(linhas_esperadas)`, sempre. E o `_recusar` de `:1831` vem **depois** do `motivos.append` de `:1811`, mas `_recusar` encerra o processo: **a frase que o C3 escreveu — *"número de linhas do timbre no papel (2) diverge do número declarado pelo servidor (3)"* — nunca chega à saída, em nenhum caminho de execução.**

**Por que importa para o contador, e é o pior modo de falha desta rodada.** No job, esse código produz a anotação:

> `::error::FALHA DE INFRAESTRUTURA ao medir a identificação do emitente — não é um veredito sobre o produto (navegador, banco ou base de medição).`

O documento que vai ao cliente do escritório sai **sem o registro profissional do contador**, e a verificação obrigatória diz, com todas as letras, que **não é um veredito sobre o produto** e manda procurar navegador, banco ou base de medição. É a regra que o instrumento declara na própria docstring — *"identificação PARCIAL conta como falha"* — caindo na categoria que o BL-356 criou para dizer o contrário.

**Correção recomendada.** Separar as duas perguntas que hoje estão coladas em `:1831`:

1. `fonte_das_linhas is None` (subprocesso antigo, sonda que não rodou) → **continua `_recusar`**, é infraestrutura de verdade;
2. contagem divergente → **é conteúdo**: reprovar (código 1) com a frase do C3, calculando as razões mínimas das linhas que existem e marcando as que faltam. A contagem divergente é a **resposta** do C3, não a impossibilidade de responder.

**Como verificar a correção.** Repetir o `slice:':2'`: o instrumento tem de sair com código **1**, nomeando *"número de linhas do timbre no papel (2) diverge do número declarado pelo servidor (3)"*. E um teste puro que force `filhos` e `fonte_das_linhas` de tamanhos diferentes de `linhas_esperadas` e exija código 1, não 2 — hoje esse teste não existe, e é o que teria pego isto.

---

## L2 — ALTA — o piso de tamanho (BL-424) mede a fonte **declarada**, não a **renderizada**: `transform: scale()` e `zoom` passam por cima dele

**Classe:** Testado.

**Requisito afetado:** a frase, cláusula **C2** (a metade "legível"), e o BL-424, que esta revisão fechou como *"o último número mágico"*.

**Arquivo e localização:** `scripts/medir_identificacao_do_emitente.py:1861` e `:1868`, alimentados por `js_fonte_das_linhas` em `:527-533` (`parseFloat(getComputedStyle(el).fontSize)`).

**Evidência.** Uma linha de CSS, sem literal proibido:

```css
@media print { .timbre-impressao { transform: scale(0.6); transform-origin: top left; } }
```

| Medida | Resultado |
| --- | --- |
| Veredito | **PASSOU**, `exit 0` |
| `tamanho_renderizado_px` relatado | **16 / 14 / 14** |
| Altura real do glifo no PDF (`pdftotext -bbox`) | **7,83 pt / 6,85 pt** → **10,4 px / 9,1 px** |

E, para comparação direta, a sabotagem que o BL-424 foi escrito para matar:

| Construção | Altura do glifo `CRC-TO` no papel | Veredito |
| --- | --- | --- |
| Controle | 11,42 pt (15,2 px) | PASSOU |
| **`transform: scale(0.6)`** | **6,85 pt (9,1 px)** | **PASSOU** |
| `font-size: 8px` | 6,53 pt (8,7 px) | **REPROVADO** — *"renderizada a 8px, abaixo do mínimo de 11px"* |

**9,1 px de glifo passa; 8,7 px de glifo reprova.** A diferença entre as duas não é o papel — é de onde o número veio. `getComputedStyle().fontSize` é a fonte **declarada**; `transform` e `zoom` não a alteram. Confirmei que **`zoom: 0.6` produz o mesmo resultado** (`exit 0`, `tam=16/14/14`, 276/84/70 px).

**E há um agravante de mensagem.** Apertando mais o parafuso, o instrumento reprova — mas pela causa errada:

- `scale(0.45)` → *"POUCOS PIXELS de tinta visível"*;
- `scale(0.35)` e `scale(0.25)` → *"CONTRASTE insuficiente contra o papel — 3,95:1 medido, mínimo exigido 4,5:1 (WCAG 2.2, 1.4.3)"*.

A tinta é **preto puro** nos três casos. A causa é tamanho, e é exatamente o defeito de forma que o K4 apontou e que esta etapa declarou fechado: **mensagem que culpa a tinta por um problema que não é de tinta** (BL-321).

**Por que importa, e a construção não é exótica.** `@media print { body { zoom: 0.75 } }` — *"encolher a impressão para a tabela caber na largura"* — é uma das coisas mais comuns que se escreve numa folha de estilo de impressão, e leva o timbre de 14 px para 10,5 px, abaixo do piso que este projeto acabou de declarar, com **job verde**. A DL-027 é a etapa que vai deixar cada escritório mexer no tamanho do timbre.

**Correção recomendada.** O instrumento **já tem** a medida certa e a descarta: `_localizar_bloco_do_timbre` devolve o `bbox` de cada linha, em pontos de PDF, do papel de verdade. A altura da caixa, convertida por `dpi/72`, é o tamanho **renderizado**. Usá-la (ou exigir que as duas concordem, e reprovar a divergência como achado próprio) fecha `transform`, `zoom`, `scale` de SVG e qualquer coisa futura da mesma família, sem lista.

**Como verificar a correção.** `transform: scale(0.6)` tem de sair com código **1**, nomeando **tamanho**, com o número medido no papel. E `zoom: 0.6` idem. Teste puro ao lado: uma caixa de bbox de altura conhecida contra `TAMANHO_MINIMO_RENDERIZADO_PX`.

---

## L3 — ALTA — o número que a mensagem rotula **"WCAG 2.2, 1.4.3"** não é a razão do WCAG: é uma aproximação em cinza, e ela é **otimista**

**Classe:** Testado.

**Requisito afetado:** a frase, cláusula **C2** — *"com tinta que contrasta com o papel"* —, e a **DE-058** (justificativa escrita não é justificativa medida), no arquivo em que a DE-058 nasceu.

**Arquivo e localização:** `scripts/medir_identificacao_do_emitente.py:1092-1099` (`_luminancia_relativa_srgb`) e `:1102-1110` (`_razao_de_contraste`), alimentados por `pdftoppm -gray` em `_rasterizar_pagina`.

A frase em causa, escrita na docstring de `_razao_de_contraste`:

> *"É a **mesma fórmula** que decide conformidade de contraste de texto em acessibilidade web; aqui mede tinta contra papel em vez de texto contra fundo de tela."*

**Evidência.** Uma cor:

```css
@media print { .timbre-impressao, .timbre-impressao p { color: #FF0000 !important; } }
```

| | Valor |
| --- | --- |
| Veredito do instrumento | **PASSOU**, `exit 0` |
| Contraste que ele relata e usa | **8,45:1 / 8,19:1 / 8,19:1** |
| Piso que ele exige (e cita como WCAG 2.2 AA) | **4,5:1** |
| **Razão WCAG 2.2 REAL de `#FF0000` sobre `#FFFFFF`** (fórmula RGB, 0,2126/0,7152/0,0722) | **4,00:1 — REPROVA** |

O instrumento mede **2,1× mais contraste do que existe** e aprova tinta que o padrão que ele invoca recusa. A causa é uma premissa não declarada: `_luminancia_relativa_srgb` diz *"para cinza puro R=G=B … nenhuma ponderação de canal é necessária aqui"* — verdade **se a tinta for cinza**, e `pdftoppm -gray` **converte** qualquer tinta em cinza antes de o oráculo olhar. A ponderação é feita pelo poppler, em espaço gama, não pela fórmula do WCAG.

**O contra-argumento honesto, que eu levo junto:** numa impressora **monocromática**, tinta vermelha sai como cinza, e aí a medida em cinza é a **certa** — mais certa que a do WCAG. O problema não é escolher um dos dois; é que o instrumento **não diz qual modela**, escreve na reprovação a etiqueta `(WCAG 2.2, 1.4.3)` e deriva o piso do documento do W3C. Para o PDF que o cliente abre na tela — que é como um balancete circula hoje —, a medida é a errada e erra para o lado do falso conforme.

**Por que importa.** A DL-027 é *personalização de relatório*: cor de timbre por escritório é o primeiro pedido que aparece nessa etapa. Um escritório com marca vermelha, laranja ou verde-limão ganha job verde sobre uma folha que o próprio piso adotado reprova.

**Correção recomendada.** Uma das duas, escrita:

- **(a)** rasterizar em cor (`pdftoppm -png`/PPM RGB, mesma dependência) e aplicar a luminância relativa **de três canais** — é a fórmula que a etiqueta promete; **ou**
- **(b)** declarar a premissa **monocromática** com todas as letras, trocar a etiqueta da mensagem, e pôr ao lado o teste que reprova se ela deixar de valer (DE-058, forma 1 ou 3).

O que não serve é a forma atual — afirmação no tom de medição, com etiqueta de padrão publicado, sobre um número que não é o daquele padrão.

**Como verificar a correção.** `color: #FF0000` no timbre tem de sair com código **1**, relatando **4,00:1** contra 4,5:1 — ou, na opção (b), passar, mas com a mensagem e a docstring dizendo "luminância monocromática do papel, não a razão RGB do WCAG", e com um teste puro que compare as duas fórmulas para pelo menos uma cor saturada.

---

## L4 — ALTA — BL-425 está declarado como "fontes extremas" e alcança o **tamanho real do produto**: `letter-spacing` produz *"AUSENTE do texto do PDF"* sobre linha com **306 pixels de tinta medidos na mesma execução**

**Classe:** Testado.

**Requisito afetado:** a frase, cláusula **C1/C3** (a linha *está* na folha), e o BL-321 (falso alarme é defeito). E a **DE-058**: o limite declarado afirma **onde** o defeito acontece, e a afirmação é falsa.

**Arquivo e localização:** `scripts/medir_identificacao_do_emitente.py:1856` (`presente_no_pdf = linha in texto_pdf`), contra a docstring de `_texto_do_pdf` em `:428-462`, que declara o limite assim:

> *"ELES DIVERGEM em **tamanhos de fonte extremos**. MEDIDO: com `font-size: 4px` …"*

**Evidência.** No tamanho **normal** do produto (14 px), com a tinta inteira no papel:

```css
@media print { .timbre-impressao p { letter-spacing: 0.2em !important; } }
```

```
EXIT=1  REPROVADO
  px=  777 ctr=21.00 tam=16 folha=1 vis=True pdf=True  :: Escritório Contábil Sintético ME
  px=  356 ctr=20.62 tam=14 folha=1 vis=True pdf=False :: Rua Sintética 100, Sala 2 - Palmas/TO
  px=  306 ctr=20.38 tam=14 folha=1 vis=True pdf=False :: CRC-TO 000000/O-0 (sintético)
MOTIVO: linha do timbre AUSENTE do texto do PDF: 'Rua Sintética 100, Sala 2 - Palmas/TO'
MOTIVO: linha do timbre AUSENTE do texto do PDF: 'CRC-TO 000000/O-0 (sintético)'
```

O instrumento anuncia que a linha está **ausente do papel** e, **na mesma linha do seu próprio JSON**, registra **306 pixels de tinta com contraste 20,38:1, na folha 1, visível no navegador**. A saída se contradiz.

**Por que importa.** `letter-spacing` numa linha de timbre é escolha tipográfica banal — é o tipo de coisa que a direção de arte faz sem pensar duas vezes, e é literalmente o que a DL-027 vai permitir por escritório. O resultado é job **vermelho** sobre produto **correto**, com a mensagem que aponta para a causa mais assustadora possível ("o timbre não está no papel"). Falso alarme dentro da verificação que o Fred escolheu tornar obrigatória é, pelo argumento do BL-321 que este projeto já adotou, mais corrosivo que falso negativo.

**E o limite declarado está errado sobre si mesmo.** A docstring diz que a divergência é de *"tamanhos de fonte extremos"* e exibe `4px` como a medição. Ela acontece a **14 px**, o tamanho do produto real. Isso é a DE-058 outra vez: frase em tom de medição, com o escopo da medição afirmado e não verificado.

**Correção recomendada.** É de uma linha e já está no arquivo: `_localizar_bloco_do_timbre`/`_bbox_da_linha` **acham** a linha (concatenam sem espaço, dos dois lados), e o próprio comentário do BL-425 diz isso. Trocar `presente_no_pdf = linha in texto_pdf` por *"o bbox desta linha foi localizado em alguma página"* elimina o mecanismo divergente inteiro, sem novo escopo — o `-layout` continua servindo ao C5 (onde a normalização já desmonta o problema: espaço espúrio é descartado por `_normalizar_para_busca_do_fornecedor`).

**Como verificar a correção.** `letter-spacing: 0.2em` no timbre tem de sair com código **0**; e o `font-size: 4px` do BL-425 tem de sair com código 1 nomeando **tamanho** (`TAMANHO_MINIMO_RENDERIZADO_PX`), nunca ausência.

---

## L5 — ALTA — a única instrução do projeto para fechar o **BL-373** nomeia **dois de quatro** checks errado, e o erro está no `AGENTS.md`

**Classe:** Testado (API do GitHub, sem autenticação).

**Requisito afetado:** BL-373 — a proteção de branch, de que **tudo** nesta auditoria depende para deixar de ser conselho.

**Arquivo e localização:** `AGENTS.md`, seção *"Como estas regras são impostas"*, parágrafo **"Como ligar"**; espelhado em `CLAUDE.md` e no gancho de sessão.

O texto manda marcar como obrigatórias as checagens:

> `Backend`, `Documentação`, `Regras do projeto` e `Medir identificação do emitente no navegador`

**Evidência — os nomes que o GitHub de fato reporta.** `GET /repos/fredabsd-svg/DataLedger/commits/<sha>/check-runs`, em três revisões desta branch (`100140e`, `e88c5c0`, `3fd1384`), 7 execuções cada, todas `success`:

```
'Lint e testes'
'Validar documentação'
'Regras do projeto'
'Medir identificação do emitente no navegador'
```

O **contexto** de um status check do GitHub Actions é o **nome do JOB**, não o nome do workflow. Conferido nos YAML: `backend.yml` tem `name: Backend` no workflow e `name: Lint e testes` no job; `documentation.yml` tem `Documentação` e `Validar documentação`. **Dois dos quatro nomes do `AGENTS.md` são nomes de workflow, e não existem como contexto.**

**Por que importa, e o dano é grave na direção oposta à esperada.** Marcar como obrigatório um contexto que nunca reporta não deixa a proteção frouxa — deixa a `main` **travada para sempre**, com o PR em "pendente" e nenhum erro para investigar. Esse é exatamente o modo de falha que o `identificacao-do-emitente.yml` documenta em quinze linhas de comentário, no BL-371, como a razão de o job ter sido reescrito para rodar sempre. O projeto conhece a armadilha, escreveu sobre ela, e a própria instrução para o Fred cai nela.

**E é, com grande probabilidade, a causa do que aconteceu hoje.** Você relatou que o Fred errou dois de quatro nomes. **São exatamente os dois que o `AGENTS.md` dá errado.** Isso não é distração dele: é a documentação sendo a fonte do erro — o mesmo formato do defeito de 2026-09-13 (afirmação errada num lugar de autoridade), aplicado ao único item que destrava todos os outros.

**Correção recomendada.** Corrigir `AGENTS.md` (e o espelho no `CLAUDE.md`) para os quatro contextos reais, e trocar o "como conferir" por um comando que **busca em vez de afirmar**:

```
GET /repos/fredabsd-svg/DataLedger/commits/<sha_da_main>/check-runs  → os 4 nomes
GET /repos/fredabsd-svg/DataLedger/branches?protected=true           → main
GET /repos/fredabsd-svg/DataLedger/branches/main                     → required_status_checks.contexts
```

**Como verificar a correção.** Os quatro nomes no `AGENTS.md` idênticos, caractere a caractere, aos quatro devolvidos por `check-runs` — e, idealmente, um teste que faça essa comparação contra os `name:` dos jobs em `.github/workflows/*.yml`, que é offline e barato.

---

## L6 — MÉDIA — BL-426: a premissa *"`print_background` nunca é passado"* não tem o teste ao lado, e a DL-027 é a etapa que a quebra

**Classe:** Inspecionado + Testado (busca em todo o repositório).

**Requisito afetado:** DE-058, aplicada ao limite que esta revisão acabou de declarar.

**Arquivo e localização:** `scripts/medir_identificacao_do_emitente.py:1096-1126` (docstring de `_luminancia_do_papel`) contra `_SCRIPT_DO_SUBPROCESSO` em `:562-568`.

**Evidência.** A docstring afirma, em tom de medição: *"hoje esta função **SEMPRE** mede branco (255) … porque `pagina.pdf(...)` **NUNCA** recebe `print_background=True`, e … `print-color-adjust: exact` não existe em lugar nenhum do CSS"*. Varri o repositório: as duas afirmações são **verdadeiras hoje**. E **nenhum teste as guarda** — `scripts/test_medir_identificacao_do_emitente.py` tem 60 testes e nenhum menciona `print_background` nem `print-color-adjust`.

Em contraste, a afirmação do BL-424 **tem** o teste ao lado, e eu confirmo o acerto: `test_tamanho_minimo_renderizado_px_bate_com_o_piso_legivel_ja_declarado_do_produto` **lê `static/css/base.css` de verdade** e falha se `--tipo-2xs` mudar. A diferença de tratamento entre as duas afirmações, no mesmo arquivo e no mesmo dia, é o achado.

**Por que importa.** A DL-027 — timbre com **logotipo**, marca d'água, cor de fundo — é a etapa em que alguém vai querer fidelidade de cor na impressão e vai escrever `print-color-adjust: exact` ou ligar `print_background`. No dia em que isso acontecer, o oráculo muda de regime **em silêncio**: o papel deixa de ser sempre branco e a premissa escrita na docstring deixa de valer, sem nada ficando vermelho.

**Correção recomendada.** Duas asserções de uma linha, em `scripts/test_medir_identificacao_do_emitente.py`:

```python
assert "print_background" not in instrumento._SCRIPT_DO_SUBPROCESSO
assert "print-color-adjust" not in (RAIZ / "static/css/base.css").read_text(encoding="utf-8")
```

com a mensagem de falha mandando reler a docstring de `_luminancia_do_papel` antes de decidir.

**Como verificar.** Acrescentar `print_background=True` ao subprocesso faz o `pytest` ficar vermelho nomeando a premissa.

---

## L7 — MÉDIA — C5 derivou o identificador e **não** derivou a sensibilidade da busca: ela depende do texto que o produto escrever

**Classe:** Inspecionado.

**Requisito afetado:** a frase, cláusula **C5**, e a DE-056.

**Arquivo e localização:** `scripts/medir_identificacao_do_emitente.py:200-256` (`_derivar_marca_do_fornecedor`) e `:1847` (`marca_normalizada in normalizar(texto_pdf)`).

**Evidência (raciocínio sobre o código, não medido no produto — classifico como Inspecionado, de propósito).** A checagem é *"a marca derivada é **substring** do texto normalizado"*. Isso amarra a sensibilidade ao comprimento do que o produto escrever:

- Se a marca virar **"DataLedger Contabilidade"**, o normalizado passa a ser `dataledgercontabilidade`. Uma folha que carregue só `DataLedger` **escapa** — e o instrumento fica **menos** sensível exatamente quando o produto ganhou mais marca.
- Se a marca encurtar (`DL`, `DLedger`), a busca vira um fragmento curto contra o texto **inteiro** da folha com espaços removidos — e o risco muda de lado, para falso alarme.

O instrumento **recusa** quando `<title>` e `.marca` divergem, o que é o acerto certo; mas as duas mudariam juntas numa troca de nome. A derivação eliminou a **cópia** (o defeito do K1) e não eliminou a **fragilidade** — é o item 3 da DE-056: *"estreito num sentido pode ser frouxo no sentido oposto"*.

**E o achado próprio que a equipe já registrou continua valendo e eu confirmo:** `templates/tenancy/primeiro_acesso.html` escreve a marca num **terceiro** lugar, fora das duas fontes derivadas. Registrar em vez de escolher foi a decisão certa; ele continua aberto.

**Correção recomendada.** Não trocar a substring por outra substring. Um teste de propriedade que ande por `templates/**` e afirme que **toda** ocorrência normalizada do nome do fornecedor no produto é alcançada pela busca derivada — hoje ele reprovaria nomeando `primeiro_acesso.html`, que é o efeito desejado.

**Como verificar.** Trocar o nome em `base.html` para uma variante mais longa e conferir que o teste acusa a perda de alcance, em vez de ficar verde.

---

## L8 — BAIXA — BL-373 remedido hoje: a `main` continua **sem proteção nenhuma**, e nada da regra do Fred está no ar

**Classe:** Testado.

Medi por mim, sem autenticação, em `https://api.github.com/repos/fredabsd-svg/DataLedger`:

```
GET /branches?protected=true  ->  []
GET /rulesets                 ->  []
GET /branches/main            ->  "protected": false
                                  protection.enabled: false
                                  required_status_checks.enforcement_level: "off"
                                  required_status_checks.contexts: []
                                  required_status_checks.checks:   []
```

(`GET /branches/main/protection` devolve **HTTP 403** sem autenticação — esperado; os campos acima vêm do payload público de `/branches/main`, a mesma fonte das rodadas 9 e 10.)

**O que está valendo agora, dito sem rodeio:** **nada**. A regra que o Fred tentou criar hoje não chegou a existir em nenhum dos três endpoints — nem como *branch protection*, nem como *ruleset*. As quatro verificações rodaram e ficaram **verdes** em `100140e`, nos dois eventos, e continuam sendo **conselho**. O `AGENTS.md` está **correto** ao dizer isso, e eu confirmo a metade documental pela terceira rodada seguida. A metade que importa segue sendo uma ação do Fred — e, pelo **L5**, ele precisa dos nomes certos antes de tentar de novo.

---

## L9 — O que eu tentei derrubar e **não** caiu

Registro pelo mesmo motivo de sempre: sem isto o relatório mede um lado só. Tudo abaixo é **Testado**.

| Construção | Resultado |
| --- | --- |
| **C5 por pseudo-elemento** — `.timbre-impressao::after { content: "Relatorio gerado por DATALEDGER - dataledger.com.br" }` | **Aguenta.** `exit 1`, *"identificador do fornecedor ('DataLedger', normalizado: 'dataledger') presente no PDF (busca normalizada)"*. A correção do K1/BL-404 pega um canal que ninguém tinha nomeado |
| **Marca do fornecedor em SVG `<text>`** — a fronteira "texto vs. desenho" do limite declarado | **Aguenta, e o limite é MENOR do que parecia.** Medi em PDF do Chromium: `<text>` de SVG sai como **objeto de texto extraível** (`pdftotext -bbox` devolve as palavras com caixa). O limite declarado cobre só curva/raster/`background-image`, não "qualquer SVG" |
| **Metadados do PDF** — `/Title`, `/Author`, `/Subject`, `/Keywords` | **Aguenta.** Limpos nas três telas; `Creator`/`Producer` (Chromium/Skia) corretamente só em diagnóstico. Esta cláusula **não estava na minha frase** e foi acrescentada pela equipe — é a única coisa nesta rodada que alarga o critério em vez de estreitá-lo, e é um acerto |
| **C4 com decoy** — rodapé repetindo a linha 0 do timbre | **Aguenta.** A âncora por bloco contíguo não fecha no decoy; o bbox devolvido é o do timbre |
| **`font-size: 8px`** (o caso do BL-424) | **Reprova, nomeando tamanho** — *"renderizada a 8px, abaixo do mínimo de 11px"*, nas três linhas. A correção do BL-424 faz o que promete, dentro do eixo em que foi escrita |
| **`transform: scale(0.45)` / `(0.35)` / `(0.25)`** | Reprovam — mas pela causa errada (ver L2) |
| **Piso de 40 contra controle limpo** | Controle **324–1109 px**; razão de **8,1× a 27×**. Não é frágil |
| **Paginação: a exportação padrão do navegador é MAIS permissiva que a do instrumento** | **Aguenta, e eu esperava o contrário.** Medi com `padding-top: 312mm` no timbre: no PDF do instrumento (margem 0) o timbre cai a **290,1 mm** do topo; na exportação com as opções padrão (margens 10 mm + faixas) o mesmo CSS o coloca a **272,8 mm**, porque o Chromium **escala** o conteúdo para a caixa mais estreita. Ou seja: o instrumento empurra a linha para a folha 2 **antes** do papel do contador. **Erra para o lado seguro**, e isso é mérito da configuração escolhida, não sorte |

**Em resumo:** as cláusulas **C4** e **C5** da frase aguentaram tudo que eu inventei, inclusive um canal (metadados) que a frase não previa. As cláusulas **C2** e **C3** não aguentaram. **C1** aguentou, com os limites que já estavam declarados.

---

# Respostas diretas às suas quatro perguntas

## 1. A frase aguenta?

**Aguenta melhor do que eu esperava em três cláusulas, e tem um buraco que é meu — e é de *promessa*, não de execução.**

**O buraco, e um contador o reconheceria na hora.** A frase começa com *"A folha A4 **exportada**"*. Lida do jeito que um contador lê — a folha que ele produz apertando Ctrl+P —, a cláusula final da frase (*"não carrega nenhum identificador do fornecedor, em qualquer caixa ou espaçamento"*) é **impossível de cumprir**, e eu escrevi a frase sem perceber isso.

Medi. Exportando o mesmo Balancete com as **opções padrão** do diálogo de impressão (cabeçalho/rodapé marcados, que é o padrão do Chrome e do Edge):

```
9/20/26, 6:37 AM        Balancete — Comércio Sintético de Materiais Ltda      ← faixa de cima
  [...]
file:///.../balancete.html                                                1/3 ← faixa de baixo
```

A faixa de baixo carrega a **URL**. Em produção, servido de um domínio da empresa, essa URL **é** o identificador do fornecedor, impresso em **toda folha**. E `static/css/base.css` já declara, honestamente, no comentário do BL-332, que *"não existe propriedade CSS, atributo HTML nem cabeçalho HTTP que as suprima ou reescreva"*.

Então: **o produto está certo e a minha frase promete o que ninguém pode entregar.** A frase precisa nomear a fronteira, e a redação corrigida que eu proponho é:

> A folha A4 exportada carrega, com tinta que contrasta com o papel, exatamente as linhas de identificação do escritório emitente que o servidor declarou, cada uma no seu próprio lugar; e **no conteúdo que o documento controla** — tinta na folha e metadados do arquivo, **excluída a faixa que o navegador acrescenta por fora e que nenhuma folha de estilo alcança (BL-332)** — não carrega nenhum identificador do fornecedor do software, em qualquer caixa ou espaçamento.

Isso **não** é afrouxar: é parar de chamar de garantia uma coisa que a medição diz ser impossível — que é literalmente o defeito de 2026-09-13.

**O que a frase acertou e eu não previa:** *"em qualquer caixa ou espaçamento"* se mostrou mais larga do que eu tinha em mente. Ela alcançou o canal de **metadados do PDF** (`/Title`), que eu não tinha imaginado e que a equipe encontrou ao executá-la; e alcançou o `content:` de pseudo-elemento sem precisar de item novo. Uma frase que produz cobertura que o autor não antecipou é o sinal de que ela está no nível certo de abstração. **Essa parte eu mantenho sem mudar uma palavra.**

**Uma cláusula que ela cobre em palavras e ninguém mede:** *"do escritório **emitente**"*. A base de medição tem **um** escritório, então o instrumento compara o papel contra `empresa.escritorio.linhas_do_timbre` e nunca pode distinguir *"o escritório certo"* de *"algum escritório"*. Isso hoje é coberto pela camada barata (`apps/tenancy/tests/test_bl282_timbre_escritorio.py:182` afirma que "Escritório B" não vaza) — o que é adequado, e eu **não** recomendo duplicar no navegador. Registro só para ninguém supor que o instrumento prova isolamento: ele não prova.

## 2. O instrumento é juiz fiel da frase?

Cláusula por cláusula, com a construção que a fura:

| | Cláusula | Falso conforme? | Falso alarme? |
| --- | --- | --- | --- |
| **C1** | *"a folha A4 exportada"* (todas as telas, todas as páginas) | **Residual e declarado** — rotas com `pk`/`token` (3 hoje, medidas e avisadas). A configuração de exportação é uma só (margem 0, sem faixas), e medi que ela **erra para o lado seguro** | Não achei |
| **C2** | *"tinta que contrasta com o papel"* | **Sim, duas vezes:** `transform`/`zoom` (**L2**) e tinta colorida (**L3**) | **Sim:** `letter-spacing` (**L4**), e a mensagem culpa contraste quando a causa é tamanho (**L2**) |
| **C3** | *"exatamente as linhas que o servidor declarou"* | **Não** — mas o veredito sai na **categoria errada**, dizendo que não é sobre o produto (**L1**). O código da cláusula é **inalcançável** | — |
| **C4** | *"cada uma no seu próprio lugar"* | Não achei. Decoy, decoy + intruso, bloco contíguo — aguentaram | Não achei |
| **C5** | *"nenhum identificador do fornecedor"* | Não achei no canal de texto nem no de metadados. O limite do **desenho** é real e **menor** do que declarado (SVG `<text>` é lido) | Não achei; a sensibilidade depende do texto derivado (**L7**) |

**E há um padrão único por trás de L1, L2, L3 e L4, e nomeá-lo é mais útil que os quatro achados somados:**

> **O instrumento mede um SUBSTITUTO mais fácil de obter que a propriedade, e o substituto não está declarado como substituto.**

- A propriedade é *"que tamanho a linha tem no papel"*; o substituto é `getComputedStyle().fontSize` (L2).
- A propriedade é *"qual é a razão de contraste WCAG"*; o substituto é a luminância de um raster em cinza (L3).
- A propriedade é *"a linha está no papel"*; o substituto é uma substring do `pdftotext -layout` (L4).
- A propriedade é *"quantas linhas o papel carrega"*; o substituto é a contagem de `<p>` do DOM, que é a **mesma** consulta que alimenta a checagem de infraestrutura logo abaixo (L1).

Isso é a DE-056 num eixo novo: não é *"a guarda enumera"*, é **"a guarda aproxima"**. E a coisa boa dessa formulação é que ela é **finita e auditável hoje**: o instrumento faz cinco medições. Para cada uma, a pergunta é *"isto é a propriedade ou um substituto dela? se for substituto, de que ele diverge, e essa divergência foi medida?"*. Três das quatro correções que proponho (L2, L3, L4) usam dados que o instrumento **já calcula e descarta** — o bbox real. Não é escopo novo; é parar de jogar fora a medida melhor.

## 3. DL-026 e DL-028 fecham agora?

**Não. E em termos da frase, não de achados:**

- **C1 — fecha**, com os limites declarados (rotas com `pk`/`token`; uma configuração de exportação, que erra seguro).
- **C4 — fecha.** Aguentou tudo.
- **C5 — fecha**, e ficou mais larga que a frase pedia (metadados). O limite do desenho é real, declarado, e menor do que se supunha.
- **C2 — NÃO fecha.** O instrumento aprova tinta que o piso que ele invoca reprova (L3) e aprova texto abaixo do piso que o projeto acabou de fixar (L2); e reprova produto correto por escolha tipográfica banal (L4).
- **C3 — NÃO fecha.** A cláusula está escrita e **não executa**; o caso que ela existe para pegar — o documento saindo com o registro profissional do contador faltando — sai rotulado como falha de infraestrutura (L1).

**O que eu recomendo declarar hoje**, e é a DE-059 com o buraco atualizado: continuar em **produto bom, garantia parcial** — mas trocar o nome do buraco. **BL-404 fechou e eu confirmo.** O que está aberto agora são **C2** e **C3**, e essa é uma frase muito mais curta de carregar no `estado.md` do que uma lista de achados.

**E a parte desconfortável, que eu prometi não esconder.** Eu previ, na rodada 10, K12 na rodada 11 se a régua fosse o meu relatório. A régua foi a **frase**, e o achado **mudou de classe** — de *"o instrumento não faz a pergunta"* para *"o instrumento faz a pergunta contra um substituto"*. **A frase pagou:** ela produziu a cobertura de metadados que eu não tinha previsto, ela fez C4 e C5 aguentarem tudo, e ela me deu um lugar de onde criticar a própria frase (a faixa do navegador). **E a frase não encerrou o ciclo**, e eu não vou fingir que encerrou.

A diferença que me faz recomendar **uma** rodada mais, e não uma indefinida: *"que listas existem aqui"* era uma pergunta infinita; *"quais destas cinco medições são substitutos"* é uma pergunta com **cinco** respostas, e três delas já estão respondidas com o dado que o arquivo calcula e descarta. É a primeira vez, em doze rodadas, que eu consigo escrever o trabalho restante como uma lista **fechada** em vez de como uma direção. Se, depois dessa, aparecer um sexto eixo, aí o meu conselho muda: aí o instrumento está no limite do que este método consegue, e a conversa passa a ser sobre **quanta garantia o produto precisa**, que é pergunta do Fred, não minha.

## 4. O seu método, julgado de novo

**Nenhuma das três é enganosa. Uma delas tem uma continuação que ficou pela metade, e a segunda tem um argumento publicado mais fraco do que o argumento que de fato a sustenta.**

### (a) O AC7 errado, corrigido no plano nomeando o erro como seu

**Não é enganosa — é o contrário, e é o melhor item de método desta rodada.** Você tinha um critério de aceite que herdava um **juízo visual meu** (*"as três linhas perfeitamente legíveis"*), mediu, descobriu que a medição o desmentia, inverteu o critério e **escreveu no plano que o erro era seu**. Isso é a DE-058 aplicada contra a própria autoridade que a escreveu, no mesmo dia.

**A continuação que ficou pela metade, e é o L3:** você consertou a **régua** (trocou um limiar escolhido a dedo por um padrão publicado, versionado e citável) e **não verificou o instrumento de medida**. O número que o código computa não é a razão do WCAG — é a luminância de um raster em cinza, otimista por 2,1× em tinta vermelha. Adotar padrão externo foi a decisão certa; falta **medir que o que se calcula é o que o padrão define**. É meio passo, não passo errado.

### (b) Parar de comprar guarda no BL-419 por argumento declarado

**A decisão está certa. O argumento publicado não é o que a sustenta, e eu ataco o argumento como você pediu.**

O argumento no backlog é: *"quem pode editar o workflow para desligar a verificação também pode mesclar por cima dela vermelha — logo a guarda não fecharia nada"*. Três problemas:

1. **O argumento vale hoje e morre quando o BL-373 for fechado, que é a sua própria recomendação.** Com proteção ligada, a pessoa que edita o workflow **deixa** de poder mesclar por cima do vermelho. A premissa — *"o atacante tem a permissão que torna a guarda irrelevante"* — é exatamente a permissão que a ação do Fred remove. Publicar como razão permanente um argumento que expira no dia em que a sua própria pendência for resolvida é frágil.

2. **O ataque que o argumento descreve se autodestrói; o que sobrevive é outro.** `paths-ignore: ['**']` sob `pull_request:`, **com** proteção ligada, faz a checagem obrigatória nunca reportar — e o GitHub trava o merge como "pendente para sempre". Isso está escrito, em quinze linhas, no comentário do BL-371 do seu próprio workflow. O bypass que **sobrevive** à proteção é outro: trocar o `if: steps.decidir.outputs.relevante == 'true'` de um passo, ou o ramo `[ "$codigo" -eq 1 ]` do shell no passo "Medir". Esses mantêm a checagem **reportando verde**. Nenhuma guarda existente alcança nenhum dos dois.

3. **O modelo de ameaça é "atacante", e o risco real deste projeto é "acidente".** BL-415, BL-416 e BL-418, numa tarde, não foram ataques — foram erros honestos dentro de guardas escritas para fechar o erro anterior. E as duas coisas **não têm a mesma detectabilidade**: mesclar por cima do vermelho deixa uma execução vermelha no histórico, auditável para sempre; um gatilho mal escopado deixa **build verde e rastro nenhum**. Tratar as duas como equivalentes é o passo do argumento que não se sustenta.

**A razão que eu acho que de fato sustenta a decisão, e é melhor que a publicada:** *"toda guarda que eu escrever vai morar no mesmo YAML que ela guarda; a regressão é infinita por construção, e o único lugar em que ela pode terminar é fora do repositório — na proteção de branch. Por isso não compro a décima sexta guarda: não porque ela seria inútil contra um atacante, mas porque ela seria **mais uma instância do mesmo problema**."* Essa versão não expira, não depende de modelo de ameaça, e diz a verdade sobre o porquê.

### (c) Acionar verificação independente antes de me chamar

**Não é enganosa, e é mensuravelmente produtiva.** Ela produziu o **BL-424**, que é um achado real num eixo que nenhum critério exercitava, e eu confirmei a correção: `font-size: 8px` hoje reprova **nomeando tamanho**, e o valor vem de um token que o produto já documentava, lido por um teste que abre `static/css/base.css` de verdade. Isso é DE-055 + DE-058 funcionando juntas, e é a segunda vez que um achado de gravidade aparece **antes** da auditoria em vez de por ela.

**A ressalva, e ela é a DE-056 sobre a própria DE-055:** a verificação independente ficou nos eixos que o **plano** nomeou — contraste, tamanho, papel escuro, fronteiras pt→px. **Nenhum** dos L1–L4 veio dali. E o L1 é o mais instrutivo: ele está **dentro da lista de cláusulas** — o C3 tem uma tabela no plano, uma docstring no código, um comentário explicando a escolha de lista em vez de dicionário, e **não executa**. Ninguém rodou a sabotagem do C3. Verificação independente melhora a busca; ela não substitui **exercitar cada cláusula contra o produto**, uma por uma, com o código de saída conferido — que é o que o critério de aceite 1 a 8 do plano manda fazer e o que o C3 não recebeu.

**A regra de método que eu proporia, e é barata:** para cada cláusula da frase, **um** caso de aceite que a exercite ponta a ponta e afirme o **código de saída** e a **substring da mensagem**. Hoje existem 60 testes puros excelentes e **zero** que afirme "esta sabotagem sai com código 1 e a mensagem contém tal frase". O L1 é literalmente um `assert` desses que nunca foi escrito.

---

# Resumo dos achados

| # | Gravidade | Classe | Cláusula / requisito | Uma linha |
| --- | --- | --- | --- | --- |
| **L1** | ALTA | Testado | **C3** / BL-356 | Timbre com 2 das 3 linhas declaradas sai como **"FALHA DE INFRAESTRUTURA — não é um veredito sobre o produto"**; o código do C3 é inalcançável |
| **L2** | ALTA | Testado | **C2** / BL-424 | `transform: scale(0.6)` e `zoom: 0.6` passam com o glifo a 9,1 px, enquanto `font-size: 8px` (8,7 px) reprova |
| **L3** | ALTA | Testado | **C2** / DE-058 | `color: #FF0000` mede **8,45:1** e passa; a razão WCAG real é **4,00:1** e reprova. O número rotulado "WCAG 2.2, 1.4.3" não é o do WCAG |
| **L4** | ALTA | Testado | **C1/C3** / BL-321 | `letter-spacing: 0.2em` no tamanho real do produto → *"AUSENTE do texto do PDF"* sobre linha com **306 px de tinta** medidos na mesma execução |
| **L5** | ALTA | Testado | BL-373 | `AGENTS.md` manda o Fred marcar `Backend` e `Documentação`; os contextos reais são `Lint e testes` e `Validar documentação` |
| **L6** | MÉDIA | Inspecionado + Testado | DE-058 / BL-426 | A premissa *"`print_background` nunca é passado"* não tem teste ao lado; duas linhas resolvem |
| **L7** | MÉDIA | Inspecionado | **C5** / DE-056 | A derivação matou a cópia e não a fragilidade: a busca é substring do texto derivado |
| **L8** | BAIXA | Testado | BL-373 | `main` sem proteção nos três endpoints; nada da regra tentada hoje está no ar |
| **L9** | — | Testado | — | O que aguentou: pseudo-elemento, SVG `<text>`, metadados, decoy do C4, piso de 40, e a paginação (que erra seguro) |

**Nota sobre gravidade, declarada para você poder discordar com o mesmo dado que eu tenho:** pela letra da **DE-054**, L1 a L4 defendem o item 5 (*"o documento que sai para o cliente"*) e seriam **bloqueadores**. Não os classifico assim porque **nenhum deles devolve a marca do fornecedor à folha nem apaga a identificação do escritório com job verde** — que foi o corte que separou K1 de K2–K4 na rodada anterior, e eu mantenho o mesmo corte para não mudar a régua no meio. L2 e L3 são os que mais se aproximam: os dois produzem **job verde sobre folha degradada** abaixo de pisos que o próprio projeto fixou. Se você quiser aplicar a DE-054 ao pé da letra e chamá-los de bloqueador, o dado sustenta. **O parecer não muda nos dois casos.**

---

# Parecer

## REPROVADO

**Motivo, em uma frase:** cinco achados de **gravidade alta**, dos quais quatro estão dentro do instrumento que esta etapa entregou — duas cláusulas da frase (**C2** e **C3**) não são julgadas com fidelidade, sendo que o C3 está escrito em código que **não executa** e manda quem lê o job vermelho procurar o defeito no navegador quando o defeito está no timbre.

Não aprovo com ressalvas porque C2 e C3 são metade da frase que esta etapa foi criada para executar, e ressalva que esconde falha essencial não é ressalva.

**O que está bom e eu não quero que se perca na reprovação, porque é bastante:**

- **O BL-404 fechou de verdade.** Refiz o bloqueador da rodada 10 por um canal que ele não usava — `::after { content: ... }` com `DATALEDGER` em caixa alta e o domínio — e o instrumento saiu com código 1 nomeando a normalização. A correção é por propriedade, não por lista.
- **A cobertura de metadados do PDF não estava na minha frase.** Ela foi acrescentada pela equipe ao executá-la, e é a única coisa nesta rodada que **alarga** o critério. É o sinal mais forte de que comprar a frase valeu.
- **As cláusulas C4 e C5 aguentaram tudo que eu inventei**, inclusive a fronteira texto/desenho, que medi e está **menor** do que se declarou.
- **O BL-424 é um achado real, encontrado por verificação independente, com o valor lido do produto e teste que abre `static/css/base.css`.** Confirmei que `8px` reprova nomeando tamanho.
- **A paginação erra para o lado seguro**, e eu esperava o contrário: medi que a exportação com as opções padrão do navegador **escala** o conteúdo e é **mais** permissiva que a do instrumento.
- **Os quatro números que você declarou conferem, um a um**, e o custo do critério 9 é real: **4 s** de medição, contra 4 s na rodada 10, com cinco cláusulas a mais.
- **A tese do caminho A está confirmada pela terceira rodada seguida.** Continuo **não** recomendando revertê-la. Todo achado desta rodada foi encontrado abrindo o Chromium e olhando o PDF — inclusive os quatro que são contra o próprio instrumento de navegador.

**E o item que eu peço para você tratar antes de qualquer correção de código:** o **L5**. Corrigir dois nomes no `AGENTS.md` custa minutos, é a causa provável do erro do Fred hoje, e é o único item desta auditoria que muda a natureza de todos os outros — enquanto a proteção não estiver ligada e **lida na API**, tudo aqui, inclusive esta reprovação, é **conselho**.

**Nenhuma afirmação deste relatório é declaração de ausência de defeitos, de segurança absoluta ou de conformidade legal.** Auditoria de software não substitui a validação profissional das regras contábeis e legais pelo responsável técnico do domínio — e, em especial, **a adequação do conteúdo do timbre às normas aplicáveis ao documento contábil é decisão do Fred**, não deste relatório. Onde precisei julgar legibilidade, medi ou declarei não medido: pelo **BL-422**, não olhei imagem nenhuma para formar juízo, e as três pendências de piso — **PE-57** (o WCAG como empréstimo), **PE-58** (o tamanho mínimo no papel) e a questão nova que o **L3** abre (papel monocromático ou colorido?) — continuam sendo dele.
